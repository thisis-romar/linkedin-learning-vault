---
type: course
cssclasses:
  - lle-course
  - lle-level-intermediate
slug: learning-functional-programming-with-javascript-es6-plus-24300838
url: "https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838"
duration_minutes: 188
duration: 3h 8m
level: Intermediate
updated: 1/10/2025
learners: 1853
skills:
  - Programming
  - Web Development
  - Front-End Development
  - JavaScript
  - ECMAScript
exercise_files: true
github: "https://github.com/LinkedInLearning/learning-functional-programming-with-javascript-es6---coderpad--4252164/codespaces"
thumbnail: "https://media.licdn.com/dms/image/v2/D4E0DAQHBDe-qSJ3vaQ/learning-public-crop_675_1200/learning-public-crop_675_1200/0/1734460645917?e=2147483647&amp;v=beta&amp;t=h4mbUjkb8iOcWM5Bc5zrZmRzeM1wRSkBGsepC50Xkpg"
linkedin_topic: Software Development
learning_paths:
  - '[[Explore Web Development with Vanilla JavaScript]]'
  - '[[Improve Your JavaScript Language Skills]]'
prev_courses:
  - '[[Learning App Building With Vanilla Javascript]]'
  - null
next_courses:
  - '[[JavaScript- Modern Browser APIs]]'
  - '[[JavaScript- Async]]'
path_nav: '[{"path":"Explore Web Development with Vanilla JavaScript","position":2,"total":8,"prev":"Learning App Building With Vanilla Javascript","next":"JavaScript- Modern Browser APIs"},{"path":"Improve Your JavaScript Language Skills","position":1,"total":8,"prev":null,"next":"JavaScript- Async"}]'
path_count: 2
tags:
  - course
  - topic/software-development
  - topic/web-development
  - skill/programming
  - skill/web-development
  - skill/front-end-development
  - skill/javascript
  - skill/ecmascript
status: not-started
created: 2026-05-03
---

> <!-- lle:github-source --> [View this note on GitHub](https://github.com/thisis-romar/linkedin-learning-vault/blob/github-browse/Courses/Software%20Development/Learning%20Functional%20Programming%20with%20JavaScript%20ES6%2B.md)

![Learning Functional Programming with JavaScript ES6+](https://media.licdn.com/dms/image/v2/D4E0DAQHBDe-qSJ3vaQ/learning-public-crop_675_1200/learning-public-crop_675_1200/0/1734460645917?e=2147483647&amp;v=beta&amp;t=h4mbUjkb8iOcWM5Bc5zrZmRzeM1wRSkBGsepC50Xkpg)

# Learning Functional Programming with JavaScript ES6+

> Want to write more robust, maintainable code? In this course, discover how this can be accomplished with functional programming, and learn about the functional concepts at the heart of many JavaScript frameworks and programs. Functional programming is a radically different way of writing code. While at first it may seem a bit counterintuitive, once you get comfortable with it, you may find that yo

> [LinkedIn Learning](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838) | 3h 8m | Intermediate | 2K learners
> [Jump to Path Context ↓](#path-context)

## Table of Contents

### Chapter Overview

- [**Introduction**](#introduction) (3 videos)
  - [Functional programming will change the way you think about code](#functional-programming-will-change-the-way-you-think-about-code)
  - [What you should know](#what-you-should-know)
  - [Basic setup](#basic-setup)
- [**1. Introductory Functional Concepts**](#1-introductory-functional-concepts) (9 videos)
  - [The goal of functional programming](#the-goal-of-functional-programming)
  - [Declarative vs. imperative programming](#declarative-vs-imperative-programming)
  - [Immutability](#immutability)
  - [Separation of data and functions](#separation-of-data-and-functions)
  - [Converting object-oriented to functional code](#converting-object-oriented-to-functional-code)
  - [First-class functions](#first-class-functions)
  - [Ensuring immutability with ESLint](#ensuring-immutability-with-eslint)
  - [Installing and setting up ESLint](#installing-and-setting-up-eslint)
  - [Ensuring immutability: Finalize ESLint](#ensuring-immutability-finalize-eslint)
- [**2. First-Class Functions**](#2-first-class-functions) (9 videos)
  - [JavaScript arrow function syntax](#javascript-arrow-function-syntax)
  - [Using functions as data](#using-functions-as-data)
  - [Creating arrays of functions](#creating-arrays-of-functions)
  - [Passing functions as arguments](#passing-functions-as-arguments)
  - [Returning functions](#returning-functions)
  - [Understanding closure](#understanding-closure)
  - [Higher-order functions](#higher-order-functions)
  - [Tour of CoderPad](#tour-of-coderpad)
  - [Solution: More higher-order functions](#solution-more-higher-order-functions)
- [**3. JavaScript Array Manipulation**](#3-javascript-array-manipulation) (10 videos)
  - [Basics of functional data manipulation](#basics-of-functional-data-manipulation)
  - [The spread operator](#the-spread-operator)
  - [Using the map() function](#using-the-map-function)
  - [Using the filter() function](#using-the-filter-function)
  - [Using the every() and some() functions](#using-the-every-and-some-functions)
  - [Preventing mutation with the slice() function](#preventing-mutation-with-the-slice-function)
  - [Using the sort() function](#using-the-sort-function)
  - [Using the reduce() function](#using-the-reduce-function)
  - [Combining array functions](#combining-array-functions)
  - [Solution: Recreating the map() function](#solution-recreating-the-map-function)
- [**4. Advanced Functional Concepts**](#4-advanced-functional-concepts) (3 videos)
  - [Currying and partial application](#currying-and-partial-application)
  - [Recursion](#recursion)
  - [Memoization](#memoization)
- [**5. Code Conversion Challenges**](#5-code-conversion-challenges) (2 videos)
  - [Solution: Count occurrences](#solution-count-occurrences)
  - [Solution: Anagrams](#solution-anagrams)
- [**Conclusion**](#conclusion) (1 videos)
  - [Next steps in your ES6+ journey](#next-steps-in-your-es6-journey)

### Introduction

[↑ Back to Table of Contents](#table-of-contents)

#### [Functional programming will change the way you think about code](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/functional-programming-will-change-the-way-you-think-about-code?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/functional-programming-will-change-the-way-you-think-about-code?u=76281980&t=0)** - Writing code using [[Functional Programming]] concepts is the single most important approach you can take to ensure your applications are both maintainable and scalable. An increasing number of developers are switching from [[Object-Oriented Programming (OOP)|object-oriented programming]] to functional programming as a way to minimize the potential for bugs in their code while maximizing its readability and reusability. I'm Shaun Wassell, and in this course I'll show you the basics of functional programming in [[JavaScript]]. So let's jump right in and see how to take your JavaScript code to a whole new level.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Functional Programming]] (3), [[JavaScript]] (2), [[Object-Oriented Programming (OOP)|Object-oriented programming]] (1)
> **Speakers:** - writing (1)

#### [What you should know](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/what-you-should-know?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/what-you-should-know?u=76281980&t=0)** - [Instructor] To get the most out of this course, there are a few prerequisites that it would be helpful for you to know. The first is a basic knowledge of [[JavaScript]] and ideally, some experience with ES6 syntax. If you're not already comfortable with ES6 syntax, it's not hard to learn. If you're interested, I'd recommend checking out one of the ES6 courses in our library. It'd also be helpful for you to have some experience with basic command line operations. All we'll be using for this course is basic commands like mkdir to make directories, cd to change directories, and ls to see the contents of a directory, as well as a few commands specific to NodeJS. Keep in mind also that if you're on [[Windows]], the commands might be a little different. There are articles out there like this one that show many of the basic [[Linux]] commands and their Windows equivalents. And finally, since I'll be using it as a reference point throughout this course, a basic knowledge of [[Object-Oriented Programming (OOP)|object-oriented programming]] concepts would be helpful to have as well. And even if you're not familiar with object-oriented programming, it's not a big deal. You should still be able to follow along comfortably with the examples. While I highly recommend that you follow along with me as I write the code, I've also included the finished state for all the code that I write in this course, in the exercise files for your reference. So basically, this course is for low intermediate to professional developers who have a desire to improve their code by learning and applying functional concepts. If you fit that description, then this course is definitely for you.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Windows]] (2), [[Object-Oriented Programming (OOP)|Object-oriented programming]] (2), [[JavaScript]] (1), [[Linux]] (1)
> **CLI Commands:** mkdir (1), make (1), cd (1), ls (1)
> **Env Vars:** es6 (3)
> **Tools:** command line (1)
> **Exercise Files:** exercise files (1)
> **Definitions:** is a  (1)
> **Warnings:** keep in mind (1)
> **Speakers:** - [instructor] (1)

#### [Basic setup](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/basic-setup?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/basic-setup?u=76281980&t=0)** - [Instructor] All right, well, one last thing before we get started here is, depending on how you're planning on following along, assuming, of course, that you are planning on following along, you're going to want to make sure that you have the latest version of [[Node.js]] installed. And you can do that by heading over to Node.js's website. And there's a link here to download Node.js, the LTS version, which you're going to want there. You can see that currently that's 20.18.0, but you might see a different number there, and that's all right too. As long as you have at least that version, everything should be good here. So, once you've done that, if you're going to follow along locally, you should be able to run the command node -v and see that version. You can see one minor version behind, but we'll just go with that. And you should also be able to run [[npm]] -v and see a version that looks something like what I have here. Now, that's if you're going to follow along locally. If you want to follow along using [[Codespaces]], what you're going to want to do is head over to the repo for this course, and it might look a little bit different. The name might not be quite the same as what you see here, but you should just be able to click on the Code dropdown for that. Go to the Codespaces tab, and then there should be a button here that says Create codespace. You can see that I've already created one and they assign random, somewhat humorous names to them, as you can see. And the codespace is going to look an awful lot like Visual Studio Code. And if you want to follow along using this, it's going to make it a lot easier
>
> **[1:33](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/basic-setup?u=76281980&t=93)** to check out the correct branches for the exercise files. But whether you want to do this locally or in Codespaces, that's up to you, and that should be all we need to do. Oh, and just one last thing, is that Codespaces should already have Node.js installed for you. You can see that we have the same versions there. npm -v. And that gives us pretty much the exact same thing actually, as what we had locally. So, anyway, now that we've gone over that, let's jump right in and get started.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Node.js]] (4), [[Codespaces]] (4), [[npm]] (2)
> **CLI Commands:** node (5), make (2), npm (2)
> **File Paths:** node.js (4)
> **UI Navigation:** dropdown (1), go to (1)
> **Env Vars:** lts (1)
> **Versions:** 20.18.0 (1)
> **Tools:** visual studio (1)
> **Exercise Files:** exercise files (1)


### 1. Introductory Functional Concepts

[↑ Back to Table of Contents](#table-of-contents)

#### [The goal of functional programming](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/the-goal-of-functional-programming?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/the-goal-of-functional-programming?u=76281980&t=0)** - [Narrator] So, the big question most people ask when they hear about how useful [[Functional Programming]] is is, "What exactly is functional programming?" Well, to answer this question, it might be helpful to consider the question of why we would want to use functional programming in the first place. In other words, what are the problems that functional programming sets out to solve? Well, if you currently use an [[Object-Oriented Programming (OOP)|object-oriented programming]] language, such as [[Java]] or C++, or especially a procedural programming language such as C, you've probably run into this problem before. There are certain bugs that are difficult to track down and fix, partly because they're hard to recreate, it takes a long, convoluted series of steps to make them show up, and partly because even when you figure out how to recreate them, it's next to impossible for you to keep track of all the changes that occur while the program is running. In a typical enterprise sized program, thousands of variables are being operated on in thousands of different places at runtime, and the application can get itself into buggy states, which can be difficult to recreate. This is exactly the type of situation that functional programming aims to avoid. Once you learn to think and program in a functional style, this type of situation will become much less frequent. You'll learn many techniques that can be used to create very large, powerful, and virtually bug-free code bases that are composed of many smaller, self-contained, and easily testable parts. So then, at its core, functional programming is concerned with taking a large number of complex ideas present in any large computer program and organizing them in a coherent way,
>
> **[1:34](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/the-goal-of-functional-programming?u=76281980&t=94)** while at the same time making sure that the code remains easy to test and modify. Of course, this sort of organization is what object-oriented programming aims to do as well, although it achieves it with only varied success. The difference between object-oriented and functional programming is in the choices they make with regards to organizing code. As you probably know already, the basic idea behind object-oriented programming is that humans think in terms of objects and relationships between these objects, and therefore computer programs should be organized in terms of objects so that we can leverage our existing thought patterns to write programs more easily. The problem with this, of course, is that our thought patterns are often not as well defined as we sometimes like to think. Once we go beyond contrived examples, such as SUV is a subclass of car, car is a subclass of vehicle, which demonstrates the concept but isn't very helpful, the object-oriented programming doctrine doesn't provide much guidance regarding code organization. We're left with a rather daunting array of concepts like interfaces, subclasses, constructors, public, private, and protected data, accessors, mutators, and method overriding, all of which form a complicated web of relationships and are prone to abuse by all but the most experienced and disciplined programmers. On the other hand, functional programming aims to bring the precision of mathematical functions into computer programs. While there will always be room for human error in programming, no matter what paradigm is being used, adding the provable certainty that comes with mathematics into computer programming makes it far easier to avoid bugs.
>
> **[3:09](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/the-goal-of-functional-programming?u=76281980&t=189)** Imagine if we were able to represent all parts of a computer program as simply as a mathematical function, such as function of X equals x plus one. Where could a bug possibly be hiding in functions like this? We'll look further into the details of functional programming along with the many differences between it and object-oriented programming throughout the [[Representational State Transfer (REST)|rest]] of the course. Since many of you are probably most familiar with object-oriented programming, I'll continue to use it in a lot of comparisons with functional programming to clarify the concepts I show you.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Functional Programming]] (10), [[Object-Oriented Programming (OOP)|Object-oriented programming]] (6), [[Java]] (1), [[Representational State Transfer (REST)|Rest]] (1)
> **Analogies:** such as (4), imagine (1)
> **Definitions:** is a  (2), in other words (1)
> **CLI Commands:** make (2)
> **Env Vars:** suv (1)
> **Speakers:** - [narrator] (1)

#### [Declarative vs. imperative programming](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/declarative-vs-imperative-vs-programming?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/declarative-vs-imperative-vs-programming?u=76281980&t=0)** - [Instructor] One of the first things you'll want to understand about [[Functional Programming]] is that it's a declarative style of programming as opposed to things like [[Object-Oriented Programming (OOP)|object-oriented programming]] and procedural programming, which are imperative styles of programming. What this means is that functional programming puts more focus on what things are as opposed to how to get them. A simple example of this would be if we were to talk about houses. If we wanted to describe a house in a declarative way, if we wanted to describe what a house is, we might say that a house is a foundation, four walls and a roof on top. On the other hand, if we were describing a house in an imperative way, if we wanted to describe how to make a house, we might say, in order to make a house, you need to pour the foundation, build four walls, and then put a roof on top. Let's expand on this a bit and look at a more programming related example. Let's say that we want to write a program that tells us the average of an array of numbers. Imperative programming would specify the steps required to calculate the result, something like this. First, we set X equal to zero. Second, we add the first number in the array to X, and then we repeat step two for the [[Representational State Transfer (REST)|rest]] of the numbers in the array and finally, we divide X by the length of the array. Now, unless you've worked with functional programming before, this is probably how you're used to writing programs, thinking of how to get the result. On the other hand, if we wanted to solve the same problem in a declarative way, we could simply say that X is the sum of all the numbers in the array divided by the length of the array.
>
> **[1:33](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/declarative-vs-imperative-vs-programming?u=76281980&t=93)** Or if you're more mathematically inclined, we could rewrite it using mathematical notation like this. Now, if it never occurred to you that it's possible to write programs in this way, by simply stating what something is, you're not alone. You probably notice that talking about things declaratively, talking about what they are, instead of how to get them, feels a lot more natural and well, frankly, it's a lot easier for many problems. This way of talking about things may also remind some of you of mathematical functions such as these, and as I mentioned before, this is one of the main ideas behind functional programming to bring the precision of mathematical functions into our programs. As you may have figured, this is also where functional programming gets its name. So it's all very easy to say that functional programming brings the precision of mathematics into programming. But what does that mean exactly? Well, there are three main concepts behind functional programming which allow us to do just that. These concepts are one, immutability, two, separating functions and data, and three first-class functions. Over the course of the next few videos, I'll go into depth on what each of these concepts means, contrast it with how other paradigms do things and explain its advantages.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Functional Programming]] (7), [[Object-Oriented Programming (OOP)|Object-oriented programming]] (1), [[Representational State Transfer (REST)|Rest]] (1)
> **CLI Commands:** make (2)
> **Definitions:** is a  (1)
> **Analogies:** such as (1)
> **Speakers:** - [instructor] (1)

#### [Immutability](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/immutability?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/immutability?u=76281980&t=0)** - [Narrator] The first major concept of [[Functional Programming]] is immutability, and it's a concept that may surprise a lot of people at first. Most programmers learned early on that you could assign a value to a variable. For example, we can define a variable called X and store the value five in it, and then later on in the program, we can change the value of that variable to some completely different number, and later on we can change its value again, and so on. However, in functional programming, this is not allowed. When we say that X is equal to five, we mean that for the [[Representational State Transfer (REST)|rest]] of the program, X will only ever be five. There's no way we can change it. In short, immutability means that most of the values in a program are constant, which in [[JavaScript]] means using the constant keyword instead of the var or let keywords. Another way to think about it is this. In object-oriented and procedural programming, we treat variables as buckets that we can put values into. We call this assigning values to variables. So at one point in time, X might hold the value three. At another point it might hold the value 10. At another point, it might be negative four, and so on. On the other hand, in functional programming, we don't assign, we define. Instead of naming buckets that hold different values at different times, we name the values themselves. When we say that X equals three, we don't mean that X is a container that's currently holding the value three. We literally mean that X is three, in the same way that Pi is 3.14159 et cetera. Could you imagine changing the value of Pi? Of course not.
>
> **[1:33](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/immutability?u=76281980&t=93)** Functional programming treats all values as if they were as concrete and unchanging as Pi or any other mathematical constant. And so once we've defined X, for the whole entire program, X can never be anything else. It's just another name for whatever value we've defined it as. At first glance, this may seem strange to most programmers. After all, what good is a program where you can't change anything? How can a program like that be useful at all? Let's look at a quick example. What if in a certain theoretical program we have an employee and we want to raise their salary? Well, in [[Object-Oriented Programming (OOP)|object-oriented programming]], of course, we can just change it directly, usually by calling a member function like this. In functional programming, on the other hand, we would instead define a new constant that represents the updated data and then use this constant in our future calculations. Notice here also that I'm not using the new keyword as we do an object oriented programming. I'm just defining our data in a JavaScript object. We'll learn more about why this is later on. The advantage of immutability and the reason that functional programming places such an emphasis on it is that it frees us from having to deal with state change. When a program contains many variables that are all changing constantly at different times, it can be very hard to know what state a program is in at any given point in time. As programs increase in size to include thousands or even millions of individual variables, this can lead to hard-to-find bugs and an overall fragile code base that programmers are afraid to make changes to. This is a problem that even [[Test-Driven Development]]
>
> **[3:06](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/immutability?u=76281980&t=186)** can't solve completely, since the task of testing all possible states that a program might get itself into is nearly impossible in programs of considerable size. In functional programming, on the other hand, we start off with an immutable set of data as a single source of truth, and then we use functions to combine this data piece by piece, and transform it into useful information. This data is usually retrieved from a database or some other memory storage. There are two powerful advantages to this approach. The first is that the original data in a program will always remain intact, which makes bugs much easier to find. The second is that programs constructed in this way are much easier to keep track of, since you can focus on any given piece individually. The only thing that determines the output of a given piece is the input. We don't have to think about the entire system all the time. If the concept of immutability doesn't quite make sense to you yet, or if the advantages don't seem apparent, don't worry. Later on in the course, we'll take a look at many examples that incorporate immutability. For now, just remember that in functional programming, all data is immutable, so use constant instead of let or var.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Functional Programming]] (8), [[JavaScript]] (2), [[Representational State Transfer (REST)|Rest]] (1), [[Object-Oriented Programming (OOP)|Object-oriented programming]] (1), [[Test-Driven Development]] (1)
> **Definitions:** is a  (3), means that (1), we call this (1)
> **CLI Commands:** find (2), make (2)
> **Analogies:** for example (1), imagine (1)
> **Versions:** 3.14159 (1)
> **Speakers:** - [narrator] (1)

#### [Separation of data and functions](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/separation-of-data-and-functions?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/separation-of-data-and-functions?u=76281980&t=0)** - [Instructor] The second main concept of [[Functional Programming]] is the separation of data and functions. This is a case where functional programming is directly in contrast with [[Object-Oriented Programming (OOP)|object-oriented programming]], where data and functions are almost always grouped together. Let's see what this looks like. First of all, for our purposes, let's discuss what data and functions actually are. For our purposes, data might be any values that a program contains. People's names, addresses, and social security numbers in a payroll program. The models years, and colors of cars on a used car website. The positions, health levels, and weapons of characters in a video game, anything. All of this is data. In object-oriented programming, this data is usually wrapped up inside objects as member variables, and the only way we can access it is using an object's methods. In functional programming on the other hand, this data is represented by simple arrays and hashes, or in this case, [[JavaScript]] objects. And a function is any operation that we can apply to our data to convert it into useful information. For example, if we want to find the average salary of programmers at our company, find all the cars on our website that were made after a certain year, or find if two characters are colliding in our video game. In object-oriented programming, functions are wrapped up inside objects along with the data that they operate on, and allow us to access or make changes to that data. In functional programming on the other hand, functions are completely separate entities from the data that they operate on. In order to operate on given data, the data must be passed as arguments to the function
>
> **[1:34](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/separation-of-data-and-functions?u=76281980&t=94)** instead of using the this keyword as in object-oriented programming. And because of the rule of immutability, they should never make changes to any of the data they touch. They only return a modified copy of that data.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Functional Programming]] (4), [[Object-Oriented Programming (OOP)|Object-oriented programming]] (4), [[JavaScript]] (1)
> **CLI Commands:** find (3), make (2)
> **Definitions:** is a  (1)
> **Analogies:** for example (1)
> **Speakers:** - [instructor] (1)

#### [Converting object-oriented to functional code](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/converting-object-oriented-to-functional-code?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/converting-object-oriented-to-functional-code?u=76281980&t=0)** - If you're coming from an object-oriented background, it may seem strange to you that [[Functional Programming]] places an emphasis on keeping data and functions separate. Since one of the central concepts of [[Object-Oriented Programming (OOP)|object-oriented programming]] is that we put data and the functions related to that data together in objects. To understand why it makes sense to keep data and functions separate and functional programming, it might be helpful to think about why we keep them together in object-oriented programming in the first place. The main reason that it's useful to keep data and functions together in the same object is that this allows us to interact with the member variables of an object without touching them directly. What's wrong with touching variables directly? Well, if we give programmers direct access to variables, they can, and in my experience, will change them in ways that they're not supposed to. Consider the simple case of a person object with publicly accessible properties, first name, last name, and initials. Now, if we allow programmers to change these properties directly, they have to remember to reset initials every time they change either first name or last name, which they're almost certain to forget somewhere, which introduces bugs in the program. The most popular solution to this is to make the variables private, which in [[JavaScript]] is usually done by just adding an underscore before the variable name and telling developers not to touch it, and then we allow programmers access to the variables only through functions, which make sure that the variables are kept up to date. In functional programming however, we don't need to worry about making all our data private because programmers can't alter the data in unwanted ways.
>
> **[1:33](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/converting-object-oriented-to-functional-code?u=76281980&t=93)** Because of the rule of immutability, which we discussed in the previous video, the data remains a constant source of truth. Note that in JavaScript we need a little extra help to keep developers from modifying our object properties, but we'll talk about that in the coming video. If we want to transform the data in some way, such as changing someone's name, we simply define a new constant that represents the updated data. We then use this updated constant in all our future calculations knowing exactly what data it represents. Let's take a look at a simple example to demonstrate how functional programming and object-oriented programming handle functions and data differently. Let's imagine the classic example of creating a simple to-do list program that helps people keep track of tasks they need to complete. Let's look at what an object-oriented approach to this program would look like. First, we'd create a to-do item class that contains properties such as the name, category, and deadline for the item and whether or not it's been completed, as well as functions such as change, name or mark as done, and then of course, our getters to access our private variables. Then we might have another class called to-do list, which contains an array of to-do items as well as functions for adding or removing items or sorting them in different ways. In functional programming on the other hand, we'd keep the data and functions separate. Remember that in functional programming, instead of wrapping up our data's properties as member variables and side classes, we usually represent data using constructs such as arrays or hashes, which in JavaScript, somewhat confusingly, we call objects.
>
> **[3:08](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/converting-object-oriented-to-functional-code?u=76281980&t=188)** So for this program, we might have JavaScript objects that represent to-do items with name, category, and deadline properties, and the list itself, which we would simply represent with an array that holds a bunch of to-do items. Then outside of these classes, we define the functions for combining or transforming our data, such as sorting it or marking an item as done.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Functional Programming]] (6), [[JavaScript]] (4), [[Object-Oriented Programming (OOP)|Object-oriented programming]] (3)
> **Analogies:** such as (3), imagine (1)
> **CLI Commands:** make (2)
> **Cross-References:** we discussed (1), previous video (1)
> **Warnings:** note that (1)
> **Speakers:** - if (1)

#### [First-class functions](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/first-class-functions?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/first-class-functions?u=76281980&t=0)** - [Instructor] The third major concept of [[Functional Programming]] is something called first-class functions. This is where things really start to get interesting and where the power and flexibility of functional programming really starts to shine through. So what exactly do we mean when we talk about first-class functions? Well, in [[Object-Oriented Programming (OOP)|object-oriented programming]], we generally treat data and functions as different types of entities. We would probably never think of, for example, creating an array of functions or passing functions as arguments to other functions, or even stranger, returning functions from other functions. However, in functional programming, it's not only possible to do these things, it's, in fact, a source of tremendous flexibility. This is exactly what we mean when we say that a language supports first-class functions. It allows us to treat functions in a very similar way to how we normally treat other values, such as numbers, strings, and objects. But hold on a minute. Doesn't this go against what I said in the previous video about the separation of data and functions in functional programming? Well, at first glance, it might seem like it, so let me clarify. In object-oriented programming, since functions and data are both defined within the same class, they tend to be tightly coupled. In other words, the functions inside a given class usually reference the data that they operate on directly, as is the case here with this._name. In other words, you generally can't take a given function and remove it from the class it's in and still have it work on its own. In functional programming, on the other hand, we design our functions specifically so that they work in isolation.
>
> **[1:33](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/first-class-functions?u=76281980&t=93)** All of the data that a function needs to operate on is passed in through the arguments. And the only thing that will change the output of the function is a change in the arguments. In other words, functions aren't influenced by any kind of internal or external state. There's a name for this kind of function. We call them pure functions, and in functional programming, we try our best to make every single one of our functions pure. And this is what we mean when we say that we want to keep data and functions separate. Now, you might be wondering why we'd want to do stuff like this. What could we possibly gain from doing weird things like having arrays of functions or passing functions as arguments? In fact, this simple change in mindset toward functions opens up a world of possibilities in terms of [[Software Design]]. It gives us a lot of flexibility and greatly increases our opportunities for code reuse. It also allows us to do useful things like combine existing functions to create new functions. We'll see in the next chapter how this is done and what it's implications are for our programs.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Functional Programming]] (6), [[Object-Oriented Programming (OOP)|Object-oriented programming]] (2), [[Software Design]] (1)
> **Definitions:** in other words (3), is a  (1)
> **Cross-References:** previous video (1), in the next (1)
> **Analogies:** for example (1), such as (1)
> **CLI Commands:** make (1)
> **Speakers:** - [instructor] (1)

#### [Ensuring immutability with ESLint](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/ensuring-immutability-with-eslint?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/ensuring-immutability-with-eslint?u=76281980&t=0)** - [Instructor] So I talked in a previous video about the importance of immutability in [[Functional Programming]]. Immutability allows us to avoid the many bugs that occur in computer programs as a side effect of state change. It also means that we don't have to worry about using private variables to avoid changes coming from unexpected places, since we simply can't make changes to any piece of data. That being said, if we want to ensure complete immutability in [[JavaScript]], there's something we need to keep in mind. While using the const keyword prevents us from directly changing the value of a piece of data, for example, if we say const X equals five, we can't simply say X equals six, since this will throw an error. However, using constant JavaScript is a little funny when working with arrays and objects. For example, if we define an array called numbers, like this, constant numbers 1, 2, 3, 4, 5, we can never directly redefine numbers as a different array like this, numbers equals hello. But we can modify the individual elements of an array like this. Numbers zero equals 100. And JavaScript won't complain about this at all, even though we've defined our numbers array using the const keyword. Also, if we're not careful, we can accidentally call a mutating array method such as reverse on a supposedly immutable array like this. Numbers reverse, and this will actually modify the original array
>
> **[1:35](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/ensuring-immutability-with-eslint?u=76281980&t=95)** without any complaint from JavaScript. And the same is true for objects as well. If we define an object called person, const person, with name and age properties, name [[John the Ripper|John]] Doe, age 34, we can never directly redefine person as another object like this, person equals name Bob. But if we try and reset a property of our object like this, person.name equals Bob, JavaScript won't complain even though this is modifying our data. Now I'm going to show you an approach that we can use to prevent mutation in our code, and this is by using [[ESLint]] rules. If you've never worked with or heard of ESLint before, it's an extremely helpful tool that helps you catch errors or stylistic flaws in your code. ESLint won't add anything extra to your code. We can simply run it and it looks at our existing code and tells us any problems it finds, for example, if we're doing accidental mutation. Now there are many different ESLint plugins that we can use depending on our coding style and concerns. There are ones that check for correct indentation, ones that make sure you put semicolons at the end of every line, et cetera. But for the purposes of this course, the Lint rules we're interested in are the ones that will automatically tell us if we try and mutate anything in our code there. There are several existing plugins that already implement this functionality, and the one we're going to use is called ESLint Plugin Immutable. I'm not going to go through all the details about ESLint here,
>
> **[3:10](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/ensuring-immutability-with-eslint?u=76281980&t=190)** but I'll just walk you through the steps required to get this set up.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[ESLint]] (6), [[JavaScript]] (5), [[Functional Programming]] (1), [[John the Ripper|John]] (1)
> **Analogies:** for example (3), such as (1)
> **Definitions:** means that (1), is a  (1), is called (1)
> **CLI Commands:** make (2)
> **Cross-References:** previous video (1)
> **Warnings:** keep in mind (1)
> **Prerequisites:** set up (1)
> **Speakers:** - [instructor] (1)

#### [Installing and setting up ESLint](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/installing-and-setting-up-eslint?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/installing-and-setting-up-eslint?u=76281980&t=0)** - [Instructor] The first thing we're going to do is open up a terminal inside our Exercise Files folder. I'm going to use the integrated terminal inside VS code, but you're more than welcome to use Mac's built-in terminal, as well. And then inside our Exercise Files folder we're going to initialize an [[npm]] package with npm init -y. Once we've done that we'll install [[ESLint]] as a dev dependency by typing npm install --save-dev eslint.
>
> **[0:32](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/installing-and-setting-up-eslint?u=76281980&t=32)** Then we'll initialize eslint for our project by running npx eslint--init. And then it's going to ask us a series of questions, so we'll answer those. And to make things easier on ourselves we're going to select use a popular style guide, when it asks how would you like to define a style for your project. This enables us to use a predefined set of rules. And we're going to select Airbnbs. And now we see that eslint created this eslintrc.js file for us. This file tells eslint what to look for in our code.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[ESLint]] (6), [[npm]] (3)
> **CLI Commands:** npm (3), npx (1), make (1)
> **Tools:** terminal (3), vs code (1)
> **Exercise Files:** exercise files (2)
> **Prerequisites:** install (2)
> **File Paths:** eslintrc.js (1)
> **Env Vars:** npm (1)
> **Speakers:** - [instructor] (1)

#### [Ensuring immutability: Finalize ESLint](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/ensuring-immutability-finalize-eslint?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/ensuring-immutability-finalize-eslint?u=76281980&t=0)** - [Instructor] The next thing we're going to do is install the [[ESLint]]-plugin-immutable package for our project. So in the terminal, we'll type [[npm]] install --save-dev eslint-plugin-immutable,
>
> **[0:16](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/ensuring-immutability-finalize-eslint?u=76281980&t=16)** and hit Enter. And then all we have to do is add some of these rules to our eslintrc file. So we'll open up eslintrc.js, and add a plugins property right above rules. plugins. And it's going to be an array. And then we simply add our immutable package to it, like this, immutable. And don't forget to add the commas. Then inside the rules object, we're going to add a rule from immutable. Specifically, we're going to use the immutable/no-mutation rule. And we're going to give it the value 2 because we want ESLint to treat this as an error if it finds it, not just a warning, which would be 1. And for those of you who are wondering, if we want to specifically disable a rule, we could set this value to 0, like this. But we're going to leave it as 2. Let's save our file as well. So now, if we delete all this code and rewrite some code that we might find when we're accidentally mutating our data, like this. const person equals, name, [[John the Ripper|John]] Doe, and age equals 34. And then we try and mutate the name property. person.name equals Bob. And then we save our code. And then if we go into our terminal and type npx eslint, and then the directory that we want to run lint on,
>
> **[1:48](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/ensuring-immutability-finalize-eslint?u=76281980&t=108)** in this case, just the current directory we're in. So we'll use dot, and hit Enter. We see that in addition to a few other lint errors, ESLint also gives us an error saying that no object mutation is allowed. Keep in mind also that if you don't want to use the default rules from Airbnb, you can go into your eslintrc and simply delete extends: 'airbnb-base' from the object. But I'm going to leave it in there. So there you have it. When we're creating a functional codebase and want to ensure that we don't accidentally mutate any of our data, we can set up ESLint to catch any mutations for us.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[ESLint]] (6), [[npm]] (1), [[John the Ripper|John]] (1)
> **CLI Commands:** npm (1), find (1), npx (1)
> **Prerequisites:** install (2), set up (1)
> **Tools:** terminal (2)
> **Warnings:** warning (1), keep in mind (1)
> **File Paths:** eslintrc.js (1)
> **Best Practices:** don't forget (1)
> **Speakers:** - [instructor] (1)


### 2. First-Class Functions

[↑ Back to Table of Contents](#table-of-contents)

#### [JavaScript arrow function syntax](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/javascript-arrow-function-syntax?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/javascript-arrow-function-syntax?u=76281980&t=0)** - [Instructor] Alright, so now that we've taken a look at a number of more theoretical concepts that have to do with [[Functional Programming]], let's start moving into something more concrete. Now, throughout the [[Representational State Transfer (REST)|rest]] of the course, we're going to want to take full advantage of [[JavaScript]]'s syntax features, and the first thing that we're really going to want to do here is we're going to want to get a little bit more familiar with JavaScript's syntax for defining functions, right? Because in functional programming, we're going to be creating functions so frequently, it's important that we're very familiar with all of the different ways that you can define functions in JavaScript. Alright, so what I'm going to do first of all is I'm going to create a new folder here, and I'll just call this something like examples. And inside this folder, I'm going to create a new file, and I'll call this something like arrow-functions, or you know what, we'll call it function-syntax.js. And inside here, let's start off with taking a look at the most well known ways of defining functions in JavaScript, and then we'll take a look at all of the different ways that we can define functions. So first of all, if you want to use the function keyword... And there is a key difference there that I'll discuss shortly between using the function keyword and not using the function keyword. But if you want to use the function keyword, there's really two ways of doing this. You can say, function myFunction, and then if that function has arguments, it's going to look like that, (arg1, arg2), and then you'd have the function body right here, right?
>
> **[1:35](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/javascript-arrow-function-syntax?u=76281980&t=95)** Maybe you want to just return the sum of these two numbers. Well, that's probably the most common and definitely the oldest way of defining functions, and by that I don't mean that there's anything wrong with this. But there's another way of doing this that really fits a little bit more into this idea of first class functions as we'll see, and that looks like this. We can actually say, const myFunction, and I'm going to just say myFunction2 here to give it a slightly different name than this other one here. And we can just say = function, with those two arguments there. So in this case, we're using the function keyword still, but we're doing it a little bit differently. Again, there's a key difference here between these two ways of defining things, and I'll discuss that in more detail shortly. Now, in more recent versions of JavaScript, and they're not really that recent, this was a thing that started with ES6. In more recent versions of JavaScript, we can actually get rid of this function keyword entirely by using something called arrow functions. Now, you may already be very familiar with arrow functions, or you may not, but either way, since I'm going to be using this syntax very heavily throughout the rest of the course, let's go through a few examples to get a little bit more comfortable with them. So let's start with a very simple example. We're just going to do this same example of adding two numbers together. The most long-winded way to write this using arrow notation would be something like this. We could say const add,
>
> **[3:07](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/javascript-arrow-function-syntax?u=76281980&t=187)** I'm just going to call it add at this point. And then we say =, and then we can just jump right to the parentheses, right? So we can say, you know, (arg1, arg2), and then we can say something like const sum = arg1 + arg2, and then we'll just return the sum. Now again, this is the longer way of writing arrow functions. And the reason that I've started here is because I want to give us the chance to see a few nice things that arrow functions allow us to do syntax wise for specific cases. So the first thing here is if we have an arrow function that has only one statement in the function body, and that statement is a return statement... So if we were to rewrite our add function, which I'm just going to comment out here. If we were to rewrite our add function like this, and say const add = (arg1, arg2), and then just sort of cut out that const sum in the function and just say return arg1 + arg2, well, we can actually shorten this function, and arrow functions allow us to do that by removing the return keyword, and we also have to remove the curly braces, right? So basically, this is going to do exactly the same thing as this longer version up above, but as you can see it's much shorter, which when you're working with functions on a very, very regular basis as we're going to be in functional programming, every little syntax difference helps. So anyway, there are a few more nice things that arrow functions allow us to do syntax wise.
>
> **[4:42](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/javascript-arrow-function-syntax?u=76281980&t=282)** The next thing that we're going to look at is let's say that we have a function that takes only one argument. We're going to call this function something like double. And here's what that would look like. We could say that it takes a number and returns that number times two, right? Well, when we have a function that has only one argument in it, what arrow functions allow us to do is actually remove the parentheses from around that argument, and that gives us something that looks like this, right? Now, you may or may not miss those parentheses around the arguments, it may make it a little bit easier to tell what you're looking at there with those parentheses, so you can choose whether you actually want to take them off or not. But anyway, that is the next sort of syntax improvement that JavaScript gives us. And just as a side note here, if you have a function that doesn't take any arguments, and you want to use arrow function syntax for this, right? We'll just say something like const twoPlusThree. Well, in that case, you do have to add those parentheses back on. So it's only in the situation where you have a single argument that you can remove those parentheses. You'll actually see later on that this is a very frequently encountered situation. So, just keep that one in mind because we'll be coming back to it later. Now, one more thing to keep in mind is if we want to write an arrow function that returns an object, as we will many times in functional programming for reasons that you'll see a little bit later, what we need to do
>
> **[6:15](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/javascript-arrow-function-syntax?u=76281980&t=375)** if we're planning on omitting those brackets or curly braces, is we actually have to put extra parentheses around the object that we're returning. So here's what this is going to look like. We'll just say something like const createPersonData. And what this is going to look like, it's just going to be a function that doesn't take any arguments and returns an object. And as I said, we have to put parentheses around the curly brackets of the object so that JavaScript knows it's an object. So we could say something like name: 'Jane Doe' here, age, maybe 35, and job, we'll just say programmer, right? Alright, so that's just something to keep in mind there. And notice that if you try and remove these parentheses, the reason that we need them there in the first place is because if we leave them off, JavaScript thinks that this is a function body, and it gets a little bit confused that it finds things that look like an object's attributes inside of it. So that's why we need those parentheses around the curly braces. We'll just add those back. And anyway, just to review the several different ways that we can define functions in JavaScript, the first way is using the function keyword, the second way is using the function keyword and actually assigning that to a variable. We'll talk about that in more detail shortly. The third way is using arrow functions with a full function body and the return keyword. The fourth way is omitting the return keyword, if you have a short enough function body.
>
> **[7:48](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/javascript-arrow-function-syntax?u=76281980&t=468)** The fifth way is if you have only one argument, you can remove those parentheses around the arguments. The sixth way... Actually, this is just sort of a repeat of what we had up there, so we'll count this as number six here. The sixth way is if you're returning an object from an arrow function and you're using this shorter syntax that we saw up here, you need to put parentheses around the object that you're returning. Alright, so there's really six or seven different ways that you can keep in mind here to write functions in JavaScript. And the last thing I wanted to point out here is what makes this first function syntax significantly different from the other syntaxes here. There's one key difference, and that is that functions that are defined this first way are hoisted, right? There's a [[Microsoft Word|word]] in JavaScript called function hoisting. And basically what that means is if we define this function anywhere in the file, then we can use it before the function definition, right? So we could still say myFunction(1, 2) for example, up here at the top, and it would work. Now, that is the only function definition that we're allowed to do that with. If we were to do that with any of these other functions here, because these functions are defined as variables, those functions wouldn't be hoisted and we would get an error. Alright, so anyway, this has been an in-depth look at how to define functions in JavaScript, and the many different syntaxes for doing so.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[JavaScript]] (13), [[Functional Programming]] (4), [[Representational State Transfer (REST)|Rest]] (2), [[Microsoft Word|Word]] (1)
> **Code Identifiers:** myfunction (3), myfunction2 (1), twoplusthree (1), createpersondata (1)
> **Definitions:** is a  (4), defined as (1)
> **Warnings:** keep in mind (3)
> **File Paths:** function-syntax.js (1)
> **CLI Commands:** make (1)
> **Env Vars:** es6 (1)
> **Analogies:** for example (1)

#### [Using functions as data](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/using-functions-as-data?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/using-functions-as-data?u=76281980&t=0)** - [Instructor] All right, so now that we've taken an in-depth look at the syntax or syntaxes, plural, of defining functions in [[JavaScript]], let's move on to the next topic. And that is, I mentioned in a previous video that [[Functional Programming]] treats functions as first class citizens, meaning that we can treat functions in a very similar way to other types, such as strings, numbers, or objects. So what I'm going to do here, first of all, is create a new file. And we'll call this something like functions as data.js. All right, so what we're going to do here is we're going to take a look at what this concept of first class functions means for our code and a few useful things that we can do with it. Now, the easiest way to demonstrate how first class functions can be used like other data types, is with the following example. Let's say that we start off with a very simple function here. We'll just call it sayHello and all that this function is going to do is it's just going to say console.log and log out the message, "Hello" to the console. All right, notice how I've used the very compact, arrow function syntax for this. You could have done this really in any other way if you wanted. So now that we have this function, the next thing that we can do is we can actually define another function, right? We'll call it, just sayHello2, and we can set it equal to our function, sayHello. All right, so now that we've done this, which might already seem a little bit strange to you, certainly this isn't something that we usually do
>
> **[1:34](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/using-functions-as-data?u=76281980&t=94)** outside of functional programming, we can actually call sayHello2 in the same way that we called sayHello, right? Just by adding parentheses to the end. And if we run our code, which by the way you can do by saying node and the path to the file, in our case, it's going to be examples/functions-as-data.js. And sure enough, we see that that logs out Hello to the console. All right, so in other words, because functions in functional programming can be treated in the same way as any other data type, we can assign them to variables just like we can do the same thing with, you know, strings, numbers and so on. So let's take this a step further. This same thing that we saw here is also true if we add arguments to our functions. So what I'm going to do here is I'm going to say something like here, in fact, we'll just modify this one here. We'll say const sayHello and we'll make this function, take a name argument, and I'm going to remove the parentheses from around that there just to go along with what I was talking about previously about the different functions syntaxes and the shortcuts we can take. So now we're going to sayHello and we'll add that name to the end of this. So we'll say plus name and we'll add an exclamation point there because it's exciting of course. All right, so now if we wanted to call, sayHello, we could just say, sayHello with parentheses after it. And I'm just going to use my name here, but of course, you can use yours and we can do the same thing with sayHello2
>
> **[3:08](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/using-functions-as-data?u=76281980&t=188)** just by saying const sayHello2 = sayHello. We can put parentheses after that and I'm going to use my name again and let's just run this here. And sure enough, we'll see that those two functions behave in exactly the same way. All right, so you've probably noticed by now, and I think I've pointed this out already, that when defining functions with the ES six arrow syntax, we use the const keyword. And this is exactly the same syntax that we use when defining other types, right? So just like how we can say, you know, const x = 1 or const message = 'Hi!' Right? We can say const function = and then we can have a function that does something, right? For example, adds two numbers together. All right? And just like with these other types, like numbers or strings, we can actually do interesting things when assigning functions to constants. For example, we could do things like use a ternary operator to dynamically change a function's definition, right? So here's what that might look like. We could say something like const shouldAdd, and we'll just set that to false here. And then we'll say const function. And we're going to use a ternary operator here to decide what this function's definition should actually be based on the definition of this shouldAdd constant. All right?
>
> **[4:41](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/using-functions-as-data?u=76281980&t=281)** So we're going to say shouldAdd, and if that's true, we're going to have that function, add numbers together, otherwise, we're going to have that function, maybe multiply numbers together, right? So X times y. All right, and in this case, we can still call fn, just like we would call any other function with two numbers. So we'll just say 10 and 20. And if should add is false, that's going to multiply those together. Let's just run our code and see. Oops, here, let's actually print that out to the console here by saying console.log(fn(10,20)). All right. And now, let's try running our code again. And sure enough, we see that that gives us 200, so it's multiplied them together. But if we change, this should add variable to true and run it again, we'll see that the definition and behavior of this fn function has completely changed because of it. All right, so that's just a very introductory example of how this kind of thinking, right? Thinking about functions as data can really help us write code that's more flexible and reusable than just regular procedural thinking. So let's take a look at another more interesting example of this. A common use case for this kind of thing would be for doing things like mocking out pieces of our code during development. So let's say that we have some kind of networking function, right? Maybe this function makes a network request, we'll call this function fetchData. And inside this functions body,
>
> **[6:16](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/using-functions-as-data?u=76281980&t=376)** what we're going to do is we're just going to, for now, we'll just log out something like, loading data from the network, right? Something like that. And that would be a pretty time consuming operation, right? So if you were doing something like running integration tests or unit tests, you might want to have the ability to easily mock out this function by creating sort of a fake version. And here's what that's going to look like. First of all, we'll just have this [[Fetch]] data thing return just an empty object there. In reality, this would actually make a network request, but I'll skip over that part for now. And then what we're going to do is we're actually going to create a fake version of this function. We'll call that fetch data fake. And this one can simply return an object that we've pre-populated with data, right? So this would serve for, you know, running unit tests or something like that. So we'll say, name: 'Jane Doe' age: 35, and we'll say, job" 'Programmer' again, okay? Cool. So at this point, now that we have two alternative versions of this fetchData function, and I'm actually going to change the name of this one to fetchDataReal so we have a real version and a fake version. What we can do because of the way that functions are treated in functional programming is we can actually say const fetchData = and then we could take something like an environment variable into account to choose between the real version or the fake version
>
> **[7:51](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/using-functions-as-data?u=76281980&t=471)** of this function. So here's what that might look like. We could say something like const IS_TESTING, all right? And this would be either true or false depending on whether we were running this code in a testing environment. You can set this using environment variables and we could just say const fetchData = IS_TESTING. And if we're running this in test mode, we would want to use the fake version of this function. Otherwise, we would want to use the real version of this function. So we'll say fetchDataReal there. All right, so the idea here is that if we just exported this fetch data function, then the [[Representational State Transfer (REST)|rest]] of our application could use this fetchData function without worrying about what the implementation was, whether it was the fake version or the real version. So this is just one example of something that's available to us in functional programming just by changing our mindset around what functions are and treating them like we treat other data types.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Functional Programming]] (5), [[Fetch]] (3), [[JavaScript]] (1), [[Representational State Transfer (REST)|Rest]] (1)
> **Code Identifiers:** sayhello (8), fetchdata (5), sayhello2 (4), shouldadd (3), fetchdatareal (2)
> **Analogies:** just like (4), for example (2), such as (1)
> **CLI Commands:** make (2), node (1)
> **File Paths:** data.js (1), examples/functions-as-data.js (1)
> **Env Vars:** is_testing (2)
> **Cross-References:** previous video (1)
> **Definitions:** in other words (1)

#### [Creating arrays of functions](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/creating-arrays-of-functions?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/creating-arrays-of-functions?u=76281980&t=0)** - Alright, so now that we've seen a few interesting things that can be done with first class functions in this video, we're going to take a look at another interesting example. And that is just like how we can create arrays of numbers or strings or objects. Because functions in [[JavaScript]] are first class. We can also create an array of functions. So let's take a look at how to do this here. I'm going to create a new file inside our examples folder. We'll call this arrays of functions.js. And inside here, let's jump into our first example of how to create arrays of functions. And the first question you probably have is, why would we want to do something like that, right? Why would we want to create an array of functions? Well, let's say that we have a few different functions in our program, maybe one's called const double, and this just takes a number and doubles it. And maybe another one is called minus one. And what this one does is it takes a number and returns that number minus one, right? And maybe let's do one more here. We'll say something like, const square equals X, and then we'll just return X times X here. Well, we might want to apply these three functions in order to the same number and get the result that comes out on the other side. So in other words, we might have a variable here called my number. And let's just set that to 42 here. And we might want to know
>
> **[1:32](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/creating-arrays-of-functions?u=76281980&t=92)** what is the result when we double this number, then subtract one from that result, then square that result, right? And you could obviously do these in other orders. Well, the way that we could do that, an easy way to do that would be to simply create an array of these functions and loop through it. I'll show you how to do that in just a minute. But let me show you first what kind of trouble this saves us from. Without creating an array of functions, we would actually have to say something like, const doubled equals double my number. All right, so we're getting that result. Then we could say, const, one less equals, and then we will subtract one from doubled. Oops, that should be minus one doubled. And then we'll say const squared equals, and then we're going to call our square function on the result of subtracting one. So we're going to call that on one less. And that finally gives us the answer. So we'll say console.log, the answer is, and we'll just add squared onto the end there. All right, so let's run our code here. We're going to say node examples slash arrays of functions.js. Sure enough, we see that that gives us the answer here, 6,889. And really the point of this example is that that's a lot of extra code that we have to go through there, and it's not necessarily the most readable either. Now, you could obviously nest these. So you could say something like minus one and call that on the result of double my number
>
> **[3:07](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/creating-arrays-of-functions?u=76281980&t=187)** and then call square on the result of that. But that's really not very readable either. So the best way to go about doing this, I'm just going to remove these things here and we'll just call this result. The best way to do this would be to simply loop through all of our functions. We can say four const, fn of, and then we're going to actually make an array of our functions by saying const functions equals. And then we'll say that we want to double it, then subtract one, then square it. And now we just have to say const fn of functions. And just like when we loop through an array of numbers or an array of strings in each iteration of this loop, fn is going to represent a different function in this array. So here's what this is going to look like. We're just going to say, we'll start off here by saying, let result equals my number. And yes, I am using let here, and I am mutating this variable a little bit later on. We'll take a look at how we can do something like this without any mutation whatsoever. But this is just to prove a point for now what you can do with arrays of functions. So now we're just going to say result equals whatever function we're currently looking at called on the previous result. All right, so that's all we have to do there. And that will give us the same answer as what we had before. And now if we want to change the order of these functions, that's really easy to do, right?
>
> **[4:41](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/creating-arrays-of-functions?u=76281980&t=281)** We can just, you know, let's say that we wanted to square it first before doubling and subtracting one. We just have to move that square function up. And if we rerun our code now, we'll see that that will give us a completely different answer. All right, now, another interesting thing that you can do here that might shed a little bit more light on the nature of first class functions is you can actually use JavaScript's built in math functions and incorporate them into the array in this same way. Alright, so here's what we might want to do here. We could add, for example, the math.squareroot function, and that'll work just like the other functions that we defined here, right? So we're allowed to put that into an array and work with that as data, just like any of the functions that we've created ourselves. So let's just try running this again. And sure enough, we see that that square root has been added in giving us a different answer. Now, another thing that I wanted to point out here before we move on, and this is very important, is that notice when we put these functions into an array, or whenever we're referring to the function itself, and we don't want to call it, we don't put parentheses after the function name, right? If you were to put parentheses after the function name, as you often see in documentation for things, that's saying something significantly different, right? That's actually referring to the return value of each of these functions instead of referring to the function itself.
>
> **[6:15](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/creating-arrays-of-functions?u=76281980&t=375)** So when we just want to create an array of functions, or as we saw previously when we just wanted to assign a function to a constant, we can simply use the function's name without any parentheses after it.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[JavaScript]] (2)
> **Analogies:** just like (4), for example (1)
> **File Paths:** functions.js (2)
> **CLI Commands:** node (1), make (1)
> **Definitions:** is called (1), in other words (1)
> **Cross-References:** as we saw (1)
> **Speakers:** - alright (1)

#### [Passing functions as arguments](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/passing-functions-as-arguments?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/passing-functions-as-arguments?u=76281980&t=0)** - [Shaun] All right, so now that we've seen a few ways that we can treat functions in the same way as other types, such as numbers, strings and objects, the next step here is to take a look at how we can pass functions as arguments to other functions. This is where things start to get a little bit more interesting here. So to get started, let's actually create a new file here. We'll just call this something like passing-functions.js. And inside this file, what we're going to do is we're just going to create a simple add function, right? We've already done this before, but I'm just going to use this as a talking point here. Now, up until now, whenever you've defined a function, you've probably been used to passing arguments into functions with the purpose of specifying what the data is that we're operating on, right? So if we pass two numbers into a function called add, for example. All right, we'll just say console.log add, we'll just do three and four. We're specifying the two numbers that we want to add together, right? So what happens here is, when we call that add function with those two numbers, x here gets replaced with three, y here gets replaced with four, and it returns the result of that. So that's what you're used to doing, right, is passing arguments to specify what the data is that our function is operating on. But here's the exciting part is because of first-class functions, we can pass arguments into our functions,
>
> **[1:34](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/passing-functions-as-arguments?u=76281980&t=94)** not only to specify what the data is, but what operations should be performed on our data. Now, that might sound a little bit confusing, so let me just illustrate with an example here. Let's add another function that we'll call subtract. And this one's just going to simply take two arguments and return the first one minus the second one. And let's say further that we have a function here, which we'll call something like combine, and we'll just actually pick two numbers here so that the data in our function will be already predefined, but the operation won't be. We're going to say combine4and5 And what this function is going to do is it's actually going to take a function as an argument, right? So we'll say function. And it's just going to return that function called with the numbers four and five, all right? So again, this is a little bit strange because we're actually kind of turning things on their head a little bit. We're not going to be passing the data into our combine4and5 function, that's already set. What we're actually doing though is we're passing in an argument to specify what we want to happen to that data. So here's how this works now, we can simply call combine4and5 and pass either the add or subtract function as an argument. So we can say combine4and5 using add. We can say, here, we'll do console.log,
>
> **[3:08](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/passing-functions-as-arguments?u=76281980&t=188)** we can combine four and five using subtract. And really if we have any other functions as well, as long as they take two numbers as arguments and will therefore fit in with this thing here, that'll work in the same way. So let's actually run our code. We're going to say node examples/passing-functions.js. And sure enough, we see that, in one case, we've added four and five together, and in the other case, we've subtracted five from four. All right, so again, all that's happening here is just like how when we pass numbers to add or subtract and call those functions, how those get replaced, like four and five here, when we pass a function into another function as an argument, that function is simply going to get replaced with whatever we pass. So if we pass add into combine4and5, that's just going to translate into this, right? It's going to return add called with four and five. If we pass subtract in, that's going to look like this, we're going to have subtract four and five. And if we pass in something else, right? we're actually allowed to pass in other preset functions, right? Such as the Math.min function provided by [[JavaScript]], or Math.max, and those will work in the same way. Here, let me actually just make sure I change this back to function there. And if we run this now, sure enough, we'll see that when we pass in Min, the way that those numbers are combined is by simply returning the minimum. When we pass in Math.max,
>
> **[4:42](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/passing-functions-as-arguments?u=76281980&t=282)** the way that those numbers are combined is by giving us the maximum. All right, so let's just solidify this a little bit more by thinking about some other ways in which we could use this concept. Let's try and do the same kind of thing with strings, what I'm going to do here is I'm going to say const, and we'll call this function combineNames. And what this is going to do is it's simply going to take a function that will tell us how to combine a first name and a last name. And it's going to return that function called with preset data. I'm going to use my first and last name here. If you're following along, you can obviously use your own name. And now that we have this combineNames function, let's think about a few different ways that we might want to combine a first and last name, right? We might want to just put them together with a space in the middle. So here's how that would look like. We'll just say something like const simple. And that'll just take the first and last name, and it'll return the first and last name appended together with a space in the middle, like this. So we'll say first plus space plus last. And we might also have something like a government form notation, all right, where you have the last name first, followed by a comma, and then the first name after that, all in caps. So here's what that would look like. We'll just say government form, and that'll be first and last. And then we will return here. I'm actually going to use back ticks to make this easier.
>
> **[6:13](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/passing-functions-as-arguments?u=76281980&t=373)** We're going to say last.toUppercase, and then we're going to have a comma, and then we're going to have first.toUppercase. And you can probably think of plenty of other ways here. You might want to have like an initials function here where you just have first and last, and then we have the first letter of the first name plus the first letter of the last name, like so. And all of these can now be passed into this combineNames function in order to combine those names in different ways. So we could say combineNames the simple way. And here I'm just going to add console.log before this so that we're printing this out. Console.log, combineNames, there we go. And we'll do the same thing now with the government form notation and with the initials, we'll just say combineNames, govtForm. And then we'll combine those names with initials. And if we run our code now with node examples/passing-functions.js. And oops, it looks like I mistyped the function name, it should have uppercase with a capital C. And oh, I also forgot to put parentheses after this. So let's just add those and let's try this again. And sure enough, what we'll see is that just by passing different functions as arguments here, we got completely different results given the same data. So anyway, that's just an example of how to pass functions as arguments to other functions. The next thing that we're going to do is bring this full circle by seeing how we can return functions from other functions.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[JavaScript]] (1)
> **Code Identifiers:** combinenames (6), touppercase (2), govtform (1)
> **CLI Commands:** node (2), make (2)
> **Analogies:** such as (2), for example (1), just like (1)
> **File Paths:** examples/passing-functions.js (2), passing-functions.js (1)
> **Definitions:** is a  (1)
> **Speakers:** - [shaun] (1)

#### [Returning functions](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/returning-functions?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/returning-functions?u=76281980&t=0)** - [Presenter] All right, so far we've seen how to treat functions as constants, right, as other data types. We've seen how we can create arrays of functions, and we've also seen how we can pass functions as arguments to other functions. So the next thing that we're going to take a look at here, and I'm going to create a file for this so that we can demonstrate it, is how to return functions from other functions; and we'll also, of course, take a look at why you'd want to do this in the first place. So I'm going to say returning-functions as the name of this file. And inside here, what we're going to do is we'll start off by taking a look at the simplest possible example of returning functions from other functions. Now, this is going to look a little bit funny, the syntax here, but I'll explain it and I'll even show you a trick for understanding it a little bit better, if you find this syntax confusing. What we're going to do is we're going to say const and we're going to call this function createPrinter. I'll explain that name in a minute. And then, we're going to put a pair of parentheses after that, right? This function is not going to take any arguments. And then, what this function is going to do is it's actually going to return another function. Now, here's where things get a little bit weird. Just like how if we wanted to return a string here, we would just say 'Hello!', or if we wanted to return a number, we would just say 10, for example. When we want to return a function from a function, we're just going to define another arrow function right here as its return value. So yes, you have one pair of parentheses
>
> **[1:38](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/returning-functions?u=76281980&t=98)** followed by an arrow, followed by another pair of parentheses followed by another arrow, and then we're just going to have this function that we're returning log something out to the console and we'll say something like, Hello [[Functional Programming]]! All right, so now that we've created this function that returns another function, you're probably wondering "How on earth do you use this thing?" And the answer to that is actually pretty straightforward. So what we're going to do is we're going to say, const printer = and then we're going to call our createPrinter function. All right, now as I said, this createPrinter function, it returns a function. So at the point in time when we say const printer = createPrinter, it's the same thing as if we said const printer = and then we put the return value after it, right? So I'm actually just going to copy this here and paste it there. All right, so in other words, this printer thing that we've defined here is this function that was returned by our createPrinter function and that means that we can just call it like this and it will do whatever that function was supposed to do. So if we run our code now, which we can do by saying node examples/returning-functions.js... Whoops, it looks like I spelled createPrinter wrong in the second case here. Let's try that again and we'll rerun our code. Sure enough, we see that it says Hello functional programming! Cool, so another thing that you can do here,
>
> **[3:11](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/returning-functions?u=76281980&t=191)** we've actually gone to the trouble of naming this function that was returned from createPrinter and calling that separately, but you actually don't even have to do that. So what this leads to, and this is the part that really unnerves people sometimes, what you can actually do is you can just say createPrinter and you can just put two sets of parentheses after it. And what that will do, the first set of parentheses is going to return a function so you can kind of think of that as if this entire thing here, createPrinter with those two parentheses after it, is just this function up here, and then by putting parentheses after that, we're actually calling that function. All right, so people generally find that disturbing the first time they see it, but once you get used to it, it should be a little bit more obvious what it does. All right, so that's the basics of returning functions from other functions. Let's go back to the proverbial elephant in the room here: this weird looking syntax here with the double sets of parentheses and the double arrows. Well, if you find this confusing at all, you might find it helpful to simply rewrite this function using a different function syntax for each of these functions. For example, here's what you might do, and I'm just going to comment this thing out here for now and rewrite it differently. What you might want to do is say something like function createPrinter, and then inside the body of this function,
>
> **[4:46](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/returning-functions?u=76281980&t=286)** which we've defined using the function keyword, we're just going to say return and then we'll create a new function here, and this function will actually be the one that says console.log 'Hello, functional programming!' All right, so these two things are going to do pretty much the same thing. Obviously, the behavior is a little bit different because we've defined this with the function keyword versus just arrow function syntax, which is we've seen affects function hoisting. But a lot of people find that this is a little bit easier to tell what's going on, right? You can actually see that it's returning a new function and that you can simply assign that function to a variable or even call it like we did down here. Great, so that is the basics of returning functions from other functions. Let's take a look at one simple way that we can apply this concept, and I'm just going to delete that code down there so that we don't end up with multiple things getting printed out to the console. What we're going to do here is let's pretend that we have a few different functions that all share a little bit of repetitive similarity. Let me show you what I mean. I'm going to say const double =, and this function is simply going to take a number and multiply it by two. After that, we'll have a function called triple. This is just going to take a number and multiply it by three. And then we'll have quadruple, and this is just going to take a number and multiply it by four.
>
> **[6:20](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/returning-functions?u=76281980&t=380)** Now, what you'll notice here is that all of these functions are pretty much identical except for one little piece of those functions, and that is the number that it's multiplying our argument by. Now, this kind of situation is where the concept of returning functions can become very powerful because what we can actually do with returning functions is that we can actually use this concept to create different versions or variations of similar functions. So you can think of these functions that return functions almost as helping us define families of functions like what we have here. So let me show you what this is going to look like here. What we can actually do is create a function that returns a function, and we're going to call this function something like createMultiplier. And here's what this function is going to do. It's going to take a number, which we'll call y, and it's going to return a function that's sort of the generic version of all of these functions again, and this function will sort of define this entire family of functions. And what that's going to look like is we're going to say x, and then we're going to return x times y, right? So this function that you see right here is, as I said, the generic version of these three other functions. And by passing in y ahead of time, we can actually recreate all of those functions a little bit more easily using this createMultiplier function.
>
> **[7:56](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/returning-functions?u=76281980&t=476)** So here's what this is going to look like now. We can actually define double, triple, and quadruple just by calling this createMultiplier function with different arguments, right? So we can say const double = createMultiplier 2. Notice that that's going to set y to 2, and that gives us the exact same function that we had before when we defined double. All right, we're going to say const triple = createMultiplier 3. Again, that's going to replace y with 3, and that's going to give us the exact function that we had before there. And finally, we're going to say const quadruple = createMultiplier and that's going to be 4. And now, we can call any of these functions in exactly the same way as we would've been able to call them had we just defined them in place like what we saw before. So let's just say console.log, we'll just double 10, maybe we'll do console.log triple 10 as well. And why don't we just do quadruple 10 while we're at it? All right, so let's take a look at this now. What we're going to see is if we run this returning-functions file, we see that we get 20, 30, and 40. In other words, each of these functions is behaving in exactly the same way as what we had before, but we actually created each of these functions more or less on the fly by using this createMultiplier function. Now, the last thing I'll say about this is that in general, this kind of thing, returning functions,
>
> **[9:32](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/returning-functions?u=76281980&t=572)** makes our code more flexible because look what we can do. If we wanted to know all of the places where numbers were being multiplied in our code now and actually see that in action, what we could do is we could actually expand this createMultiplier function a little bit, and we could just say something like console.log and we could say 'Multiplying ', and then we'll say x, and then we'll say ' by ', and then we'll add y onto the end there. All right, so now by making that single change, we've been able to make that change to this entire family of functions, and if we run our code now, what we'll see is that, sure enough, we see that printed out there. This is a little bit of a simplistic example, of course, but with more complex situations, this kind of thing, this ability to create families of functions like this, is an incredibly powerful concept. We'll see some examples of this later on in the course.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Functional Programming]] (3)
> **Code Identifiers:** createprinter (10), createmultiplier (8)
> **CLI Commands:** find (5), node (1), make (1)
> **Definitions:** is a  (3), in other words (2), means that (1), is an  (1)
> **Analogies:** for example (2), just like (1)
> **File Paths:** examples/returning-functions.js (1)
> **Cross-References:** go back to (1)
> **Speakers:** - [presenter] (1)

#### [Understanding closure](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/understanding-closure?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/understanding-closure?u=76281980&t=0)** - [Instructor] All right, so previously we talked about how it's possible to return functions from other functions in [[JavaScript]]. And this concept actually leads us to another very important concept in [[Functional Programming]], and this is something called closure. Now, basically, closure means that when we return a function from another function as we've done here, oops, let me try that again. There we go. The function that we return still has access to the scope that it was returned from. So that probably doesn't mean a whole lot to you yet. So let me show you an example that will demonstrate closure. First of all, I'm going to create a new file here, which we'll call something like closure.js And inside here what we're going to do is we're going to create a function that returns a function. We'll call this createPrinter again. But the difference here is that this function is going to have a variable inside of it called something like my favorite number, and we'll make that 42. All right? So inside here now we're going to say return. And as I said, we're going to return a function that simply logs something out to the console, but this time, the thing that's going to be logged out to the console will involve this, my favorite number variable. So here's what that's going to look like. I'm going to use back ticks here. We're going to say "My favorite number is," and then we're going to insert the value
>
> **[1:33](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/understanding-closure?u=76281980&t=93)** of my favorite number into that string. Great. So now that we've defined this new Create Printer function, let's use this thing by saying const printer = createPrinter And now that we've done that, we're going to say printer with parentheses after it. And let's run our code now by saying node examples/closure.js Oops, let's try that again. There we go. And sure enough, we see that this program prints out my favorite number is 42 to the console. Now at first glance, this might not seem like anything too special, but let's take a closer look. When we return our printer function by calling Create Printer, we can see that this printer function somehow still has access to the constant called my favorite number. Even though, if we try and access that constant right here in the same place by saying console.log my favorite number, right? We're going to see that that will give us an error saying, my favorite number is not defined because that's out of scope. So this is the basics of closure when working with functional programming. And just to review what that means, it means that this function that was returned from our Create Printer function still has access to the scope that it was returned from, right?
>
> **[3:06](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/understanding-closure?u=76281980&t=186)** Including all of the variables, all of the arguments, really any other value or variable or function even, that it could have accessed right in here. So in order to illustrate this a little bit further, let's take a look at an interesting example. I'm going to show you how to use closure to implement truly private variables in JavaScript. And this might seem a little bit ironic in a course devoted to functional programming, since functional programming makes every effort to avoid using these private variables, since usually they just mean that our code is stateful. But I think this is going to be an excellent example of closure in JavaScript, and I hope that it'll help you wrap your head around first class functions a little bit better. So let's imagine that we want to create a person class. Well, instead of using the class keyword as you would generally do here, we're just going to define this as a function that returns a new person object. So here's what that's going to look like. We're just going to say const person equals, and then this will take the name, age, and job, and the syntax that I just used there is object destructuring, sometimes called props destructuring. It's the same kind of thing that you see when working with [[React.js|React]] in JavaScript. Basically, this just gets the name, age, and job properties of an object that we pass to this thing as an argument. And then what we're going to do is we're going to return just an empty object for now. You'll see what we're going to do with this in a minute. So now that we've done that, the next thing that we're going to do is we're going to define our quote unquote
>
> **[4:39](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/understanding-closure?u=76281980&t=279)** "member variables." And here's what that's going to look like. We're just going to say, let person name = name. Let person age = age. Let person job = job. Okay? So now that we have those three things, here comes the fun part. We're going to use closure to make it so that these variables are only accessible to the outside world by using getters and setters. So here's what this is going to look like. We're going to start by writing the getter function for the person's name, we'll just say, get name. And this will just be a function that returns the value of person name. And now what we'll see is if we create a new person from this person function, we'll say const person equals, and then I'm just going to say person, and I'll use name Shawn, age, 123 and job, developer. All right. Well, what we're going to see here is that now that we've created this person, we can say, person.getName Because look up here. That's all that we've returned in this object. We can say person.getname And let's actually just print out the value of that there, like so. All right. And if we run our code, what we're going to see here is that, oops, it looks like I need to comment out that line from my demonstration beforehand of my favorite number. Let's try this again now.
>
> **[6:12](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/understanding-closure?u=76281980&t=372)** And if we run our code, sure enough, we see that that gives us the person's name. However, if we try and say person.name, we're not allowed to do that, right? Notice that this object here doesn't have any property called name. If we run this code, we'll see that that is simply undefined. All right, so that's how we would create a simple getter function. And you could do the same thing for age and job, but I'll leave that up to you because it's the same exact syntax. Let's see how to do a setter. If we wanted to allow users to change these variables in a very specific way, all we would have to do is say something like Set name. And actually what I'm going to do is I'm going to say increment age here. And what we're going to do is we're just going to say, personAge plus equals one. And let's also add a get age function here so that we'll be able to see that we'll say personAge. That's going to be the value that this returns. And now if we access the person's age, which we can only do by saying person.getAge, by the way, and we can only modify it now by saying person.incrementAge All right? And here, we'll print out the person's age after calling that here by copying and pasting that. What we'll see if we run our code is that sure enough, through closure, we have access to that person age variable, but only through those functions that we've returned, right? We can't just say person.age,
>
> **[7:47](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/understanding-closure?u=76281980&t=467)** because that will return undefined. All right, so hopefully this example has really helped you to understand the concept of closure a little bit better, as well as the concept of returning functions.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[JavaScript]] (4), [[Functional Programming]] (4), [[React.js|React]] (1)
> **Code Identifiers:** createprinter (2), personage (2), getname (1), getage (1), incrementage (1)
> **CLI Commands:** make (2), node (1)
> **File Paths:** closure.js (1), examples/closure.js (1)
> **Cross-References:** we talked about (1)
> **Analogies:** imagine (1)
> **Speakers:** - [instructor] (1)

#### [Higher-order functions](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/higher-order-functions?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/higher-order-functions?u=76281980&t=0)** - [Instructor] All right, so we've spent quite a bit of time in previous videos talking about first-class functions in [[JavaScript]], and we've seen how JavaScript treats functions in pretty much the same way as strings, numbers, objects, things like that, right? Other data types. Well, there's a term for functions that either take other functions as arguments or return functions, or as we're about to see, both are possible in the same function. We call these things higher-order functions. And I'm going to show you how to use them in this video. So first of all, let's start off by creating a new file. We'll just call this new file higher-order.js. All right, and inside this file, we're going to do the first demonstration of this. We're just going to see how this idea of higher-order functions can allow us to really separate the functionality out of certain functions in ways that we weren't able to do with a more procedural mindset. So here's what this is going to look like. We're going to start off by creating a function called divide. And this function is simply going to take two numbers, which we'll call num1 and num2, and return num1 divided by num2. Now, obviously in this case, we'd like to prevent the second argument from being 0 here, and maybe display an error of some sort if the user ends up passing in a 0 as the second argument. So the way that this normally is added is by adding it to the function body itself, right?
>
> **[1:38](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/higher-order-functions?u=76281980&t=98)** So what we're going to do here is we're going to expand this and say return num1 / num2. And inside here we're going to do our check. We're going to check to see if num2 is equal to 0. And if it is, we're going to want to just log something out to the console. We'll say something like, 'Error! Can\'t divide by zero!' All right? And then maybe we'll just return nothing there. Okay, so, this works, and this kind of argument checking is pretty common in many codebases. But the problem with this is that we're cluttering up our divide function here with argument checking logic, Right? Ideally, each function should really only have one main concern. And in the case of our divide function, we don't want it to have to worry about checking its own arguments. That really is kind of cluttering up our function and obscuring the real job of this function down here. So here's our basic plan of attack here. What we're going to do is we're going to create another function that we'll be able to use to sort of wrap our divide function and provide this argument-checking logic for us. That might sound a little bit confusing and a little bit vague. So let me show you what this is going to look like. What we're going to do here is we're going to create a new function here. We'll call this something like secondArgZeroCheck,
>
> **[3:16](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/higher-order-functions?u=76281980&t=196)** all right, something like that. And what this is going to do is it's actually going to take a function as an argument, and it's going to make sure that that function won't be called if the second argument is 0. So here's what this is going to look like. We're going to return a function from inside of here. Actually, here we'll just return a function the arrow function way. And I'm going to do something here that's a little bit confusing that I'll talk about later. So in case you haven't seen this syntax before, don't worry too much about it. This is just called the spread operator. And basically what it does is it gives us any and all of the arguments that were passed into the function as an array. All right, so it's a nice way to allow the calling code to pass in however many args it wants. So now that we've done this, what we're going to do is, inside this function that we're returning, this is where we're going to write the argument-checking logic. So essentially we're just going to say if, and we're going to duplicate what we had up here, in fact, I can just copy this and paste it there like so. We do have to change this to args[1], right? Getting the second arg there, and we'll change this message as well, since our secondArgZeroCheck function doesn't really know what we want to do with this. We're just going to say something like, 'Error! Second arg cannot be zero!' All right. And that's what happens if the argument is 0,
>
> **[4:48](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/higher-order-functions?u=76281980&t=288)** we're just going to log out that error and return. Otherwise, what we're going to do is we're just going to call the function that we passed in originally with whatever these args happen to be. So we're going to say return function, called with all of the arguments, we have to use the dot, dot dot before that here again as well, in order to actually pass the contents of an array as arguments instead of the array itself. And now, what we can do is we can remove this logic from our divide function, bringing it back to a nice simple function that it was before. And here's how we can actually use these two functions together to provide the same exact functionality that our divide function had before when it was doing its own argument checking. What we're going to do is we're just going to say const, and we'll call this something like divideSafe, equals, and now we're just going to pass our divide function to secondArgZeroCheck by saying secondArgZeroCheck(divide). And now, this divideSafe function that we have is going to have pretty much the exact same behavior as what we saw earlier when our divide function had this logic inside of it. So let's test this out. We're going to say console.log(divideSafe), and let's try passing in two numbers that are valid here, 10 and 2. And let's actually also pass in, we'll say divideSafe, and then we'll say 10 and 0.
>
> **[6:22](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/higher-order-functions?u=76281980&t=382)** So let's see how this thing behaves there. What we're going to see is if we run our code with node examples/higher-order.js, sure enough, we see that the first one returns 5 and the second one actually displays that error saying second arg cannot be zero and returns undefined. All right, now, it might not be immediately clear how this fits together. So I would highly recommend that you walk through this visually. You know, when calling divideSafe. Remember that when we pass a function as an argument, all we're doing is just passing any call to function with whatever that function was. So here we're just saying return divide(...args), right? And if we pass in 10 and 2 here, we're just replacing args with 10 and 2. All right? And the [[Representational State Transfer (REST)|rest]] of the stuff, right, this if statement, this is going to get called now every time we call this divideSafe thing, and it'll provide a guard around this divide thing, meaning that divide isn't going to get called if this second argument is 0. So anyway, that's the basic idea of higher-order functions. And just like how we saw that returning functions could be used to create sort of families of functions, higher-order functions can be used to actually add extra functionality onto existing functions without modifying the functions themselves. And this is a really powerful idea. So anyway, that is the basics
>
> **[7:57](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/higher-order-functions?u=76281980&t=477)** of higher-order functions in JavaScript.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[JavaScript]] (3), [[Representational State Transfer (REST)|Rest]] (1)
> **Code Identifiers:** dividesafe (6), secondargzerocheck (4)
> **File Paths:** higher-order.js (1), examples/higher-order.js (1)
> **CLI Commands:** make (1), node (1)
> **Definitions:** we call this (1), is a  (1)
> **Analogies:** just like (1)
> **Speakers:** - [instructor] (1)

#### [Tour of CoderPad](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/tour-of-coderpad?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/tour-of-coderpad?u=76281980&t=0)** - [Instructor] All right, so now that we've learned all about first-class functions in [[Functional Programming]] with [[JavaScript]], it's time for you to do a challenge. Now, this course uses automated code challenges with an integrated editor, and that's what you're going to see next, is a screen that looks a lot like mine. Now, just to give you an idea of what the different parts of this screen are. Up in the top left-hand corner, this is where you're going to see the instructions for the challenge. There's nothing there right now, but there will be when you open up the challenge. Underneath that is the Console output. This is where you'll see feedback when you try and implement your solution to this challenge and run your code. Over here on the right-hand side, this is where you see the function that you'll have to implement. Sometimes this code will look different and sometimes there will be multiple functions, but in general, this is where you are going to put your solution. And finally, down here in the bottom right-hand corner is some test code that will show you an example of how your code is going to be run. All right, now, once you've given the challenge a try and added your code here, you can click this Test my code button, and the challenge will give you feedback over here in the Console output. And that should be about all you need to know. So best of luck in your first challenge, and you'll see a few more of these throughout the course.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Functional Programming]] (1), [[JavaScript]] (1)
> **Definitions:** is a  (1)
> **Speakers:** - [instructor] (1)
> **Non-Speech:** (upbeat music) (1)

#### [Solution: More higher-order functions](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/solution-more-higher-order-functions?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/solution-more-higher-order-functions?u=76281980&t=0)** (bright electronic music)
>
> **[0:06](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/solution-more-higher-order-functions?u=76281980&t=6)** - [Instructor] All right, well, hopefully you gave this challenge a try. Let's jump in and take a look at the solutions. So, I mentioned in the instructions that really the first thing to do was take a look at the higher-order function that we created previously for checking whether an argument was zero, and use that as sort of a template. So, I'm going to do that right here. We'll start off by saying return function. All right, and this function, I also mentioned in the instructions that this function was only going to need to take two arguments, so we didn't need to use the spread operator like we did previously. So, I just said arg1 and arg2, and by the way, I used the function keyword syntax here in both of these cases, but you could have just as easily used the arrow function syntax, or really any other syntax as well. Okay, so once we're inside here, right, this function that we're returning, the next thing that we need to do is we need to check to make sure that these two arguments are the same length. Now, you could include other type checks if you wanted, like checking that they're both arrays, but I'll just leave that up to you. We're just going to say if arg1.length is not equal to arg2.length, well, in that case, what we want to do is we want to simply return an empty object, and you could of course log something out here to the console saying that the two arguments must be the same length,
>
> **[1:38](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/solution-more-higher-order-functions?u=76281980&t=98)** but again, I'll leave that up to you. Otherwise, what we want to do is we simply want to return our original function, whatever function was passed in here and wrapped, we just want to pass those two arguments directly to the function, so function arg1 and function arg2, and that should be all we need to do, so let's give this thing a try. We're just going to run Test my code, and sure enough, we see that the two tests passed there, right? Both when the input was the same length and when the input was different lengths. So, that is the solution to this challenge. Hopefully you were able to figure that out, and don't worry if your code doesn't look quite the same as mine, as long as you used this concept of higher-order functions and taking a function as an argument, as well as returning a new function that wraps it, that's pretty much all you need to do. So, that is the solution to the higher-order functions challenge.

> [!info]- Semantic Content
>
> **CLI Commands:** make (1)
> **Exercise Files:** template (1)
> **Speakers:** - [instructor] (1)
> **Non-Speech:** (bright electronic music) (1)


### 3. JavaScript Array Manipulation

[↑ Back to Table of Contents](#table-of-contents)

#### [Basics of functional data manipulation](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/basics-of-functional-data-manipulation?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/basics-of-functional-data-manipulation?u=76281980&t=0)** - [Instructor] All right, so now that we've taken a look at first class functions, which as you've seen, give you a lot of power and flexibility in your programs, the next thing that we're going to take a look at here is the basics of manipulating data in a functional way, right? So in other words, when you have some data and you want to modify it, how do you do that in [[Functional Programming]]? Well, let's start off by taking a look at the procedural way of making these kinds of changes. In the procedural way, we generally write our programs in such a way that we're specifying how to calculate the result, right? So in other words, we're specifying a process or procedure, hence procedural, for how to calculate the result that we're looking for. And this generally involves a series of steps. First, we're going to initialize a variable, then we're going to loop through all of the elements in an array, and this is usually done using a for loop, and generally that for loop will modify the variable that we initialized, so that once we're done with the loop, that variable is going to contain the result. Now, the problem with this is that between the step where we initialize the variable and when we're done and have the result, that variable is kind of in this uncomfortable intermediate point where it's not really the result, but it's also not what we initialized it as. And this can make our programs pretty difficult
>
> **[1:34](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/basics-of-functional-data-manipulation?u=76281980&t=94)** to troubleshoot if something doesn't work the way we want it to, as I'm sure you've experienced when working with things like complex or nested loops. So, the functional way of manipulating data, I really think you're going to like this way a lot better. The first thing to know about the functional way of manipulating data is that instead of specifying how to calculate the result and the steps required to calculate the result, we simply write our programs in such a way that it tells [[JavaScript]] what it is that we want. That might sound a little bit strange, you'll see exactly what this looks like very shortly. But generally it means that we don't use any for loops, right? So instead of for loops, we're generally going to use a number of other functions with names like map, filter, reduce, and sort. And these functions will allow us to get the exact same result and apply the exact same operations as we were able to do with a for loop, but in a much more functional and declarative way. Now, we'll get into these functions shortly, but one thing that I want you to remember here before we get into these functions is that with some of these functions, you might find yourself wondering how the function is actually working, right? What process is actually going on behind the scenes with a lot of these functions? For example, with the sort function, you might wonder, is the function using quick sort, merge sort? You know, what kind of algorithm
>
> **[3:07](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/basics-of-functional-data-manipulation?u=76281980&t=187)** is it using behind the scenes? Well, I'm going to recommend that you do your best to not worry about how these things actually work behind the scenes, right? That's not really the point. The point of these functions is to allow you to express what you want instead of how to calculate what you want in a declarative way. So again, just keep that in mind. We'll see what this looks like in much more detail when we jump into these functions, which we're going to do right now.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Functional Programming]] (1), [[JavaScript]] (1)
> **CLI Commands:** make (1), find (1)
> **Definitions:** in other words (2)
> **Analogies:** for example (1)
> **Warnings:** troubleshoot (1)
> **Prerequisites:** required to (1)
> **Speakers:** - [instructor] (1)

#### [The spread operator](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/the-spread-operator?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/the-spread-operator?u=76281980&t=0)** - [Instructor] The first thing that we're going to take a look at here with functional [[Data Manipulation]] is actually not a function at all, but it is a very helpful syntax improvement that allows us to manipulate [[JavaScript]] data in a much more functional way than using for loops, which would be the normal way of doing this. And this syntax is called the spread operator. And to see what this looks like, let's start off by creating a new file in examples. We'll just call this spread-operator.js. And to start off with, let's imagine that we have two objects here, right? So one might be maybe data about a person. So we'll have maybe, you know, name, we'll say [[John the Ripper|John]] Doe, and we'll say age and we'll say maybe 40. And let's say that we have another object as well. And this object contains, let's say maybe career data that would apply to this person. So let's say something like careerData, and in fact we'll just call it career. And this maybe says something like job title. Here, so we'll have just title and we'll say maybe Software Developer, and then maybe we'll say salary, and then let's say maybe a million, all right? One can dream, of course. And now that we have these two objects, let's say that we want to combine the keys and values that both objects contain into a single new object that contains both the person's data and the career data.
>
> **[1:36](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/the-spread-operator?u=76281980&t=96)** Well, unfortunately, the procedural way of doing this, which is the way that I usually see people try and go about it, would look like this. We would have to actually loop through all of the keys and values of both of these objects and add it to a new object, right? So we might say something like const key in person, and then we would have this object here. We'll say something like const personWithCareerData equals, we start off with an empty object and then we would loop through and add the keys and values from person, and the keys and values from career to this personWithCareerData object. You probably know what that looks like now, so I'll just leave that up to you if you want to actually type that out. But the fact is that that's just a lot of typing and a lot of steps to have to specify in order to perform a pretty simple operation. So here's what the spread operator allows us to do. We're simply going to say something like const personWithCareerData, and then we're going to define this as if we were going to define a new object, but here's where the spread operator comes in. If we want all the keys and values from the person and career objects, all we have to do here just say ...person, and then ...career. And that is all we have to do in order to get a new object with all of those keys and values.
>
> **[3:10](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/the-spread-operator?u=76281980&t=190)** So let's just print this out. We'll say console.log(personWithCareerData). And if we run our code here now, what we're going to see, we'll just say node examples/spread-operator.js. Sure enough, we see that we have all of those keys and values in a single object. And notice that what we're doing here, just like I said previously, is instead of writing our program as a series of steps required to get some final result, we're simply saying what it is we want, right? We're saying we want a new object with all of the keys and values from person and all of the keys and values from career. And that's really the declarative way of doing this. All right, so that's the basics of how the spread operator works. Another thing to know is that if there are duplicated keys with different values, whichever object is listed last when using the spread operator will be the new value. So in other words, if career has a name property, and maybe this one's Johnny Doe instead of John Doe, well, what we're going to see is that Johnny Doe will be the ultimate value in this personWithCareerData object. So let's run this thing, and sure enough, we see that it says Johnny Doe. If we reverse these, right, if we say career before person, we would see that the final value for name would be the person value instead of the career value.
>
> **[4:42](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/the-spread-operator?u=76281980&t=282)** So let's run that again, and we see that that's John Doe, the value from the person object. So order does matter when using the spread operator if there are duplicated keys and values. The last thing to know about working with the spread operator with objects is that we can actually add other keys and values around these spread operators here. Like if we wanted to add something else on to here, let's say something like hairColor, maybe brown, right? Something like that. Well, that's just going to be added in. And notice again that this is just a declarative way of doing things, right? We're not actually specifying this as a step where we say personWithCareerData.hairColor, and we explicitly set that to a value in some place in our program. We're just saying that this is what we define this personWithCareerData thing to be in the first place. All right, so that's how to use the spread operator for objects, but the fact is that it can also be used with arrays. So if we have an array, for example, and we want to add some elements to the end of it, instead of using the push method, which mutates the array that it's called on. We'll talk about mutating methods in much more detail shortly. Let's just say maybe something like const numbers = [1, 2, 3, 4, 5], and we want to add more numbers on the end. Instead of saying numbers.push(6), numbers.push(7), numbers.push(8) or whatever, we can actually just say const updatedNumbers, all right. You can choose what to call it.
>
> **[6:15](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/the-spread-operator?u=76281980&t=375)** In most cases, there will actually be some sort of situation that you want reflected in the variable or constant name, but you can just say ...numbers, and then you can add whatever numbers you want extra after this. So 6, 7, 8, 9, 10 maybe, and you can add it before too if you want. So we could have -1 and 0 before those numbers. And this just represents that we want all of the elements from the numbers array to be inserted here. So what it does is it sort of takes all of the elements from inside the array and spreads them into that spot of the array, just like we saw with the spread operator with objects. So that's more or less why it's called the spread operator. And those are two ways that you can use the spread operator in JavaScript. There are other ways, of course. In fact, we saw another way of doing this back when we were looking at higher order functions, right? We saw that you could actually use the spread operator as a way to get all of the arguments in a function as an array, as well as pass an array as separate arguments, right? So again, if you had maybe, you know, some array, we'll just call this nums = [1, 2, 3]. Well, if you just pass numbers as an argument, that's going to pass a single argument that's an array. Whereas if you say ...numbers here and call the function with that, that's going to actually pass three arguments,
>
> **[7:48](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/the-spread-operator?u=76281980&t=468)** which are the elements in the array. So that's just another thing that you can use the spread operator for there. Cool, so anyway, that is the basics of the spread operator, and that is the first way that we've seen to manipulate data in a functional or declarative way in JavaScript.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[JavaScript]] (3), [[John the Ripper|John]] (3), [[Data Manipulation]] (1)
> **Code Identifiers:** personwithcareerdata (7), haircolor (2), careerdata (1), updatednumbers (1)
> **Analogies:** just like (2), imagine (1), for example (1)
> **Definitions:** is a  (1), is called (1), in other words (1)
> **File Paths:** spread-operator.js (1), examples/spread-operator.js (1)
> **CLI Commands:** node (1)
> **Prerequisites:** required to (1)
> **Speakers:** - [instructor] (1)

#### [Using the map() function](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/using-the-map-function?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/using-the-map-function?u=76281980&t=0)** - [Instructor] All right, so now that we've taken a look at the spread operator, let's move on to the first of several built-in functions that we're going to see that will really help us manipulate data in a functional or declarative way. So the first of these functions is called map, and here's the basics of how the map function works. Essentially, the map function allows us to take an array full of elements and apply some sort of transformation to each and every element in the array. So for example, we might have an array of numbers and we might want to double all of the numbers. Now, another example of this might be if we have an array of measurements that are in inches, and we might want to convert all of those measurements to centimeter measurements. Well, that's another thing that we could use the map function for. And we might also have, let's say, an array of objects representing people and we might want an array containing just those people's names. Well, this is yet another case where we could use the map function. Now, the thing to know about this, and we'll take a look at this in just a minute, is that the general way that this is done in procedural programming is just using a for loop to loop through all of the elements and apply that transformation. But again, that's a very procedural way to go about doing things and generally can be very difficult to troubleshoot when something goes wrong.
>
> **[1:33](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/using-the-map-function?u=76281980&t=93)** So that is where the map function comes in, and the general way that the map function is used, here's where what we learned in the previous chapter about passing functions as arguments is going to come in handy. The way that this map function is used is we can simply define a function representing the transformation that we want to take place, and we can pass that to map, right? So if we have an array of numbers, let's say, we can just say numbers.map and pass any function to that, and the resulting array will be an array containing the results of calling that function with each element in the array. Now that might sound a little bit confusing still. So just to give you an idea of how this might work, if we wanted to square all of the elements in an array, here's how we would do it. We would just say numbers.map, and then you see we have an arrow function here with x that takes x, which represents each individual element in the array, and it returns x times itself, the result being an array containing all of those numbers squared. So that's the basic idea of how this map function works. Let's go over to our IDE and take a look at how the map function works as well as how it compares to the procedural approach. So I'm going to create a new file here, which I'll just call map.js. And inside this file, let's imagine that we have an array of numbers.
>
> **[3:06](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/using-the-map-function?u=76281980&t=186)** Pretty straightforward, right? We'll just say const numbers =, and you can use whatever numbers you want, but I'm just going to do the very simple 1, 2, 3, 4, 5, 6, and I'll go all the way through 10. Cool, now let's say that we wanted to modify all of these numbers in some way. I'm just going to use doubling here as an example. Well, the procedural way of doing this would look like this. We would say const doubledNumbers =, and then we have an empty array. And then we would say for (const x of numbers), and then inside here what we would do is we would just say that we want doubledNumbers to have this new number added on. And we would calculate that new number by saying const newNumber =, and then we'll just say x * 2. Okay, so we would just say doubledNumbers.push(newNumber). And then after this for loop completes, we know that we have the results so we can just say console.log(doubledNumbers), and that's all we need to do there. So if we run this thing by saying node examples/map.js, sure enough, we see that we have an array containing all of the original numbers doubled. So that's the procedural way of doing things, and it's already a little bit difficult to read. If this example were any less common, it would take you a second or two to figure out what this bit of code was actually trying to do. So let's take a look at how we can recreate
>
> **[4:40](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/using-the-map-function?u=76281980&t=280)** this exact same thing using the map function. Well, here's what it looks like. I'm just going to jump right in and show you what this looks like. I'll comment out the procedural way of doing things here so that we can compare them. And what we would do here is we would just say const doubledNumbers. Oops, let's try that again. There we go. = numbers.map. And then we're going to say x, x * 2. All right, and that is all we need to do. So essentially what this is saying is we want this doubledNumbers constant to just be all of the values from the numbers array passed into this function that we specify as an argument. Now in this case, we used an anonymous function. You don't have to do that, you could say const double = x, and then x * 2, and that would make it even more readable, right? You could just say numbers.map(double), and that tells you exactly what is going on, right? So again, this is why this kind of declarative code is so nice because you can actually see what each variable in your code is just by looking at the definition on the other side, right? So think of this kind of like a mathematical definition. We're saying that our doubledNumbers array is just all of the numbers in our numbers array doubled. All right, so let's just print that out here. We'll say console.log(doubledNumbers). And what we should see is if we run this thing like so,
>
> **[6:16](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/using-the-map-function?u=76281980&t=376)** sure enough, we should see that we have the exact same result. And I dare say that this is much nicer to look at as well as to write than what we had up here. So that is the basics of using the map function.

> [!info]- Semantic Content
>
> **Code Identifiers:** doublednumbers (8), newnumber (2)
> **Analogies:** for example (1), imagine (1), kind of like (1)
> **File Paths:** map.js (1), examples/map.js (1)
> **CLI Commands:** node (1), make (1)
> **Env Vars:** ide (1)
> **Cross-References:** previous chapter (1)
> **Definitions:** is called (1)
> **Warnings:** troubleshoot (1)

#### [Using the filter() function](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/using-the-filter-function?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/using-the-filter-function?u=76281980&t=0)** - [Instructor] All right, so now that we've seen the map function, the next declarative function that we're going to take a look at here is the filter function. Now, the map function allowed us to apply some sort of transformation to each element in an array, but the filter function is a little bit different. What it allows us to do is specify a function that will tell [[JavaScript]] which elements in an array we want to keep or discard. So for example, we might have an array of numbers again, and we might want a new array that contains only the elements in that original array that were even. Or in a slightly more complicated case, we might have an array of employee objects and we want a new array that contains only the employee objects that have a salary of more than 100,000 a year, for example. All right, now one key thing to notice here about the filter function is that we're not making any changes to individual elements, we're only deciding which elements we want to keep from the array. All right, now the way that the filter function works is we pass a function to it, but instead of that function returning a value that we want to transform each element into, it simply returns true or false, indicating whether or not we want to keep the element. Right, true means we want to keep the element,
>
> **[1:34](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/using-the-filter-function?u=76281980&t=94)** false means we want to discard the element. So for example, we might have an array of numbers again, and we want to keep all numbers that are greater than 2. Well, we would just have to define a function that returns whether an individual element is greater than 2, right? Returns either true or false in that case, and that would allow us to keep only those elements. So that's the basics of how the filter function works. Let's take a look at this in code and just like we did with map, we're going to start off by taking a look at the procedural way of filtering and then we'll see how filter can make this process more declarative and functional. So let's start off with our numbers array again. We're just going to copy and paste that here. And let's just use the simple example of maybe keeping all of the even numbers in the array. Well, the procedural way of doing this would be to create a new array. We'll just say something like even, in fact, we'll just call it evens and that's going to start off as an empty array. Then we would loop through this thing by saying for (const x of numbers). And then inside here, we would have an if statement and we would say if with some condition regarding each individual number, right? So we might say if (x % 2 === 0) right, that's how you check whether a number is even in JavaScript. In that case, we would want to say evens.push
>
> **[3:10](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/using-the-filter-function?u=76281980&t=190)** and mutate that evens array by adding x to it, right? Otherwise we wouldn't want to do anything, so there's no else statement for this for loop. Okay, so let's print out our evens array, we're going to say console.log(evens). And now if we run our code here with node examples/filter.js, sure enough, we see that that's given us only the even numbers from our array. All right, so again that is the procedural way of doing things, and it's not immediately clear just by looking at this thing what this code is supposed to do. Yes, it's pretty easy to figure out, but if you're working with a codebase that's all made up of this kind of code, that really slows your development down. So let's take a look at the way we can use the filter function here to clean this up. Really all we have to do here is define this condition that we passed to the if statement as its own function. So we're just going to say const isEven, and that'll just take a number and return. Oops, I wanted to use an arrow function there for that. So let me just start over. We'll say isEven, and that's going to take x and return x % 2 is equal to 0. So that's going to return true or false for that number. And by the way, this kind of function where it takes a single argument and returns either true or false for that argument is called a predicate. Just a little piece of terminology there that's going to be helpful for us going forward.
>
> **[4:44](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/using-the-filter-function?u=76281980&t=284)** So now that we have this isEven function, we can much more easily calculate the evens here by saying const evens = numbers.filter, and we're simply going to pass that isEven function as an argument. So again, all that we're doing here is we're creating a declarative definition instead of creating a procedural series of steps in order to get the results. So we're just saying that this evens array is simply all of the even numbers from our numbers array. All right, it's as simple as that. So just like with map, you could have done this using an anonymous function, but in general it's considered a good practice to name your functions because it just makes it more readable. So anyway, that is the basics of how to use the filter function in order to filter an array in a declarative way.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[JavaScript]] (2)
> **Analogies:** for example (3), just like (2)
> **Code Identifiers:** iseven (4)
> **CLI Commands:** make (1), node (1)
> **Definitions:** is a  (1), is called (1)
> **File Paths:** examples/filter.js (1)
> **Best Practices:** good practice (1)
> **Speakers:** - [instructor] (1)

#### [Using the every() and some() functions](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/using-the-every-and-some-functions?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/using-the-every-and-some-functions?u=76281980&t=0)** - [Instructor] All right, well, at this point we've talked about the map function and the filter function, so the next function that we're going to take a look at is actually two functions, and these are the every and some functions. Now these two functions are kind of similar to each other and they're also kind of similar to the filter function, but with every and some, what we want to do generally is take an array of some sort of data, right? Numbers, or strings, or objects, something like that. It can be anything. And we want to get a single true or false answer regarding whether the elements in the array match some sort of criteria. So to give you an idea of what this might look like, let's say that we have an array of all odd numbers, one, three, five, seven, and nine, and we want to know whether all of those numbers are odd. Well, in that case, what we would do is we would define a function called isOdd that tells us whether or not a single number is odd, and we would pass that to numbers.every. Now, in this case, it would return true since every number is odd, right? It reads almost like a sentence there. However, if any of those numbers doesn't fit the criteria, in this case, if at least one of the numbers is even or not odd, then it will return false, right? So if all of the numbers are odd, then it returns true, if any of the numbers are not odd, it returns false. And of course, if all of the elements are not odd,
>
> **[1:37](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/using-the-every-and-some-functions?u=76281980&t=97)** it returns false as well. So that's the every function. The some function behaves in a similar way, but there's one key difference to this. The some function checks to see whether any of the elements in the array returns true for the function that we pass, whereas every checks to see whether all of the elements return true for that function. So going back to our array of all odd numbers here, if we say numbers.some isOdd for this array, that's going to return true, right? Because there's at least one element in there that is odd. Now here's where things get a little bit different between some and every. In the case where we have one element that's not odd, right? If we have an even number in there, well, some is still going to return true here because there's still at least one element that returns true. Whereas with every, it would've started returning false by now. And this continues all the way up until we have not a single element in the array that returns true, at which point numbers.some will return false. All right? So what you see here is the main similarity between these two functions. In the case where all of the elements return true for some criteria, both, some, and every are going to return true. In the case where none of the elements fit a certain criteria, both some and every are going to return false. However, in the in-between case
>
> **[3:12](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/using-the-every-and-some-functions?u=76281980&t=192)** where there are some elements that return true and some that return false, that's where these two functions are going to behave differently, right? Every is going to return false in that situation, some is going to return true. Now one last question that you probably have here is whether or not there is a none function, right? Which returns whether or not none of the elements from an array match a given piece of criteria. And while there is not a specific non-function, it's actually pretty easy to create, you just have to use numbers.some with the not operator before it and that will tell you if a piece of criteria is true for none of the elements in the array. Cool. So that's the basics of the every and some functions. Let's take a look at how those work in code. I'm just going to create a new file called every-and-some.js. Inside here we're going to have our numbers array. In fact, I'll just copy that from map here. There we go. I'm just going to copy that and paste it into here. All right, and the procedural way of doing things here, definitely a lot more involved than using every or some, we would have to create some sort of result, right? We'll just say let result. We actually do have to use the let keyword here so that we can change this. We'll have that start off as false I suppose. And then what we would do, we'll just start off with every here is we would say for const x of numbers. And for each number we would want to check to see
>
> **[4:48](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/using-the-every-and-some-functions?u=76281980&t=288)** whether some piece of criteria was true for that number, right? So in our case here, if we're going to check to see whether all of the numbers are odd, we would want to say something like if, and then we'll say x modulus two is equal to zero, that means it's even, in that case, we would want to set result to false, all right? And actually in this case, we'll start off with result being true. So with this procedural implementation of every, the moment we find that one of these numbers is not odd, we would want to set the result to false and we would want to exit the for loop by saying break. All right? So that's all we would have to do there. And then we could say something like console.log result, and in this case, we know that that's going to be false because not all of these numbers are odd. So let's just test that out. We'll say node every and some, oops, that should be examples/every-and-some. And sure enough, we see false, if numbers here has only odd numbers in it, right? Let's say one, three, five, seven, nine, for example, and run our application again, we would see that that would return true. Now if we wanted to implement some using this same way of doing things, that would be a little bit different, right? We would want to start the result here off as false and if any of those numbers matched that criteria, so if any of the numbers was odd here,
>
> **[6:24](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/using-the-every-and-some-functions?u=76281980&t=384)** then we would want to set the result to true and we could simply break there, right? There's no reason for us to continue going through the array once we know that there's at least one element in there that matches our criteria. Okay? So let's just try that out again. Sure enough, that's going to return true when all of the numbers are odd. But if we make this maybe all evens, so two, four, six, eight, 10, well, that should return false there. And if we have some odd and some even, right, if we add one at the beginning there, for example, that will still return true. So that's the basics of implementing some and every procedurally. Again, pretty messy, not the most readable thing. It's much, much easier to simply use the some and every functions, here's what this is going to look like. We're just going to say console.log and we'll actually say numbers.every and we'll actually define an isOdd function here by saying const isOdd equals x, and then we'll return x modulus two is not equal to zero, which is how we tell whether a number is odd in [[JavaScript]] or not. So we're going to say numbers.every isOdd and then I'll print out numbers.some. All right? So numbers.some isOdd as well and that'll allow us to see the difference, so let's start off with the case where they're all odd. One, three, five, seven, nine, and run our code. Oops, let's just clear that out first. Sure enough, we see that both of those return true
>
> **[7:57](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/using-the-every-and-some-functions?u=76281980&t=477)** in that situation. If all of the numbers are even, so two, four, six, eight, 10, if we run this, we see that that returns false for both of those because none of the elements are odd. And the place where they're going to return different results is when we have a mixture of odds or evens, so if we have one at the beginning there, sure enough, we see that every is going to return false because not all the elements are odd and some is going to return true because we have at least one odd element in there. So that's the basics of using every and some. These make your life much easier when you need to do this kind of check inside an if statement, for example, right? So, you know, instead of having to write this whole for loop like we did before, you can simply say something like, if numbers.some isOdd, and in that case you could say something like, there is at least one odd number in the array. And that's, again, much easier than if you had to use this whole procedural approach just to calculate that condition. So that's the basics of using every and some in order to investigate the contents of an array in slightly different ways.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[JavaScript]] (1)
> **Code Identifiers:** isodd (7)
> **Analogies:** for example (3), similar to (2)
> **CLI Commands:** make (2), find (1), node (1)
> **File Paths:** every-and-some.js (1)
> **Definitions:** is a  (1)
> **Speakers:** - [instructor] (1)

#### [Preventing mutation with the slice() function](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/preventing-mutation-with-the-slice-function?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/preventing-mutation-with-the-slice-function?u=76281980&t=0)** - [Instructor] All right. So at this point we've taken a look at several functions that can be used to declaratively manipulate data in [[JavaScript]]. And the next function that we're going to take a look at is the slice function. Now, while the slice function can definitely be used for functional [[Data Manipulation]] as well, its main purpose in our case is to help us prevent mutation when using certain other functions. Let me show you what I mean here. The first thing to know here is that JavaScript arrays have a number of different functions that you can call on them. As I'm sure you're aware, and somewhat confusingly, some of these functions don't mutate the arrays that they're called on, such as .map .filter, and every in sum, as we've seen, but other functions do actually mutate the underlying array. So as an example here, array.sort, this will actually change the order of elements in the array that you call it on. I'll show you an example of this shortly. Array.reverse, this will also modify the order of elements in an array. Array.push, this will mutate the array that we call it on by adding another element to it and array.pop, this will also modify the array that it's called on by removing an element from it. And these are just a few of the mutating functions on JavaScript arrays in general. It's not always obvious whether a function
>
> **[1:34](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/preventing-mutation-with-the-slice-function?u=76281980&t=94)** that you can call on an array modifies it or not. So I would definitely recommend testing it if you come across a function that you're not sure of. So now that we're a little bit more familiar with the concept of mutating versus non mutating functions, let's go back to our slice function. What I'm going to do is just create a new file here. We'll say slice.js as the title. And the first thing that we'll do here, well the first thing that I want to demonstrate anyway is that the map function as well as filter every and sum, don't modify the array that they're called on. So in our case, notice that when we wanted to get all of the doubled numbers, we just created a new constant that stored the value of the doubled numbers, right? That didn't actually modify numbers. And in fact, if we were to print out the original numbers array, and we'll just run our code here again by saying nodeexamples/map.js, sure enough, we see that that original array remains completely intact, right? This goes right back to the concept of immutability that we talked about at the beginning of the course. However, when we use functions like sort or reverse, and I'm just going to copy this numbers array here for this demonstration into slice, when we use those kinds of functions, so we'll say numbers.sort for example, and let's print out numbers after that, we'll say console.log numbers. Well, if we run this, what we'll see
>
> **[3:09](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/preventing-mutation-with-the-slice-function?u=76281980&t=189)** by saying node examples/slice.js is that sure enough that did actually modify the order of those numbers. Now, it might not quite make sense why it chose this specific order. That's something that I'll talk about next. But the point is that the order of the array has been modified, the original array is no longer intact, which we really want to avoid in [[Functional Programming]]. And the same kind of thing here is true with .reverse. For example, if we say that and run our code, what we'll see is that sure enough, that modifies the order of those numbers in that array. So again, we've destroyed the integrity of our original array. If we say numbers.push, let's say 11, sure enough, we see if we run our code again here, that that modifies the array. So anyway, there's all of these functions that mutate arrays. But the idea here is that we really want to be able to still use these functions. It would be nice to still be able to, you know, do things like reverse the order of elements in an array without mutating the array. And that is where the slice function comes in. I'll talk about the slice function in more detail in a minute, but the first thing to know is that it can be used to create a copy of an array just by calling it in between the array that we want to avoid mutating and whatever mutating operation we create, right? So in other words, this here just creates a copy of numbers,
>
> **[4:43](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/preventing-mutation-with-the-slice-function?u=76281980&t=283)** and then we see that calling reverse on that. If we run this code again, what we'll see is that sure enough, numbers is intact. So in this case, what we would want to do is just say something like const reversedNumbers equals numbers.slice.reverse. And if we print that out here, we'll say console.log reversednumbers. Oops, let's try that again there. All right, let's just save our code and run it again. What we'll see is that sure enough, that allowed us to use the reverse function without modifying the original data. And the same thing is true with sort here, right? If we say .sort after calling .slice and run it, we see that that gave us a new array with the elements in a different order instead of modifying the original array. So that's the basic idea of using .slice. So let's actually talk about what .slice does because it can actually be kind of useful for manipulating array data in a declarative way as well. Basically, all the slice function does, I'll just print this out so I can show you, is when we call it on an array, it allows us to select a starting point and an ending point in that array that we want to create a copy of. So here's how that works. The first number that we have to specify is the starting index, right? This is the number that it will start at. So let's say that we want to start at index 2,
>
> **[6:16](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/preventing-mutation-with-the-slice-function?u=76281980&t=376)** that's going to be right here. And the second argument here is the ending point or the ending index that we want to stop before. So if we want to stop before index 6, for example, that's this one right here. We can say 6, and that's going to give us everything up to index 6, but not including the element at index 6. In other words, this first number is inclusive, and this second number is exclusive. So let's just print that out. We're going to run our code again here like so. And sure enough, we see that that gives us this nice little slice right here, 3, 4, 5, 6. Okay, now if we leave off the second argument here, the default behavior is that it will just go all the way to the end, right? So in other words, this is getting everything from index 2 to the end of our array. Let's run that and see, sure enough, we get everything from 3 through the end of the array. And finally, if we don't pass any arguments at all, the default starting point is the beginning of the array. So that's why this creates a copy of our array for us just by calling slice with no arguments. Now, one last thing that I wanted to point out here is that there's another way of creating a copy. There's actually several ways of creating a copy, but the other way that you'll see sometimes is instead of calling numbers.slice.sort, you might see something like const. And here we'll use reverse instead just to keep the name of that variable intact there, or just reverse.
>
> **[7:53](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/preventing-mutation-with-the-slice-function?u=76281980&t=473)** There we go. We can say constant reversed numbers equals, and then we can actually use the spread operator inside square brackets to create a new array that contains all of the elements from the old array. So we can say dot, dot, dot numbers .reverse, and that's going to have the same exact effect. So let's just run that here just so that we can see it. And sure enough, we see that that gives us the same result as calling slice with no arguments. So it's really up to you which one of these you want to use, whether you use slice to create a copy or the spread operator to create a copy. But the point here is that we want to create a copy that these mutating operations will be applied to instead of applying them to the original array.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[JavaScript]] (3), [[Data Manipulation]] (1), [[Functional Programming]] (1)
> **Analogies:** for example (3), such as (1)
> **File Paths:** slice.js (1), nodeexamples/map.js (1), examples/slice.js (1)
> **Definitions:** in other words (3)
> **CLI Commands:** node (1), make (1)
> **Cross-References:** go back to (1), we talked about (1)
> **Code Identifiers:** reversednumbers (1)
> **Speakers:** - [instructor] (1)

#### [Using the sort() function](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/using-the-sort-function?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/using-the-sort-function?u=76281980&t=0)** - [Instructor] All right, so now that we've taken a look at how to use the slice function to prevent mutation on some of the functions that would otherwise cause mutation, let's take a look at how to use the sort function. Now, the basic idea of the sort function is pretty straightforward. All it does is it changes the order of elements in an array based on some sort of criteria that we get to specify. That's a little bit more complex than what we saw with map and filter, as I'll show you shortly, but that's the basic idea. Now, one thing to remember here is that sort mutates. We already saw that previously, but I just wanted to point that out again. And what this means is that you're just going to want to use slice or a spread operator to create a copy of the array that you're calling sort on in order to abide by the functional concept of immutability. All right, so that's the basic idea of the sort function. Let's take a look at how to actually use this thing. It's not quite as simple as you might think. So let's start off with an array of numbers, but let's actually make those numbers in a different order. So maybe we'll do 10, 4, 5, 2, 9, 7, 8, right? I'm not going to do all of the numbers between 1 and 10 there. And let's say that we wanted to put all of these numbers in order. Well, the easy way to do this is by saying const sortedNumbers equals, and then we'll say numbers
>
> **[1:33](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/using-the-sort-function?u=76281980&t=93)** and create a copy by saying .slice, and then we can say .sort. Now, if we print out our sorted numbers now, and actually I want to add 1 in there because there's something I want to show you with this. If we print out our sorted numbers after doing that, we'll say node examples.sort.js. What we'll see is something a little bit strange. Instead of going 1, 2, 4, 5, 7, 8, 9, 10, it goes 1, 10 and then it goes back to 2, 4, 5. Well, the reason for this is that the default sorting order or sorting strategy in [[JavaScript]] is to sort elements alphabetically. In other words, 1 and 10 are closer to each other alphabetically if these were strings, right, in the same way as a and ab would be close to each other in the dictionary and would come before, you know, words that start with B, for example. And that's oftentimes not what we want. In fact, most of the time that's not what we want. So here's how to specify a different sorting strategy or sorting order when working with this sort function. All we have to do is define another function here, and we'll call this function something like numericalAscending. And the way that this function works is a little bit different than what we've seen with map and filter. What it does is it actually takes two arguments, which we'll call a and b,
>
> **[3:08](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/using-the-sort-function?u=76281980&t=188)** and these two arguments represent any two elements in the array, all right? Now the idea of this function that we're specifying here is that it will tell sort whether for any two elements in the array a should come before b. Now the way it does this is by returning a positive value if b should come before a, a negative value if a should come before b, and zero if the two values should be considered equal. So here's what this is going to look like. We're going to say if a is greater than b, well in that case a should come after b, so we're going to return a positive number. We'll just say return 1. Otherwise, we'll say else if a is less than b, then, in ascending order, we want a to come before b, so we're going to return a negative value. Otherwise, if a and b are equal, we're just going to return zero, indicating that they're equal. So that is how numericalAscending works. And if we pass this to sort now by saying numericalAscending, like so, if we run our code now, oops, there we go. What we're going to see is that sure enough, this puts it in numerical order instead of alphabetical order. Now, how would we modify this
>
> **[4:40](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/using-the-sort-function?u=76281980&t=280)** if we wanted to make it numericalDescending? Well, all we would have to do in this case, and actually I'll create a copy of this so that I don't destroy it. There we go. We'll say numericalDescending. And now all we need to do is make it so that we return a negative number when a is greater than b. We'll just use negative one here. And a positive number when a is less than b. So we'll say 1 for that. So if we pass numericalDescending now as an argument, there we go, and run our code again, we see that that will sort our numbers in descending numerical order. Now these two examples might seem a little bit silly, and in fact, there's an easier way of doing this. All we have to do is if we want to sort our numbers in ascending order, we can simply say a minus b, and that will have the same properties, right, the same positive, negative, or zero return values as what we had there, the lengthier logic that we had before. And for numericalDescending, we can just say b minus a, and that will have the same properties there as well. So let's run our code again. Sure enough, we see that that is numericalDescending. If we change this back to numericalAscending and save it, we'll see that that sorts it in ascending order. Now, this can be used in somewhat more complex situations, right? Let's say that we have, maybe instead of an array of numbers, we have an array of objects
>
> **[6:14](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/using-the-sort-function?u=76281980&t=374)** and we want to sort by some property on each of those objects, right? Let's say that we want to sort numerically by age. Well, all we would've to do is say a.age minus b.age here, and we could do the same thing with numericalDescending with that as well. I'll leave that up to you to experiment around with there, but that's the basic idea of sorting numbers using that sort function and specifying different sort orders, again by defining a function and passing that function as an argument. So yet another example of how functions can be passed as arguments and how this can often make our code much more flexible.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[JavaScript]] (1)
> **Code Identifiers:** numericaldescending (6), numericalascending (4), sortednumbers (1)
> **CLI Commands:** make (4), node (1)
> **Definitions:** in other words (1), is a  (1)
> **File Paths:** examples.sort.js (1)
> **Analogies:** for example (1)
> **Speakers:** - [instructor] (1)

#### [Using the reduce() function](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/using-the-reduce-function?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/using-the-reduce-function?u=76281980&t=0)** - [Instructor] Alright, so the next function that we're going to take a look at is the reduce function. And this function is actually pretty special because it's so powerful that it can be used to recreate pretty much any of the other functions that we've seen already, map, filter, every sum, and so on. Now, the problem with this function of course, is that it can tend to be a little bit trickier to understand conceptually, but hopefully through my demonstrations you'll be able to see how this thing works and what it can be used for. So first of all, the basic idea of the reduced function is pretty simple. All it does is it takes an array and reduces it down to a single value, or it doesn't actually have to be a value, it can be a single piece of data including an array or object. And the simplest case of this would be if we wanted to take maybe an array of numbers and reduce it down to a sum or if we wanted to get the product of all of the numbers in an array. Now this can be used for much more complicated cases as well. For example, if we wanted to take an array and maybe turn it into an object or something like that. And that's where the power of this function can really be used. But let's start off with a simpler example of this. I'm going to create a new file here, which we'll call "Reduce.JS". And inside this file, what I'm going to do first of all is copy this numbers array from map and paste it in here.
>
> **[1:36](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/using-the-reduce-function?u=76281980&t=96)** What we're going to do is we're going to start off by getting the sum of this array of numbers. Now again, the procedural way of doing this, you already know how to do that. So I'm not going to walk you through it, but it would involve a for loop usually. What we're going to do with reduce though is we're going to say const sum equals numbers.reduce. And here's how this function works. What we're going to do is we're going to pass a function to numbers.reduce. And what this function does is it tells reduce how to combine the current number that it's looking at in the array, right, so it's going to look at one number at a time in the array and it tells it how to combine that number with whatever the result is that it's calculated so far. That might seem a little bit strange. So let me just show you what this would look like if we want to get the sum of an array of numbers. All we're going to do here is we're going to say numbers.reduce and the function that we pass here is going to take two arguments. I'm going to call that result and X. And then as I said, all we need to do here is tell reduce how we want it to combine the current result so far with the current element that we're looking at in the array. In the case of getting the sum, all we need to do is say result plus X and we're returning that from this function. Now one thing that I want to make very clear here is that you don't want to say result plus equals X.
>
> **[3:12](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/using-the-reduce-function?u=76281980&t=192)** That's not the point here. You want to return of value indicating what the next result should be. So in other words, when we say result plus X, we're saying that the next time this function is called, result is going to be whatever we just returned the previous time. That might sound a little bit confusing. First, let me just demonstrate that this does, in fact, give us the sum. We will log out the sum right there. We're going to run our file by saying node example/reduce.JS. Sure enough, we see that that gives us the sum of all of those numbers. Alright, so let's take a closer look at how this is working behind the scenes. We're just going to say result plus X. We'll need to make sure we return that. And I'm just going to add curly braces there. We'll say return result plus X. And now what I'm going to do is I'm just going to log out what the value of result and X are each time this function is called. So we'll say, and I'll just use back ticks here, result is, and then we'll say result inside of there. And then we'll say X is and put X right there. All right, so now we're going to see what's actually going on behind the scenes when we call reduce. So here we go. And sure enough, we see that the first time reduce calls our function, it actually does so with the first two elements in the array. In other words, if we look at this a different way, the starting value for result is actually going to be
>
> **[4:47](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/using-the-reduce-function?u=76281980&t=287)** the first element in the array. And the first X that we're looking at is going to be the second value. And then whatever this function returns, that's going to be result the next time the function is called, right? So the first time it's called, it's called with one and two. We see that the next time result is three, X is three, that's going to return six. So sure enough, we see that the next time this function is called, the result is six, and we have the next element in our array, which is four. That gives us 10, 10 plus five is 15. 15 plus six is 21. 21 plus seven is 28. And it just keeps going until we finish. And that gives us our result. So you could do the same thing by, you know, getting the product, all you would have to do here is return result times X there. And that would give us the product, which is a pretty big number there. You can see that developing over time. Maybe what we want to do is put all of these numbers in an object as keys and have some value, for example, false, as the value for each of those keys. Well, all we would have to do in that case is just say return and then we would say X and we would say false there. But we need to incorporate the result which we have previously and that would look like this. We would just take whatever the current object is and use the spread operator to get all of the attributes from that object and in this case, we would actually need to provide a custom starting value since we wouldn't want to
>
> **[6:22](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/using-the-reduce-function?u=76281980&t=382)** start out with a number there. And now what we'll see if we run this is that sure enough that gives us an object that has one through 10 as keys and false as the value for all of those. So again, the point of that example, and please don't let that confuse you, I'll change this back to maybe a product or a sum there if you want, if that makes it easier to understand. But the point here was just to show that reduce can be used for a lot of different situations.

> [!info]- Semantic Content
>
> **Definitions:** is called (4), in other words (2), is a  (1)
> **CLI Commands:** make (2), node (1)
> **File Paths:** reduce.js (1), example/reduce.js (1)
> **Analogies:** for example (2)
> **Speakers:** - [instructor] (1)

#### [Combining array functions](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/combining-array-functions?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/combining-array-functions?u=76281980&t=0)** - [Instructor] Okay, well, at this point, we've learned about a number of different declarative functions that can help us manipulate data in a more functional way. So the next thing that we're going to take a look at here is how we can combine these functions to achieve more complex functionality. So what you see here, and you can find this in the beginning state of the exercise files if you want to follow along. But what you see here is an array containing a number of objects representing career data about different people, let's say at our company, all right? Well, there's a lot of different questions that we might want to ask just about this simple array. And as we'll see, the functions that we've learned about in this chapter can help us do so. So the first thing that we might want to know is what is the average salary of all of the people working at our company? Well, this one can be achieved pretty easily by combining map and reduce. Here's what that's going to look like. We're just going to say const average salary equals, and then we're going to say people, and we're going to start off by mapping each of these people objects to their salary attribute, right? We can't just get an average of all of these objects because we need numbers in order to do that. So here's what that's going to look like. We're going to say people.map, and for each person, we're simply going to return, P.salary, all right? So at this point, we have the salaries of all of the people,
>
> **[1:37](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/combining-array-functions?u=76281980&t=97)** but we want to get the sum of that, and then we'll divide it by the number of people to get the average. So what that's going to look like is we're going to say, .reduce, and now all we need to do is say, result and X, and we're going to say result plus X to get the sum. And finally, we're going to divide this by the number of people by saying people.length. And that is all we need to do there. So let's just print out the average salary here by saying console.log average salary. And if we run our program now by saying node examples slash combining.JS, that's what I ended up calling my file here. Sure enough, we see that the average salary is apparently 81,200, okay? So, let's take things a step further. Let's say that instead of just finding the average salary of all of the employees at our company, we want to find the average salary of all of the software engineers at our company. Well, here's how this is going to work. And by the way, in case you're thinking this isn't the most readable thing in the world, what I find really helps to make change like this a little bit more readable is simply to put each new thing on its own line so you can see what is going on at each stage of this sort of pipeline, right? So you can see in this case, in other words, that we are mapping each person to their salary. We're getting the sum of all of the salaries, and then we're dividing that by people.length. That's just something that I like to do sometimes.
>
> **[3:09](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/combining-array-functions?u=76281980&t=189)** So we'll go with it. But anyway, what we're going to do here is in order to get the average salary of all of these software engineers, we just need to add another call or another function, that is, into our chain. So here's what this is going to look like. We're actually going to add a call to .filter before we map our people. We're going to say that we only want to keep people whose job title is equal to software engineer. So here's what that's going to look like. For filter, remember, we just need to define a function that returns true or false, true meaning that we want to keep that item in the array. So we're going to say P.job title is equal to software engineer. Okay? So let's save that. And what we'll see now is if we run our code, oops, that's a very low number, and that is actually because we're dividing by the number of people, not by the number of software engineers. So in some cases, like this one, it may be necessary to actually split your chain up a little bit so that you can access some of the previous values. In our case here, we want to know how many software engineers there were so that we can get the average. So here's what this is going to look like. We're just going to say const and we'll say software engineers equals, and then we're just going to have this first line here, people.filter, job title equals software engineer. All right?
>
> **[4:43](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/combining-array-functions?u=76281980&t=283)** So now we're going to say const average salary equals, and instead of saying people.map, we're going to say software engineers.map. And now we get to divide by the length of software engineers instead of the length of people, and that should give us a more accurate average. So let's save that and try running this again. And sure enough, we see that that is definitely a more believable number. Okay? So anyway, that is the basics of combining these functions. This example has combined filter, map, and reduce, but there are plenty of other situations where you might combine other functions or combine these functions in different orders. I'll leave it up to you to experiment around with this a little bit more, but hopefully this has showed you how powerful these functions can make it, right? Because again, think about how much different our code would look if we had done this the procedural way, right? With a for loop, instead of using the map, reduce, and filter functions. This way is much cleaner and more readable, as well as easier to modify in the future, right? If we wanted to get maybe the average age instead of the average salary, all we would have to do is come into here and change this to P.age instead of P.salary. All right? So anyway, that is the basics of combining all of these functions to perform more complex calculations.

> [!info]- Semantic Content
>
> **CLI Commands:** find (3), make (2), node (1)
> **Definitions:** is an  (1), in other words (1)
> **File Paths:** combining.js (1)
> **Exercise Files:** exercise files (1)
> **Speakers:** - [instructor] (1)

#### [Solution: Recreating the map() function](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/solution-recreating-the-map-function?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/solution-recreating-the-map-function?u=76281980&t=0)** - [Instructor] Great. Well, hopefully you gave this challenge a try. Let's take a look at the solutions. So the first thing that I did here was I started off by calling reduce on the array that was passed into mapR. All right, so here's what that's going to look like. We're just going to say return arr.reduce, right? We knew that this was going to be the case just from the instructions. We knew we'd have to use reduce in order to recreate the map function. Now the next question to think about here was what is the starting value supposed to be for reducing where our desired result is an array? Well, in this case, it was just an empty array that we would add elements onto in this reduce callback here. So now that we have that, here's what this is going to look like. Our function is going to take the array that we have so far, all right? We'll just call that, well, here. We don't want to call it arr, we'll call it something else. We'll call it something like mapped. All right? That's going to include all of the elements that have already been mapped. And then the second argument here is just going to be each of the items that we're looking at. We can just call that x here. Not really the most explanatory name, but we know that that's going to be the case just from how reduce works. All right, so now that we have that, all that we need to do is
>
> **[1:33](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/solution-recreating-the-map-function?u=76281980&t=93)** inside this reduced callback function, we need to define how we want the accumulated value to change for each item. Well, all we really need to do here is we just need to say return, and then we're going to say ...mapped. All right? So we're going to use that spread operator in order to get all of the elements that are currently in mapped, and then we're going to add x onto the end. But since obviously we want to change the value of x by calling fn on it, we're going to change that to fn(x). All right, so that's one possible solution you could have done. You could have also used the concat method. This isn't something that we talked about previously, but the concat method actually has the same effect as push. But what it does is it just returns a new array with whatever item we pass to it added onto the end. So we could say mapped.concat((fn)x) instead, and that would give us the same results. So whichever one you prefer here. I personally prefer the spread operator version since that's what we learned in this course. But you can test this out. We're going to run test my code, and sure enough, we see that that test passes with concat and if we change it to the spread operator version here and run Test my code again, we'll see that we get the same correct result. All right, so that is the solution to this challenge. Congratulations if you figured that out, and you could have used either way or a different way as long as you used reduce in order to recreate the functionality of the mapped function.

> [!info]- Semantic Content
>
> **Code Identifiers:** mapr (1)
> **Cross-References:** we talked about (1)
> **Definitions:** is an  (1)
> **Speakers:** - [instructor] (1)
> **Non-Speech:** (upbeat music) (1)


### 4. Advanced Functional Concepts

[↑ Back to Table of Contents](#table-of-contents)

#### [Currying and partial application](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/currying-partial-application?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/currying-partial-application?u=76281980&t=0)** - [Instructor] All right. So the next thing that we're going to talk about here, now that we've gone through the basics of [[Data Manipulation]] and first-class functions, we're going to move on to what I would consider to be some more advanced functional concepts. And the first one that we're going to talk about here is something called partial application. Now, there is a special version of partial application called currying that we'll take a look at shortly. But first, let's start with the basics. So partial application, in order to demonstrate this concept, what I'm going to do is let's just imagine that we have a simple function called add that takes three numbers. This might seem a little bit odd, but you'll see why I'm doing this in a minute. And returns the sum of those three numbers, right? Now, calling this function is pretty straightforward. You already know how to do it, of course. All you do is you say add, and after that you put a pair of parentheses with three arguments inside of it, right? The same number as the number of parameters that we defined the function with. So if we run our code here by saying node examples/partial-application.js, sure enough, we see that that gives us the result. All right, now, so far, whenever we've seen our functions, and we've seen quite a lot of them in this course, one notable thing about them has been that when we call them, we have to specify all of the arguments in the same time and place.
>
> **[1:33](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/currying-partial-application?u=76281980&t=93)** In other words, when we want to call our add function, we have to have all three numbers right then and there, ready to pass into it, to get the result, right? Now, that might seem like kind of a given, but there are actually many situations where it would be very beneficial to be able to specify the arguments not all together, right? Either one at a time or in groups. So to give you an idea of what this might look like, let me show you what partial application would allow us to do. Basically, it would allow us to do things like this. It would allow us to call our add function with a single argument, right? One. Or two arguments, right? You know, some number of arguments less than the total number of arguments. And create a function where those two arguments have been fixed to a given value, right? So we could call this something like const add1 =, and then we call the add function with x equal to 1. And then later on we could call add1 with maybe the two remaining arguments, right? That is what partial application allows us to do, right? It allows us to create a sort of pre-configured version of a function, where one or more of the arguments have been fixed to some specific value. So to show you what partial application looks like in code and how to actually make it possible,
>
> **[3:07](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/currying-partial-application?u=76281980&t=187)** let's take this add function that we have here. And what I'm going to do is I'm going to create a new function here called addPartial. Now, this addPartial function is going to allow us to partially apply the first argument of the add function, x. And here's how it's going to do that. addPartial is simply going to be a function that takes one argument. And guess what it's going to return? It's going to return another function, which I'll put in another line so that you can kind of see it a little bit better here. It's going to return another function that takes the other two arguments. And then once we have all three arguments, we're simply going to call add, the original function, with those three arguments that we've accumulated, right? Now, again, this looks a little bit funny, perhaps, at first. And maybe it would be better if I put it all on one line, or maybe not. But the point here is that we can now call this addPartial function, and we get to pass in the arguments in different times and places in our code. So here's how we can use this thing now. We can say const add1 = addPartial. And then we're going to call that with the first argument, x, set to 1, right? Look what this is doing. This is pre configuring this x argument to be 1, so that we only have to specify the other two arguments, right?
>
> **[4:41](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/currying-partial-application?u=76281980&t=281)** So let's just go back to there. All right, we'll change that back to x and x. And now, right, now that we have the add1 function, we can just log out add1 called with any other two arguments, right? We could say 10 and 11, for example. And if we run our code, sure enough, that gives us 22. 10 plus 11 plus 1, right? So in other words, what's happening here is we're specifying the first argument here, 1 as x, and then when we call the return function, right, this function here with the other two arguments, it has all of the arguments in the same place, and it simply calls whatever function we wanted to partially apply with those three arguments. Now, this might seem a little bit abstract right now, but there are actually a lot of different situations where partial application can be helpful. For example, let's imagine that we have another function. I'm just going to sort of draw a divider line here to keep things separate. Let's imagine that we have a function here called something like getPropertyWithDefault. And what this does is it takes an object, it takes a property name, we'll just call that key, and then it takes a default value to return if the object doesn't have that key. All right? Well, here's what this is going to look like. It's pretty straightforward. We're just going to say if (obj[Key]), then we're going to return obj[Key].
>
> **[6:17](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/currying-partial-application?u=76281980&t=377)** Otherwise, what we'll do is we're going to return the default. And oops, we need to name that defaultValue here, because default is actually a [[JavaScript]] keyword. And that's our getPropertyWithDefault function. Now, what you can probably imagine is that there are going to be a lot of situations where this getPropertyWithDefault function is going to be called with the same arguments for key and defaultValue, right? So you might have a situation where you want to get, let's say, the 'name' property of different objects with the same defaultValue, right? So maybe we have a person object here, and we have the key 'name,' and then we have the default value of maybe like 'N/A.' And then later on, we do the same thing with a different object, maybe person2, 'name,' 'N/A.' Then, later on, maybe we do the same thing with a third object. Maybe this one's a building or something like that, instead of a person. Same thing. 'name' and 'N/A. And so on, so forth, right? Now, in this case there's a lot of repeated code. And if we decided to, let's say, change the name of the 'name' key in our objects to maybe something like 'displayName,' we would have to go through all of these references to 'displayName' and make that change. We'd have to change this to 'displayName,' this to 'displayName,' this to 'displayName.' You know, we'd have to make that same change
>
> **[7:51](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/currying-partial-application?u=76281980&t=471)** in a lot of different places. Same thing if we wanted to change the default value for all of these things. Now, here's an alternative. Because of what we now know about partial application, we can actually just create a partially applied version of this like so. What we're going to do is we're just going to say const, and we'll say something like getPropertyPartial. And we get to decide which arguments and in which order they're passed. So for example, we might want to allow the calling code to specify the key and defaultValue here. And then we simply have to return another function. All right? I'll put that down another line here, again, just to make it a little bit easier to read. That takes the object and simply calls getPropertyWithDefault with all three of those arguments put together. All right, so we're going to say object, key, and defaultValue like so. And now we can simply create our own getName function automatically by just saying getPropertyPartial and passing 'name' and 'N/A.' And now we can simply use this getName function in all of these cases instead of having to specify the 'name' and 'N/A' over and over again. And this is a lot easier now than what we had before. Because if we want to make a change to, let's say, the 'name' property, here, let me just finish doing that real quick. getName(building).
>
> **[9:24](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/currying-partial-application?u=76281980&t=564)** If we want to make a change to what the name of this property is, we can just say, you know, 'displayName.' And if we want to make a change to the default value, we can just change that in one place. So that is the power of partial application. And by the way, currying, in case you're wondering about that [[Microsoft Word|word]], is simply a specific case of partial application where all of the arguments are passed in one by one. So you end up with something that looks a little bit like this. And it looks strange, but what it lets you do... Here, let's just change it back to there. There we go. What it lets you do is choose exactly how many arguments you want to partially apply at any given point in time, right? So if we only wanted to specify the 'name' property here, like so, we could just say getPropertyPartial('displayName'). If we then wanted to say getNameWithDefault =. And then we could say getName, and we provide the defaultValue. And now we could use getNameWithDefault each time. All right? So that's the basic idea of currying there. You can choose whether or not you want to use currying. I usually stick to using just plain old partial application, like what you see here. But the choice is up to you. So that is the basics of partial application in [[Functional Programming]].

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Data Manipulation]] (1), [[JavaScript]] (1), [[Microsoft Word|Word]] (1), [[Functional Programming]] (1)
> **Code Identifiers:** displayname (7), defaultvalue (6), addpartial (5), getpropertywithdefault (4), getname (4)
> **CLI Commands:** make (7), node (1)
> **Analogies:** imagine (4), for example (3)
> **Definitions:** is a  (2), in other words (2)
> **File Paths:** examples/partial-application.js (1)
> **Cross-References:** go back to (1)
> **Speakers:** - [instructor] (1)

#### [Recursion](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/recursion?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/recursion?u=76281980&t=0)** - [Instructor] All right, so now that we've taken a look at currying impartial application, let's move on to the next advanced concept, which is recursion. So I'm going to create a new file here called recursion dot js. All right, and put simply, recursion is when a function calls itself in order to solve a problem. Now, there are many problems that can actually be solved more easily using recursion than without recursion, but we're just going to stick to a few simple examples here. So first things first, let's take a look at how to recreate something like a simple loop using recursion. I'm going to call this something like countdown. And what this function is going to do is it's simply going to take some argument, and without using a for loop, it's going to count down from that number, logging each number that it gets to along the way, right? So if we pass in 10, it's going to log out 10, nine, eight, seven, six, all the way down to zero, and then we'll have it say, blast off or something like that. Now, the way that you might be tempted to implement this is by using a for loop, right? We'll say for let i equals x, and then we say i is greater than zero. And then we say, i minus minus and you could do that of course, but there's actually a way using recursion that we can do this with no for loops at all. And it's actually surprisingly simple too.
>
> **[1:34](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/recursion?u=76281980&t=94)** Here's how it's going to work. We're going to say, if x is equal to zero, then what we're going to do is we're going to log out, blast off, alright? Otherwise, what we're going to do is we're simply going to call our countdown function with whatever argument we just had, minus one. So here's what this is going to look like. We're going to say else, then we'll say console dot log, and we'll log out X, and then we'll say countdown. And as I said, we're going to call it with x minus one. So that is all we need to do in order to create a recursive loop. And now let's just call countdown with, let's say the argument 10. And if we run our code with node examples slash recursion dot js, sure enough we see that it starts off at 10 and goes all the way down to one, and then it says blast off. So you might be curious about exactly what's happening here, and it might seem a little bit strange that we're able to do this in the first place because, well, where is the for loop? Well, as a matter of fact, in most situations, we don't actually need a for loop, because if we want to do the same thing over and over again, we just have to have that thing, right, that function, call itself with some change to its arguments. Now that change is very important.
>
> **[3:07](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/recursion?u=76281980&t=187)** There's actually two important pieces whenever you're using recursion to solve a problem. The first is you need to make sure that there's some sort of change that moves the state of the application or the state of the argument here towards some sort of stopping point or base case where the recursion will end, right? So these two things you really have to have both of them because if you have a change but don't have a stopping point, then well guess what? You're simply going to end up with an application that recursors infinitely. And likewise, if you have a stopping point, but no change that moves the function toward that stopping point, you're going to have infinite recursion there, right, because the function's just going to keep calling itself over and over again. So that's the basics of recursion. As I said, there are many different things that you can do with this. One of those things is calculating the Fibonacci sequence, right? So the Fibonacci sequence goes a little bit like this. You start off, the first two numbers are one and one, and from there every number is simply the previous two numbers added together. So the next one's two, then one plus two, which is three, then two plus three, which is five, then three plus five, which is eight, then five plus eight, which is 13, then eight plus 13, which is 21. You get the idea. Now, the simple way to implement this, it's actually easier to implement without a for loop,
>
> **[4:41](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/recursion?u=76281980&t=281)** which may surprise you, but here's what it's going to look like. We're just going to say const fib equals, and then we're going to take an argument n which specifies which element we want in this sequence, all right? And then all we need to do is say if n is less than or equal to one, right, in other words, if n is either of these first two numbers, then we simply want to return one, right? That's our stopping point or base case. Otherwise, what we're going to do is we're simply going to recurse by returning the sum of Fibonacci n minus one and Fibonacci n minus two, right? Those are the previous two numbers in the sequence, and that is actually all we need to do. It's deceptively simple, right? So now all we need to do is we'll just say console dot log. We'll say fib and maybe we want, let's see, zero, one, two, three, four, five. Maybe we want the fifth element in the sequence, zero indexed, of course. Well, what we're going to do is we're going to say fib five. And if we run this now, all right, we'll run node examples recursion dot js, we'll see that sure enough that gives us eight. And if you run this with a higher number like fib 10, all right, we'll run that again. Sure enough, we see that that gives us 89. You might want to work that one out by hand, but I'll leave that up to you. So anyway, that is the basics of recursion in [[Functional Programming]].

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Functional Programming]] (1)
> **CLI Commands:** node (2), make (1)
> **Definitions:** in other words (1)
> **Speakers:** - [instructor] (1)

#### [Memoization](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/memoization?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/memoization?u=76281980&t=0)** - [Speaker] Alright, so the third and final advanced functional concept that we're going to talk about here is a function called memoization. Sort of like memorization, but without the R. And interestingly enough, kind of similar as a concept. So let's create a new file and we'll say memoization.js. And in order to demonstrate this, what I want to show you is what happens if we try and run our recursion file, right? This Fibonacci function with a number much higher than 10. Let's in fact just try 10. Well, what you'll notice in this case is if we try and run our code with examples/recursion.js, it takes a long time to calculate that result. Now, instead of sitting around and waiting to see exactly how long that takes, what I'm going to do is stop it. And the reason why this is taking so long has to do with how this is being calculated, right? So you can picture that in order to calculate the 100th Fibonacci number, we need to calculate the 99th and the 98th Fibonacci numbers. However, in order to calculate the 99th Fibonacci number, we need to calculate the 98th and the 97th Fibonacci numbers. And in order to calculate the 98th Fibonacci number, we need to calculate the 97th and the 96th Fibonacci numbers. And one thing you'll notice here is that we're already seeing some repetition in the calculations that we have to perform, right?
>
> **[1:35](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/memoization?u=76281980&t=95)** So in other words, we have to calculate the 98th number twice. If you were to keep expanding this out, you'd find that we have to calculate the 97th number, I believe, four times. And that number would keep going up exponentially as we go down and get closer and closer to zero and one, right? So in other words, there's this sort of exponential growth happening in terms of how many calculations are needed, and that's what's causing our application to not produce that result. So that might seem a little bit strange, but the solution here is this memoization thing. And what I'm going to do for this is I'm going to copy the Fibonacci function and I'm going to paste it in memoization.js. Now, the key concept of memoization here is that whenever a function is called, what we want to do is we want to remember what argument that function was called with and what the result was that the function produced for that argument. So in other words, if we know that Fibonacci six is equal to eight, well, every time our Fibonacci function is called with the argument six, from now on, we can simply return eight instead of having to go all the way down the chain as we did before, right? Recursively. Alright, so here's how this works. What we're actually going to do is we're going to create a mutable object called memo. All right? So we'll say let memo.
>
> **[3:08](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/memoization?u=76281980&t=188)** And what this object is going to contain is the keys, which are going to be the arguments that this Fibonacci function was called with. And the value for that key is going to be the result that that function produced for that argument. So we would have, you know, zero, one, one, one, and then we would have two that would produce two, three would produce three, four would produce five, right? Because two plus three, you see where we're going with this. And then we would have five would produce eight, six would produce 13, and so on, so forth. Now, what our Fibonacci function is going to do is it's going to check to see if a given argument is already in this memo object. And if it is, it's simply going to return that value without calculating it itself. If it's not, it's going to figure out the answer for that argument and insert it into this memo object. All right, now you might be thinking, why are we modifying this memo object? Aren't we supposed to avoid modification in [[Functional Programming]]? Well, the answer to that is yes, and there is actually a way to do this without modifying this object, but it's actually a lot more complicated. And so for now, we're just going to be comfortable with modifying this thing. So here's how this is going to work. As I said, what we're going to do is we're going to start off by seeing if the memo object contains an entry for this argument already.
>
> **[4:44](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/memoization?u=76281980&t=284)** And if it does, we're just going to return memo and, all right? Now, on the other hand, if it doesn't, we're going to want to produce that result and make sure we set that inside this memo object. So here's what that's going to look like. Here, in this case, we'll say, const result equals one, and then we'll say memo index n equals result. And then we're just going to return the result. And then we'll do the same kind of thing down here. What we'll do is we'll say const result equals fib and minus one, plus fib and minus two. Then we're going to say memo n equals results. So we're going to save that result essentially, and now we're going to return the result just like we did before. So now that we've done this, now that we've memorized our Fibonacci function, we should be able to call it with much higher numbers. And because we're remembering the result when that was produced, it's going to go much faster this time. All right? So we're going to say fib, we'll do 100 this time just to you that same number and how much faster it's going to go with this. And if we run our code by saying node examples slash memoization.js, sure enough, we see that that is able to calculate that very large number much faster because instead of having to recalculate, you know, fib 99 or fib 98 or 97 multiple times
>
> **[6:20](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/memoization?u=76281980&t=380)** as it goes down the chain, it only has to calculate each of those once, right? Because after it calculates it once, it simply returns the result that it got the previous time so that future calls can simply look that up. So anyway, that is the basics of memorization in functional programming and how it can help us with certain functions like this recursive Fibonacci function.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Functional Programming]] (2)
> **Definitions:** in other words (3), is called (2), is a  (1)
> **File Paths:** memoization.js (3), examples/recursion.js (1)
> **CLI Commands:** find (1), make (1), node (1)
> **Analogies:** picture (1), just like (1)
> **Speakers:** - [speaker] (1)


### 5. Code Conversion Challenges

[↑ Back to Table of Contents](#table-of-contents)

#### [Solution: Count occurrences](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/solution-count-occurrences?u=76281980)

#### [Solution: Anagrams](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/solution-anagrams?u=76281980)


### Conclusion

[↑ Back to Table of Contents](#table-of-contents)

#### [Next steps in your ES6+ journey](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/next-steps-in-your-es6-plus-journey?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-functional-programming-with-javascript-es6-plus-24300838/next-steps-in-your-es6-plus-journey?u=76281980&t=0)** - Well, congratulations. You've reached the end of the course. We have covered a lot of ground in this course, and the first thing I want to do is thank you for joining me on this journey. By now, you should be familiar with the basics of [[Functional Programming]] and [[JavaScript]], as well as some of the more advanced concepts. Now, if you have questions or want to share your progress, feel free to connect with me on [[LinkedIn]] or follow me on X, formerly Twitter, where I regularly post [[Software Development]] related tips and tutorials. And finally, if you're looking for other courses, I would highly recommend checking out some of my other courses in the LinkedIn Learning Library. So keep coding, keep learning, and I can't wait to see what you build with JavaScript. Best of luck in your projects, and I hope to see you in another course.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[JavaScript]] (2), [[LinkedIn]] (2), [[Functional Programming]] (1), [[Software Development]] (1)
> **Speakers:** - well (1)


## Instructor

- [[Shaun Wassell]]

## Resources

- Exercise files available
- [GitHub Repository](https://github.com/LinkedInLearning/learning-functional-programming-with-javascript-es6---coderpad--4252164/codespaces)

## Skills Covered

- Programming
- Web Development
- Front-End Development
- JavaScript
- ECMAScript

## Path Context

### In [[Explore Web Development with Vanilla JavaScript]]
← [[Learning App Building With Vanilla Javascript]] | **2 of 8** | [[JavaScript- Modern Browser APIs]] →

### In [[Improve Your JavaScript Language Skills]]
**1 of 8** | [[JavaScript- Async]] →

## Appears In

- [[Explore Web Development with Vanilla JavaScript]]
- [[Improve Your JavaScript Language Skills]]

## Related Courses

_Courses sharing skills:_

- [[End-to-End JavaScript Testing with Cypress.io]] — Web Development, Front-End Development, JavaScript
- [[Building Modern Projects with React]] — Web Development, Front-End Development, JavaScript
- [[Learning ECMAScript 6+ (ES6+)]] — JavaScript, ECMAScript
- [[CSS- Advanced Layouts with Grid]] — Web Development, Front-End Development
- [[jQuery Essential Training]] — Web Development, Front-End Development

---

[↑ Back to top](#)