---
type: course
cssclasses:
  - lle-course
  - lle-level-intermediate
slug: netops-devops-for-network-engineers-automating-networks
url: "https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks"
duration_minutes: 167
duration: 2h 47m
level: Intermediate
updated: 10/11/2024
learners: 183113
skills:
  - DevOps
  - NetOps
exercise_files: true
thumbnail: "https://media.licdn.com/dms/image/v2/C560DAQGfP7G_UdypoQ/learning-public-crop_675_1200/learning-public-crop_675_1200/0/1650305730733?e=2147483647&amp;v=beta&amp;t=8rJjSFjV5pkTMzkNyoc4Byv6dLAxSsrVJZR8zfzwe4k"
linkedin_topic: Database Management
learning_paths:
  - '[Network Automation Professional Certificate by Arista Networks](../../Paths/Database%20Management/Professional%20Certificates/Network%20Automation%20Professional%20Certificate%20by%20Arista%20Networks.md)'
prev_courses:
  - '[Introduction to Linux (2021)](../Network%20and%20System%20Administration/Introduction%20to%20Linux%20(2021).md)'
next_courses:
  - '[Hybrid Multicloud Networking- Practical Concepts](Hybrid%20Multicloud%20Networking-%20Practical%20Concepts.md)'
path_nav: '[{"path":"Network Automation Professional Certificate by Arista Networks","position":4,"total":6,"prev":"Introduction to Linux (2021)","next":"Hybrid Multicloud Networking- Practical Concepts"}]'
path_count: 1
tags:
  - course
  - topic/database-management
  - topic/devops
  - topic/network-and-system-administration
  - skill/devops
  - skill/netops
status: not-started
created: 2026-05-03
---

> <!-- lle:github-source --> [View this note on GitHub](https://github.com/thisis-romar/linkedin-learning-vault/blob/github-browse/Courses/Database%20Management/NetOps%20(DevOps%20for%20Network%20Engineers)-%20Automating%20Networks.md)

![NetOps (DevOps for Network Engineers): Automating Networks](https://media.licdn.com/dms/image/v2/C560DAQGfP7G_UdypoQ/learning-public-crop_675_1200/learning-public-crop_675_1200/0/1650305730733?e=2147483647&amp;v=beta&amp;t=8rJjSFjV5pkTMzkNyoc4Byv6dLAxSsrVJZR8zfzwe4k)

# NetOps (DevOps for Network Engineers): Automating Networks

> DevOps—a set of principles and practices combining software development and IT operations—is becoming the standard in corporate IT around the world. It enables a shorter delivery cycle, faster deployment, easier maintenance, and improving team culture. In this course, Eric Chou shows how to apply DevOps practices in a network engineering context, using easy to understand, hands-on examples. He cov

> [LinkedIn Learning](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks) | 2h 47m | Intermediate | 183K learners
> [Jump to Path Context ↓](#path-context)

## Table of Contents

### Chapter Overview

- [**Introduction**](#introduction) (2 videos)
  - [DevOps for network engineers](#devops-for-network-engineers)
  - [What you should know](#what-you-should-know)
- [**1. DevOps for Network Engineers Overview**](#1-devops-for-network-engineers-overview) (7 videos)
  - [DevOps principles](#devops-principles)
  - [DevOps practices](#devops-practices)
  - [Unique network engineering challenges in DevOps](#unique-network-engineering-challenges-in-devops)
  - [Prerequisites for this course](#prerequisites-for-this-course)
  - [Setting up a network DevOps lab](#setting-up-a-network-devops-lab)
  - [Network automation examples with Python libraries](#network-automation-examples-with-python-libraries)
  - [Network configuration backup example with Ansible](#network-configuration-backup-example-with-ansible)
- [**2. GitLab and CI/CD**](#2-gitlab-and-cicd) (9 videos)
  - [Introduction to Git](#introduction-to-git)
  - [Basic Git operations](#basic-git-operations)
  - [GitLab fundamentals](#gitlab-fundamentals)
  - [Network application example](#network-application-example)
  - [GitLab DevOps CI/CD pipelines](#gitlab-devops-cicd-pipelines)
  - [GitLab pipeline example: Build](#gitlab-pipeline-example-build)
  - [GitLab pipeline example: Review and test](#gitlab-pipeline-example-review-and-test)
  - [GitLab pipeline example: Deploy](#gitlab-pipeline-example-deploy)
  - [GitLab pipeline example: Monitor](#gitlab-pipeline-example-monitor)
- [**3. Network DevOps Examples**](#3-network-devops-examples) (4 videos)
  - [Network configuration backup with Ansible](#network-configuration-backup-with-ansible)
  - [GitLab integration for configuration backup](#gitlab-integration-for-configuration-backup)
  - [Network API application example](#network-api-application-example)
  - [Network API integration with GitLab](#network-api-integration-with-gitlab)
- [**4. Testing in DevOps**](#4-testing-in-devops) (4 videos)
  - [Testing fundamentals](#testing-fundamentals)
  - [Testing with pytest](#testing-with-pytest)
  - [Network testing with pyATS](#network-testing-with-pyats)
  - [Network testing in GitLab](#network-testing-in-gitlab)
- [**Conclusion**](#conclusion) (2 videos)
  - [Next steps](#next-steps)
  - [Conclusion](#conclusion)

### Introduction

[↑ Back to Table of Contents](#table-of-contents)

#### [DevOps for network engineers](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/devops-for-network-engineers?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/devops-for-network-engineers?u=76281980&t=1)** - [DevOps](../../Topics/DevOps.md) is becoming the standard in corporate IT around the world today. It is a set of principles and practices combining [Software Development](../../Topics/Software%20Development.md) and IT operations to enable shorter delivery cycle, faster deployment, easier maintenance, and improving team culture. This course is designed to provide network engineers with the general understanding and practices of DevOps in the network engineering context. My goal is to bring the world of DevOps to networking with easy-to-understand, hands on examples. By the end of this course, we could all be comfortable in bringing DevOps into our daily operations. Hi, my name is Eric Chou. I'm a network engineer, software developer, author, and instructor. I have written top selling books and taught highly rated classes on the topics of network automation and [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md). I invite you to join me on my [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) Learning course, DevOps for Network Engineers.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [DevOps](../../Topics/DevOps.md) (5), [Software Development](../../Topics/Software%20Development.md) (1), [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (1), [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) (1)
> **CLI Commands:** python (1)
> **Definitions:** is a  (1)
> **Speakers:** - devops (1)

#### [What you should know](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/what-you-should-know?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/what-you-should-know?u=76281980&t=0)** - [Instructor] This course is designed to introduce you how [DevOps](../../Topics/DevOps.md) concepts could be applied in network engineering. While we will cover the fundamentals of DevOps, this course drills down on the application of these concepts in the network engineering environment. To get the most out of this course, you might want to watch DevOps Foundations available here in the library, then come back to this course. You should also be familiar with the basics of network management and engineering. Don't worry, you do not need to be an expert in [Routing Protocols](../../Skills/Network%20and%20System%20Administration/Routing%20Protocols.md) or spanning tree, but you should be comfortable in basic configuration steps for network devices using Cisco iOS or other network operating systems. We will operate in a [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md) environment using the Bash Shell. We'll also use some [Ansible](../../Skills/Network%20and%20System%20Administration/Ansible.md) playbooks as well as [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) scripts. Don't worry if you're not familiar with these technologies, the operation example should be easy to follow for their desired output and intend a goal. We're only using them to illustrate the DevOps concepts, such as source control, [continuous integration](../../Skills/Software%20Development/Continuous%20Integration%20(CI).md), pipelines and testing. While the source files are available to you, this course is not meant to be a lab course where you follow along and perform the exact steps you see on screen to learn a certain technology. Instead, we will be focused on the concepts and illustrate them using a variety of technologies. For example, we might be using Ansible playbooks to illustrate making changes on network devices, but we will not go into details on Ansible itself.
>
> **[1:36](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/what-you-should-know?u=76281980&t=96)** The goal is to introduce to you the application of DevOps in network engineering using some of the tools you might already have so you could apply them in your own environment. I want to arm you with the principles, application examples and terminology so you could transfer your network management with a modern DevOps approach. Let's get started.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [DevOps](../../Topics/DevOps.md) (6), [Ansible](../../Skills/Network%20and%20System%20Administration/Ansible.md) (3), [Routing Protocols](../../Skills/Network%20and%20System%20Administration/Routing%20Protocols.md) (1), [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md) (1), [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (1)
> **CLI Commands:** ansible (3), python (1)
> **Analogies:** such as (1), for example (1)
> **Code Identifiers:** ios (1)
> **Tools:** bash (1)
> **Speakers:** - [instructor] (1)


### 1. DevOps for Network Engineers Overview

[↑ Back to Table of Contents](#table-of-contents)

#### [DevOps principles](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/devops-principles?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/devops-principles?u=76281980&t=0)** - [Instructor] In this session, let us explore the principles behind [DevOps](../../Topics/DevOps.md). First of all, what is DevOps? As one could guess from the terms, it is a combination of [Software Development](../../Topics/Software%20Development.md), dev, and IT operations, ops. Traditionally, we would have separate semi-isolated teams, with one team handling the development of software features. Once that is done, hand it over to the operations team for deployment and operation. Over time, the two teams grow apart, because if they have different priorities, use different tools, and therefore, created some frictions between them. For the majority of my career, I belonged squarely in the operations camp. As an network engineer, I could recount many war stories of trying to provision VLANs at the last minute, scrambling to configure firewall rules, and making various last minute changes to the network in the operations and deployment phase, all the while, trying to make sure all the changes conform to the security standards. On the other hand, DevOps is meant to bring the two teams together by leveraging the same technologies, share the same vocabularies, build better collaboration, and deliver faster results. In this course, we will speak from the point of view of network operators, learning about the DevOps principles, tools, terminologies, and operations. Another way to think about DevOps is with the three interrelated categories of people, process, and tools. Everything we will talk about in this course falls into one of these three categories. For example, in DevOps, we have a group of engineers who share the same principles,
>
> **[1:34](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/devops-principles?u=76281980&t=94)** collaboratively define a set of processes conforming to these principles, and use a set of tools that enforce this process. In turn, the tools will provide feedback to the engineers to further validate or tweak the principles and practices. Keep these three interlocking components in mind as we move through the course. As we have mentioned, the primary goal of DevOps is to shorten the software cycle from development to deployment. In network engineering, we can also draw many parallels to the same design, develop, deploy, and feedback loop, as software development. For example, we could put together a new data center design, develop an action plan to validate the design, then move forward with the deployment and monitoring. A DevOps principle, such as [Version Control](../../Skills/Web%20Development/Version%20Control.md), could be applied to all these three steps. We could also benefit from another DevOps principles of making small incremental changes that could quickly respond to the changing environment. The DevOps principles are highly related to the [Agile](../../Skills/DevOps/Agile%20Development.md) and Lean software development, so let's take a look at the Lean principles and Agile development process before we move on to the DevOps principles. What are some of the Lean principles? The number one principle is to eliminate waste. What are waste? Waste are defined in everything not adding value to the customer. Here are some of the waste defined in Lean, partially done work, extra features, relearning, task switching, waiting, handoffs, defects, and my personal favorite, management activities.
>
> **[3:10](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/devops-principles?u=76281980&t=190)** Lean thinks that the software development process is a continuous learning process based on iterations. We're trying to solve problems for writing code, test with customer, learn the feedback, and rewrite the code if needed. So the second principle is to speed up and amplify the learning process. The third principle is to decide as late as possible. Think about how much uncertainties there are in today's fast changing environment, such as new customer requirements might pop up, new competitors might enter the market, not to mention technology changes really, really fast, so by delaying big decisions as late as possible, we have some flexibilities built in. The fourth principle is to deliver as fast as possible. We all know technology changes rapidly. The sooner we could get our [products](../../Skills/Software%20Development/Microsoft%20Products.md) into the customer's hands, the faster we could receive the crucial feedback that we need to make the product better. Other principles of Lean include empower the team for decision making, build integrity in the process, and optimize as whole. Let's dive into [Agile Software Development](../../Skills/DevOps/Agile%20Software%20Development.md) principles. In customer focus, the highest priority is always customer satisfaction, and we should also embrace changes and adapt accordingly. Always welcome the changing requirements. We should aim to frequently deliver working software. In a cross-functional team sense, collaboration means close daily co-operations between operators and developers. We should aim for motivated teams,
>
> **[4:44](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/devops-principles?u=76281980&t=284)** because projects that build around motivated people who should be trusted. Also, face-to-face conversation is the best form of communication. In Agile, continuous improvement means the team regularly reflects on how to be more effective. Again, DevOps originated in software development, however, all of the principles we have seen in Lean and Agile are still applicable in network engineering. Besides taking part as the operations team in an organization, let's look at how we could draw a parallel and adapt the DevOps principles within network engineering itself. If we think about the network life cycle, initially, we'll receive some requirements from network. It could be a corporate campus, a data center, or retail store, that we would design the network accordingly. We would then develop the typology, necessary equipment, software licensing, port counts, and so on. Then we would deploy the equipment per our design. This could be the actual rack and stack, configuring the equipment, and all the necessary supporting infrastructure, such as IP management, DNS, [DHCP](../../Glossary/Standard/DHCP.md), firewall policies, and so on. Once it is deployed, we will continue to operate and monitor the network. Maybe we are adding new users, VLANs, servers, security policies, and we will continue to monitor the health of the network to make sure they are operating in an optimal state. Inevitably, even the best designed and operated network will need to be updated with new features,
>
> **[6:20](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/devops-principles?u=76281980&t=380)** new business requirements, and we will repeat the cycles again. Since the network engineering cycle is very similar to software development, in my opinion, the existing DevOps best practices can and should be adopted into network engineering as well. In summary, the DevOps principles are having a collaboratively environment with joint responsibilities between development and operations teams. Have a continuous development cycle. Always try to speed our time to market with small teams. And this is a big one, try to automate as many steps as possible. We should embrace failure and learn from them, and always, always be customer focused.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [DevOps](../../Topics/DevOps.md) (15), [Software Development](../../Topics/Software%20Development.md) (6), [Agile](../../Skills/DevOps/Agile%20Development.md) (4), [Version Control](../../Skills/Web%20Development/Version%20Control.md) (1), [Products](../../Skills/Software%20Development/Microsoft%20Products.md) (1)
> **Analogies:** such as (3), for example (2), similar to (1)
> **CLI Commands:** make (3)
> **Definitions:** is a  (3)
> **Env Vars:** dns (1), dhcp (1)
> **Prerequisites:** configure (1)
> **Speakers:** - [instructor] (1)

#### [DevOps practices](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/devops-practices?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/devops-practices?u=76281980&t=0)** - [Instructor] Having learned the [DevOps](../../Topics/DevOps.md) principles, what are some of the DevOps practices? The first DevOps practice is to have a source of truth for the software code, documentation, device configuration or network state. This usually means a distributed [Version Control](../../Skills/Web%20Development/Version%20Control.md) system that all team members could refer to. The second DevOps practice is to have automated testing. Because we're trying to put our emphasis on small incremental changes, we should aim to have automated testing that ensures we do not accidentally break things when these changes are made. We should aim to have a system of [continuous integration](../../Skills/Software%20Development/Continuous%20Integration%20(CI).md). This means having a system for each developer and operations personnel put their changes into the source control, or the source of truth, as quickly as possible. This change could range from software code, device state, network configuration, and so on. It should also be pointed out the continuous integration means to have the changes make their way to the source of truth or version control system and does not equate to deployment. Once we have continuous integration, we could take the next step for [continuous delivery](../../Skills/Software%20Development/Continuous%20Delivery%20(CD).md) or continuous deployment. What is the difference between the two? Sometimes we do not have control over deployment and just delivery, such as when we have changes in a updated doc or image or when we have a mobile app that is only deployed when the client pulls the changes in. Another DevOps practice is to implement a high trust culture
>
> **[1:39](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/devops-practices?u=76281980&t=99)** for individuals and between teams. Examples of this practice could be peer review for changes as well as a blameless culture between developers and operators. As mentioned, continuous integration and deployment is a practice where developers integrate the change into a version control repository. Each integration could be verified by an automated testing process that could catch errors. Optionally, the change could be deployed either automatically or with a manual push. The system is crucial to the DevOps practice. In this course, we will use a open source project called GitLab with built-in version control, issue tracking, [Code Review](../../Skills/Software%20Development/Code%20Review.md), continuous integration, continuous delivery, and much more. One of the newer job roles as a result of the DevOps movement is the rise of site reliability engineering, or SRE. In the words of [Google](../../Glossary/Service/Google.md), SRE is what you get when you treat operations as if it is a software problem. The goal of SRE is to protect, provide for, and monitor the software and system behind services. One of the main concepts of SRE is the idea of the error budgets. It is the uptime of the service as perceived by the user measured in time. As Google is the company that started the SRE movement, feel free to follow the link to find out more about the SRE practices via the link provided.
>
> **[3:12](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/devops-practices?u=76281980&t=192)** As network engineers continue to adapt DevOps practice, sometimes you will hear the people refer to the job function and role as network DevOps. Please feel free to check out the SRE description from Google or Network Computing's article about the coming together of DevOps into network engineering. There are lots of practical examples of applying DevOps in the real world.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [DevOps](../../Topics/DevOps.md) (11), [Continuous integration](../../Skills/Software%20Development/Continuous%20Integration%20(CI).md) (5), [Version Control](../../Skills/Web%20Development/Version%20Control.md) (4), [Google](../../Glossary/Service/Google.md) (3), [Continuous delivery](../../Skills/Software%20Development/Continuous%20Delivery%20(CD).md) (2)
> **Env Vars:** sre (7)
> **CLI Commands:** make (1), find (1)
> **Definitions:** is a  (2)
> **Tools:** gitlab (1)
> **Analogies:** such as (1)
> **Speakers:** - [instructor] (1)

#### [Unique network engineering challenges in DevOps](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/unique-network-engineering-challenges-in-devops?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/unique-network-engineering-challenges-in-devops?u=76281980&t=0)** - [Instructor] In this section, let's talk about some of the challenges we face in network engineering when we try to adapt [DevOps](../../Topics/DevOps.md) mindsets and practices. The goal is not to create shame or blame. Instead, I want to put DevOps into the right context for us. More importantly, what tools and process we can use to solve these challenges. Traditionally, when we're planning and designing a network, we'll use tools such as Visio to diagram our network, perhaps use spreadsheet to keep track of IP address assignments, then use some kind of text file to keep track our standard configuration files. These steps have worked for many years. However, there's several problems with them. The first problem is they require manual updates when changes occur. The network diagram, for example, is almost out of date as soon as the first change occurs. The spreadsheet and configuration templates also requires some engineers time and effort to maintain. The second problem is once the change is made, there's not a lot of ways to keep track of the reasons behind the change or who made them. In other words, we don't have [Accountability](../../Skills/Data%20Science/Accountability.md). For example, why do we assign VLAN numbers the way we did? Nobody knows after a while. It just has always been the way we did things. Also a third problem is that these files are difficult to maintain across different teams. Not everybody has as Visio, and not everybody uses the same way to keep track of different versions. Many times we rely on emails and arbitrary naming conventions
>
> **[1:32](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/unique-network-engineering-challenges-in-devops?u=76281980&t=92)** to know the latest version of these files. The solution to these problems is to adapt a [Version Control](../../Skills/Web%20Development/Version%20Control.md) system, such as [Git](../../Skills/Software%20Development/Git.md). We could use the system to keep track of the modification histories, know who made the changes and when, all the while keeping them in a storage system that is distributed and redundant. If you think the version control system is too good to be true, it is not. It has been used and perfected over the years by software developers. We're simply borrowing them for the operations team. Another practice we have used for many years in network engineering is we use the production network as our source of truth. What do we mean by a source of truth? According to Wikipedia, the single source of truth is the practice of structuring information models and associated files such as they're mastered and edited in only one place. In network engineering, oftentimes, once the network is deployed, we will continue to make the necessary changes on the same production network. For example, when we need to add more access control list entries or new VLANs, we just go directly to the network device and make that change. This makes network changes risky. Sometimes, a normal routine change could result in a big outage. The management might lose confidence in network changes and further delay more changes or require a change control system that could potentially be prohibitive. The solution to this problem is to keep a separate system outside of production for network configuration, related information and network state.
>
> **[3:05](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/unique-network-engineering-challenges-in-devops?u=76281980&t=185)** We could use database, version control system or dedicated servers for this purpose. A nice open source project that have grown in popularity is a NetBox project as this single source of truth. Another problem that we're all painfully aware is the fact that in traditional networking, the network equipment requires manual command line interface or [CLI](../../Skills/Network%20and%20System%20Administration/CLI.md) to make configuration changes. CLIs are okay for initial configuration and learning, but not for repeated and redundant tasks. This human-driven approach will result in more mistakes and create greater skill gaps. Not to mention, they're sometimes very tedious to do. Luckily, thanks to the community effort, we have a number of network automation frameworks and languages to choose from to eliminate this manual intervention. We could use [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) programing language and libraries, such as Nornir, Scrapli and NAPALM, to automate the manual configuration process. We could also use automation frameworks, such as [Ansible](../../Skills/Network%20and%20System%20Administration/Ansible.md), to automate the changes without manual configuration. Testing is a crucial component in DevOps. But traditionally, we don't have a lot of testing. One of the main reasons has to do with network testing environments. The network equipments are specialized [Hardware](../../Topics/Hardware.md) devices, optimized to transport packets and frames over the wire as soon as possible. They're typically expensive. And not a lot of organizations
>
> **[4:38](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/unique-network-engineering-challenges-in-devops?u=76281980&t=278)** could afford a network testing environment. I remember in previous jobs, as the makeshift testbed, we used to open brand new equipments, cable them up, station network to make sure everything works just fine, repackage them and ship them to the other sites for deployment. This obviously make automated testing very difficult and is really not good for the DevOps testing principles. Luckily, in recent years, we have seen more and more network vendors adopt virtualization of their hardware. As we will do in this course, we could use a virtualized version of the network to conduct our testing. Besides lack of testing equipment, traditionally, there were no widely available testing framework. We have some commercial software that could perform SNMP, pings and trace routes, but that's about it. In recent years, we have seen several promising testing framework that came to light. Pytest is a Python testing framework that is widely used to test Python code. PyATS is a testing framework that was originally developed at Cisco and was later released to be used by the community at no cost. Batfish is the open source network configuration analysis tool that could be used in pre-change validation. Suzieq is the open source multi-vendor network observability platform that could be used for network post-change validation. The last issue I want to address is this mindset of you break it, you fix it. Because we only have the production network
>
> **[6:11](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/unique-network-engineering-challenges-in-devops?u=76281980&t=371)** as a source of truth as we have explained before, many times problems that tend to go dead could be concealed and obscured. For example, when we added a new server and updated echo in a new deployment, the older sites were often not updated. Or if we update the standard version of software on the newer network, in the newer network OS, many of the older devices were not updated due to oversight or risk. The problem is if there're attempts to fix these legacy issues that results in outage, we have this you break it, you fix it mindset. This discourages engineer's effort to fix the problem that are not obvious. And they kind of just push them into the future until they could no longer be ignored. The solution here, in my opinion, is the DevOps practice of making small incremental changes as well as adapt to blameless culture that will encourage [Cross-team Collaboration](../../Skills/Data%20Science/Cross-team%20Collaboration.md). I hope by now, you're convinced as I am that as network engineers, we have the tools and processes today to adopt DevOps culture and best practices. We could use these tools to help us become more productive and build a better culture.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [DevOps](../../Topics/DevOps.md) (6), [Version Control](../../Skills/Web%20Development/Version%20Control.md) (3), [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (3), [Hardware](../../Topics/Hardware.md) (2), [Accountability](../../Skills/Data%20Science/Accountability.md) (1)
> **CLI Commands:** make (5), python (3), git (1), ansible (1)
> **Analogies:** such as (5), for example (4)
> **Definitions:** is a  (4), in other words (1)
> **Env Vars:** vlan (1), cli (1), napalm (1), snmp (1)
> **Tools:** command line (1)
> **Speakers:** - [instructor] (1)

#### [Prerequisites for this course](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/prerequisites-for-this-course?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/prerequisites-for-this-course?u=76281980&t=0)** - In this section, let's discuss some of the prerequisites and technologies used in this course. As we know by now, [DevOps](../../Topics/DevOps.md) is a set of philosophies, practices, and tools. Many of the technologies using our DevOps pipeline, such as [Git](../../Skills/Software%20Development/Git.md) and pytest once a separate course by themselves, it is simply not possible to do a deep dive on all of them. As mentioned in the introduction, this course is not a lab long course where you will follow the course step by step to learn the new technology. Instead we will focus on the DevOps philosophies, practices, and tools. We'll be used practical examples to illustrate how they could be applied in network engineering. But don't worry if you do wish to follow the examples in own lab, I have provided the complimentary files with this course for you to download. If you would like a more in depth learning experience on the general DevOps foundations a great course by Ernest Mueller and James Wickett is available right here in the library. Now let's take a look at some of the technology we would use in this course. We will use [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md) as the underlying operating system in this course. Being comfortable in the Linux environment is the only prerequisite for this course. You do not need to be an expert in Linux as long as you feel comfortable in the environment for moving around the directory, copying and editing files, know what the sudo command does for example, you should be good to go. If you would like a refresher of Linux, a good course by my friend, David Bumble will be a great choice. We will use Cisco devices as network geared targets
>
> **[1:35](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/prerequisites-for-this-course?u=76281980&t=95)** in this course. Cisco has been the 800 pound giant in our industry since the beginning of the internet age. They offer a broad range of technologies in data center networking, security, enterprise networking. You name it. Again, no need to be an expert. As an example of the depth I will go into, if you take a look at the command line example of configure term or configure terminal interface ethernet1/2, router bgp, and knows what they do, that is good. Even if you do not know precisely what they do, you can focus on their intentions and what they try to accomplish. If you'd like to go deeper into Cisco technologies and configuration, a good collection of courses would be the Cisco DevNet learning path put together by yours truly. We will use [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) as our programming language for scripting examples Python has a welcoming and supportive community. It is a language of choice by many network automation enthusiasts, including myself. There are lots of free resources to learn Python. One of them is the beginner tutorial provided by Python software foundation. The tutorial will go over the basics of Python, as well as providing resources for all topics related to Python. We will use a few automation examples using [Ansible](../../Skills/Network%20and%20System%20Administration/Ansible.md). Ansible is one of the most popular infrastructure automation framework. It is also the most network automation friendly framework in my opinion. Having spoken and attended several Ansible conferences and meetups,
>
> **[3:07](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/prerequisites-for-this-course?u=76281980&t=187)** I could speak from personal experience of seeing how much the framework invests working with the network engineer community. If you would like a refresher on Ansible, learning Ansible would be a great resource. We will use Docker containers in this course. Containers are standard units of software that packages up code and all of its dependencies into a single image, which could then be run in an isolated environment. They're similar to [Virtual Machines](../../Skills/Cloud%20Computing/Virtual%20Machines.md) if you have used them before, but more lightweight and quicker to launch. That might sound a bit confusing. As we move through the course, we'll see examples of package our codes into containers that hopefully will clear things up a little bit. Because of the lightweight predictable nature of containers, they're widely used in the DevOps world. There's several different DevOps platforms. GitLab is one of them. GitLab is unique with its are being a single location with both a source code repository as well as DevOps automation pipeline for [continuous integration](../../Skills/Software%20Development/Continuous%20Integration%20(CI).md) delivery. This means we do not need to learn one tool for source code repository, then another code to run our automated continuous integration and delivery pipeline. This reduces the complexity and learning curve. We will spend a lot more time in the GitLab environment for the [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) of this course. I know by now you might feel a little bit overwhelmed by the number of technologies we'll touch in this course. I could relate to that feeling. The amount of technologies involved in a legitimate DevOps workflow is one of the challenges for me
>
> **[4:41](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/prerequisites-for-this-course?u=76281980&t=281)** in putting together in this course. Keep in mind the technologies, let it be Ansible, Python, Docker are all like ingredients we used to cook up a delicious dish. They're interchangeable units that we could swap out for your environment if needed be. If you like think of using [JavaScript](../../Skills/Software%20Development/JavaScript.md) instead of Python if you're more familiar with JavaScript, if your company uses Puppet, instead of Instable, that is okay too. It is more important to follow the flow and integration throughout this course. Focus on the DevOps principles and practices rather than any single technology. Are you ready? Let's see how the lab is put together for this course.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [DevOps](../../Topics/DevOps.md) (9), [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (8), [Ansible](../../Skills/Network%20and%20System%20Administration/Ansible.md) (6), [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md) (4), [Continuous integration](../../Skills/Software%20Development/Continuous%20Integration%20(CI).md) (2)
> **CLI Commands:** python (8), ansible (6), docker (2), git (1), sudo (1)
> **Tools:** gitlab (3), command line (1), terminal (1)
> **Analogies:** such as (1), for example (1), similar to (1)
> **Prerequisites:** configure (2), prerequisite (1)
> **Exercise Files:** source code (2)
> **Definitions:** is a  (2)
> **Warnings:** keep in mind (1)

#### [Setting up a network DevOps lab](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/setting-up-a-network-devops-lab?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/setting-up-a-network-devops-lab?u=76281980&t=0)** - [Presenter] Let's talk about one of my favorite topics in network engineering, which is setting up a network lab. In particular, we'll talk about how the lab is put together for this course. In our setup, we'll have a total two [Virtual Machines](../../Skills/Cloud%20Computing/Virtual%20Machines.md). The first virtual machine or host will contain our virtualized network gears. And the second host will contain our GitLab Servers as well as our GitLab Runners. We'll go over the GitLab Server and GitLab Runners set up in more detail in just a bit, but just then as the overview the GitLab servers will have our code repository, our automation instructions, and those instructions get passed down to the GitLab Runners and the GitLab Runners are the actual processes that will execute those instructions, in this case to our network gears. For the network lab we'll utilize the latest Cisco modeling lab or CML. CML is able to create multiple labs at the same time. As you can see in our environment we have created three separate environments. One is for the development, one is for stage and the one is for production. If you recall, from the [DevOps](../../Topics/DevOps.md) principles what we want to do is we have separate isolated environments so we could safely test against them before we deployment into production. I personally prefer virtualized network labs over physical gears. It is true that virtual network gears do not have 100% feature parity compared to their physical cousin. However, they're more flexible and [agile](../../Skills/DevOps/Agile%20Development.md) when it comes to changing topologies, with testing different software versions,
>
> **[1:34](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/setting-up-a-network-devops-lab?u=76281980&t=94)** or have an API to automatically start those labs. There are several options when it comes to virtualized network labs. If you don't want to use Cisco CML as I do. There are GNS3, EVE-NG, Containerlabs, and we'll go over how, if you do want to put this lab together in your own environment, how you could replicate them in those other options. If you use Cisco CML, as I do I actually provided the topology in the YAML format which CMO could just directly import them into your environment. But if you use some other networking virtualized labs then we'll go over how we could replicate those. Those topology files are simply YAML files. And if you open them up with a text editor, you can actually see the configuration, the topology, and so on. I do want to point out that CML that I use is a paid product that costs 199 U.S. dollars per year. In my opinion, it is worth the price for what they offer especially if you're network engineers. But if you would like to try it out first there's also a free CML sandbox you could register to use on developer.[cisco.com](https://cisco.com). For the GitLabs server installation, will use the self hosted all-in-one installation. GitLab has detailed installation instructions for different flavors of [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md). I have provided a step-by-step guide with a section for the installation steps on the [Ubuntu](../../Skills/Web%20Development/Ubuntu.md) 2004 LTS in the class handout. But if you want to use other flavors of Linux feel free to go over to the instruction page
>
> **[3:10](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/setting-up-a-network-devops-lab?u=76281980&t=190)** and pick the Linux flavor of your choice. Let's take a look at the CML network lab as well as the GitLab installation steps. I do a understand if you're relatively new to virtualization or Linux this might be a heavy load to install CML and GitLab, install the Runners and get them all talking together. Don't worry if you can't put this lab together locally in your own environment you should still be able to follow along in this course but I do want to go over them. So if you do want to put them in own environment what are the steps that you could follow. In the class handout you should see a lab topology graph. This is essentially what we're trying to simulate. As our production network, we have two data centers. One is LAX, and one is NYC each with a core device as well as two edge devices. The core device is NXOS and the edge devices are iOS devices. In between them, they run BGP, which one of them is ASN 100. One of them is ASN 200. And within the data centers, we run OSPF with different area numbers. In the class handout, you should find a file called CML resources where you could see the product page, the quick start guide, as well as CML sandbox that you could take a look if you wanted to try it out first. This is the product page for CML, where you could make your purchase if you want to for the licensing as well as look at the different features. If you're interested. Here's the quick start guide. Once you purchase, you could follow this guide to set up your CML and have virtual labs at your fingertips. If you want to try it out first
>
> **[4:47](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/setting-up-a-network-devops-lab?u=76281980&t=287)** you could go on to developer.[cisco.com](https://cisco.com), where they provide sandboxes that you could reserve. So if you just scroll down and you could see the CML underneath, you will reserve, and at some point they will give you the free slots where you could reserve. And at the a point you could VPN into their lab and try out CML. As mentioned before, I've already installed CML in my local environment. And if you take a look at the class files you could see three topologies. One is for full one is for dev, and one is for staging. And this full topology is where you saw that topology before we were simulating the narrow devices where we have the OSPF and BGP running between them. And they're all connected to the unmanaged switch with the external connection. Those connections where the GitLab Runners will communicate back to these network devices. Also, I want to point out YAML files are simply text files that you could just right click and open with any text editor. If you use anything other than CML, you could simply copy and paste these running configs right here. You could just scroll down. Each file will contain a complete self-contained topology as well as the running configuration that you could just configure them, change the IP address accordingly, and replicate them in your own environment, if you so choose to. Also, I want to point out where the easiest way to get GitLab running. It's actually to follow the installation step for the all-in-one installation
>
> **[6:21](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/setting-up-a-network-devops-lab?u=76281980&t=381)** where they will provide information on requirements, installation steps for different Linux flavors. For me, I have provided installation steps, basically step-by-step that I've used myself multiple times to install GitLab on Ubuntu, including the minimum requirements I've used for the Ram, CPU, as well as hard drive. The installation instructions URL for you to follow along, as well as some of the outputs that you would expect you to encounter, at least from my experience. And I will also put some of my personal recommendations such as setting up DNS. If you want to use HTPs, you know mapping back in, how to generate initial passwords as well as configure a SMTP server which will allow you to have email notifications for you. Later on we'll see when the CICD pipeline run whether they success or fail, if you have set up SMTP you could easily receive notification that way. So, as you mentioned before the GitLab servers will have the instructions for repositories as well as automation pipeline but the actual running of these instructions for [Scalability](../../Skills/Artificial%20Intelligence%20(AI)/Scalability.md) is actually handled by the GitLab Runners and for my lab or the lab for this course we actually use the Docker Runners as our GitLab Runners. So therefore I provided the installation steps for Docker Engine if you want to follow along, and running the Docker just to make sure
>
> **[7:56](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/setting-up-a-network-devops-lab?u=76281980&t=476)** that it runs on the same host. Then we could configure the GitLab Runners for CICD and so on. We'll add SSH Key to test project if you like. As I mentioned before, if you can't replicate this in your own environment, that's okay. You should still be able to follow along in this course. These instructions is meant for those who want to replicate them in your own environment, but it's not necessarily a must to follow along in this course.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md) (5), [Ubuntu](../../Skills/Web%20Development/Ubuntu.md) (2), [Virtual Machines](../../Skills/Cloud%20Computing/Virtual%20Machines.md) (1), [DevOps](../../Topics/DevOps.md) (1), [Agile](../../Skills/DevOps/Agile%20Development.md) (1)
> **Env Vars:** cml (16), yaml (3), bgp (2), asn (2), ospf (2)
> **Tools:** gitlab (17)
> **Prerequisites:** set up (3), install (3), configure (3), setup (1)
> **CLI Commands:** docker (3), make (2), find (1), ssh (1)
> **URLs:** [cisco.com](https://cisco.com) (2)
> **UI Navigation:** scroll down (2)
> **Code Identifiers:** ios (1)

#### [Network automation examples with Python libraries](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/network-automation-examples-with-python-libraries?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/network-automation-examples-with-python-libraries?u=76281980&t=0)** - [Instructor] Two of the most popular methods for network automation today are with [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) or a automation frameworks such as [Ansible](../../Skills/Network%20and%20System%20Administration/Ansible.md). The advantage of using Python is the amount of customization we could do, we can use many of the available Python libraries, modules, self-made scripts, and just the community in general to enhance our network automation pipeline. On the other hand, Ansible does a lot of the heavy lifting for us by abstracting away some of the lower level operations, and just use mostly a YAML file to specify the steps that we could do for network configuration. And it is a little bit easier to get started. In this course, we will use both Python and Ansible and see how we can integrate them into our [DevOps](../../Topics/DevOps.md) pipeline using GitLab. This is a iterative process, we will start with something simple and gradually add more bits and pieces. I will try to illustrate this iterative process as well. In this first example, we will develop a Python script that could communicate with the devices with a simple show version command and retrieve the output back. Of course, the first step is to make sure that we have some devices that we could communicate to. In my example, I'm going to use the learning tests lab, these two, lax-edig-r1, lax-edig-r2, which are iOS devices, to make sure that we have the script that's ready to go. If this is the first time you've launched the device, you want to make sure SSH is enabled and the key is generated.
>
> **[1:33](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/network-automation-examples-with-python-libraries?u=76281980&t=93)** The way to do that is to use this command, your configuration mode crypto key generate rsa is going to ask for yes or no to replace them if you had them already, like I did. And then it's going to ask you for the bits. 512 is actually a bit small, so I would recommend using 2048. After you've done that, there's a session enabled and you should be able to SSH to the device. You could go ahead and repeat that process for lax-edig-r2 as try to SSH to that device. And if I do a show ip interface brief. As you can see, this guy has interface 192.168.2.71 that's connected to the unmanaged switch in turn connect to external network. That's how I'm communicated to it and that's why I'm going to use that IP address to try to SSH that box. If I do ssh cisco@192.168.2.71. And I get an error, it says unable negotiate the key exchange with that particular device. And if you get this same error, this is a very common error when we're trying to use a newer [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md) system with Cisco devices. And it turns out that the negotiation key exchange methods are just not there. So what we could do is we could modify the ssh/config file and we'll force it to use a exchange method that both exists.
>
> **[3:06](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/network-automation-examples-with-python-libraries?u=76281980&t=186)** As you can see, I've already done it for r2 just to save some time. And we're going to go ahead and change that back for r1. So if I repeat that, now I able to SSH a device, I do a show version just for good measure. Okay, great. So let's go ahead and construct our Python script in the next step. For Python development, I would highly recommend using a virtual environment which separates the system Python environment from any other projects. Personally, I am going to create a virtual environment for this course and activate it. If you're unfamiliar with virtual environment, there are a number of good resources online. You could take a look. I found this DigitalOcean resource pretty useful. And DigitalOcean is generally has some of the better tutorials out there for Python, at least that I've encountered. So I encourage you to take a look at these if you're unfamiliar with virtual environment. Like I said, I am going to create a virtual environment for this whole course. I'm going to hop back into my developer IDE, which is Visual Studio Code. And I am going to create a virtual environment for this whole course. If you follow that tutorial, or if this is the first time that you've installed a virtual environment, you need to use this command. For me I'm using [Ubuntu](../../Skills/Web%20Development/Ubuntu.md), so I'm using the apt-get install,
>
> **[4:40](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/network-automation-examples-with-python-libraries?u=76281980&t=280)** will for python-pip, as well as python3-venv. And after that, I could use the module. I'm going to create this virtual environment. The first couple of bits is to invoke the venv module. And the second parameter is actually the name of that virtual environment. So for me, I am just going to call it venv, and go ahead and activate that. Okay, great, you can see the venv that's being activated. And I've already created a Sec_1_6_Python_Script folder that contains all the files that we're going to talk about. Just to save some time, I've already typed them out, you could go ahead and either copy and paste into your own file, or you could type along with me as you see them on the screen. In the next step, we'll use a Python library called Nornir to communicate with the device. This is a very popular Python library. If you're unfamiliar with Nornir, it is a open source library that aggregates some of the popular Python libraries, other Python libraries, such as Netmiko, NAPALM, Scrapy, use a modular plugin format. So what we're going to do is we will just move directly into installing the nornir_utils, as well as nornir_netmiko underlying plugin. So because we're using a virtual environment,
>
> **[6:18](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/network-automation-examples-with-python-libraries?u=76281980&t=378)** all the library installed will be under this venv environment and wouldn't impact the system, Python library folder. The way Nornir behaves is that we could define a host file. Actually, let me just go ahead and show this file. We're going to define that host.yml file that has all the necessary parameters, we have iosv-1, as we've seen, this is 2.71 with all the username, password, iosv-2 as 2.72. So both of them are specify with a username and password. And let's take a look at the show_version.py file. This is very simple. This is less than 20 lines. And if you minus the comments, you will probably just be about 16 lines. So we'll import the necessary libraries, then we'll InitNornir. By default, it will look for that host.yml file in the same directory. And then go ahead and run any kind of send command. For us is going to be show version. And the results going to be store in this result variable. And in order to print that results in the readable format, we previously inputted this print_result from the plugin functions. So let's take a look and see what that output is. Okay, great. So now we've executed and this is the results that we would get if it's successful. So now this script is ready, but remember,
>
> **[7:53](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/network-automation-examples-with-python-libraries?u=76281980&t=473)** we're going to use a container base runner on GitLab in the future to build our pipeline. So it would be a good idea to make sure the script runs under a docker container. So let's go ahead and create a docker container. Copy all the files over and make sure that we could execute it under a Docker environment. If you're unfamiliar with Docker, this is a good site, docs.[docker.com](https://docker.com). Getting started, and we will basically just do this first part with the docker builds to build this particular image, and then use the docker run to run that. Just to make sure it could run, we're not going to push it over to any kind of registry. This image would just live locally. Because Docker is a self-contained environment, what we need to do is we need to specify all the library that needs to be there such as our Nornir plugins, such as our Nornir library in general. So what we're going to do is we're going to do a pip freeze. So if you're curious, pip freeze were basically just specify all the libraries currently installed in your Python environment. I know this is more than what we've installed. Because of the dependencies, they've actually installed more libraries than what we've actually typed in. So what we could do is do a pip freeze
>
> **[9:26](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/network-automation-examples-with-python-libraries?u=76281980&t=566)** and redirect all the outputs into requirements.txt. And now that we have this requirements.txt file, that specify all the libraries as well. If you take a look at the Docker file, you will see the each of the lines of how to construct this Docker file. Basically, the first step is will pull the base image and in this case is official python 3.8.11-buster image. And by default, if you don't specify a registry, this will be pull from [GitHub](../../Skills/Software%20Development/GitHub.md) registry. And we have the following lines, these two are Python specific. Generally, you know, Python will produce a PYC compiled file. In this case, we don't really need that. And we really don't need a standard in/out. Because this is all being executed in a remote container, so we don't really need that. The third step, we'll create a working directory. And this is where we will copy that local requirements.txt file into that remote directory, We'll install the requirements, we'll upgrade the pip version according to the requirements.txt file, we'll copy the content basically is the host, as well as the host.yml file, as well as the show_version.py into that working directory. And then we're going to go ahead and run the script called python show_version.py. So once we have this Docker file, the way to build this image would just be do a Docker built.
>
> **[11:02](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/network-automation-examples-with-python-libraries?u=76281980&t=662)** We could specify that we're do a Docker build like this. docker build, and we just specify the directory as dots, because you will by default look for a Docker file. But what I've always wanted to do is to be more explicit. So I will use the docker build command, but we'll specify a file directly. And I will go ahead and do a remote target and say this is the image name I want to use with the dots specifying the current directory. So it's going to go ahead and do all the necessary steps in the background such as pulling the base image, as well as copying all the files over. Because I've already done this a few times, that's why I already have the base image which is cached in my dev system. If this is the first time you're doing it, which I imagine that's the case, it might take a little bit longer for you. But as you can see, now that is there, and if I do a docker image specifying the name that I specified before, which is python_example_1, I could see the image is there with the size of just a little over 930 megabytes. And now let's go ahead and run it. docker run python_example_1. And voila, I am able to run this show version script in a Docker container environment. Now this is all ready to be checked
>
> **[12:37](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/network-automation-examples-with-python-libraries?u=76281980&t=757)** into a GitLab, [Git](../../Skills/Software%20Development/Git.md) repository, and then being executed by our GitLab Runner.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (20), [Ansible](../../Skills/Network%20and%20System%20Administration/Ansible.md) (3), [DevOps](../../Topics/DevOps.md) (1), [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md) (1), [Ubuntu](../../Skills/Web%20Development/Ubuntu.md) (1)
> **CLI Commands:** python (20), docker (19), ssh (7), make (6), pip (5)
> **File Paths:** requirements.txt (4), host.yml (3), show_version.py (3)
> **Env Vars:** ssh (5), yaml (1), ide (1), napalm (1), pyc (1)
> **Code Identifiers:** show_version (3), ios (1), nornir_utils (1), nornir_netmiko (1), print_result (1)
> **Definitions:** is a  (7)
> **Versions:** 192.168.2 (2), 2.71 (1), 2.72 (1), python 3 (1), 8.11 (1)
> **Tools:** gitlab (4), visual studio (1), github (1)

#### [Network configuration backup example with Ansible](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/network-configuration-backup-example-with-ansible?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/network-configuration-backup-example-with-ansible?u=76281980&t=0)** - [Instructor] Besides [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md), another popular way to perform network automation is the use of the [Ansible](../../Skills/Network%20and%20System%20Administration/Ansible.md) framework. There are several popular automation frameworks out there that's available such as Puppet, Chef and Salt. In fact, you may have noticed during the omnibus installation of GitLab, the installation process uses ostensibly of the Chef automation framework with the so-called recipes and cookbook. That is partially because Chef and GitLab both uses the Ruby programming language as it's back end, so it makes sense for GitLab to use Chef as its automation framework. Ansible, on the other hand, uses Python as the programming as its building block. So if you already know Python, you can leverage that knowledge to extend Ansible. However, we don't need to know Python in order to get started with Ansible. Most of the Ansible syntax is in the easy to read YAML syntax. If you're not familiar with Ansible, I would highly recommend going over the two documentation, which is Ansible for network automation, as well as networking get started. I've also included the link for the YAML syntax if you want a refresher, although it's pretty self explanatory and it's pretty readable, even if you are not familiar with the YAML syntax, but it's always good to have this reference at hand if you want a refresher on how to define a list, how are the key value pair defined in the YAML syntax. So let's get started
>
> **[1:33](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/network-configuration-backup-example-with-ansible?u=76281980&t=93)** and see how we could build a playbook and do something. And then in later section, we'll go ahead and move this playbook into our GitLab [DevOps](../../Topics/DevOps.md) pipeline. Of course, in order to use Ansible, we'll need to install Ansible, since we've had PIP installed in the last example. We're just going to go ahead and use that again and use PIP to install Ansible. So we're going to go ahead and do PIP install Ansible to have that install. And then the next step is really to define our automation target. The way Ansible finds these automation host targets or inventory is to define a host file in various locations, but most conveniently is to define it in the same working directory as the playbook, where the playbook will reside. So here you could see I have a host file with a group called iOS devices. Inside I have defined two names. One is iOS V dash one. And the second one is iOS V dash two. The way that we could define variables such as the IP address, a username and password associated with this inventory host, is actually to have a host underscore vars directory and inside we'll have corresponding names such as iOS V dash one and iOS V dash two correspond to these host names of the inventory host. And so if we could just take a look at iOS V dash one as an example, inside we have the same YAML format.
>
> **[3:11](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/network-configuration-backup-example-with-ansible?u=76281980&t=191)** We have defined IP address, the username, password, and other parameters such as using the privileged mode and so on. So that's how we define the inventory, as well as the variables associated with that inventory. The heart of the Ansible automation, besides defining the inventory and associate variable, is really in the playbook. Playbook is where Ansible indicates which module to use, as well as the parameters associated with that module and what you want to do with it. In this playbook, we use the iOS config module with the backup set to true. So if we look at the documentation for Cisco iOS config module, we could see the backup is a Boolean, is a true false, and this will cause a module to create full backup of the current running config from the remote devices. So in other words, what we're going to do is this playbook will go ahead and look at our iOS V dash one, iOS V dash two device and back up the configuration. So the way to execute this playbook is to do Ansible playbook. I'm going to use the dash I switch to specify the host file as my inventory and then the backup config playbook. And it's taking its time. If you notice now that we have this backup folder that contains the running config of the remote devices.
>
> **[4:49](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/network-configuration-backup-example-with-ansible?u=76281980&t=289)** And if we run that playbook again, it will go ahead and execute again. And as you can see, more backup configurations are being stored in the backup file. So that means the playbook is working. Just like the last example, because we're going to execute this in a Docker container with a GitLab runner in the future, it's a good idea to see how we could make this playbook run in a container environment. Just like what we've done before, we're going to grab a base image and then build from there. So Red Hat actually has a bunch of official Ansible runner with different releases available for you to pull from. They're just conveniently called Ansible slash Ansible runner. So they moved away from Docker hub. That was the original location where they store their containers. And you could optionally use these as base image to build your container. However, in this example, let's see how we could just use the same Python base image as we had before and build from there. So that's why I've commented this out, but you could optionally use this as an option. So what we're going to do is we're going to do from this base Python image, if you notice before, this is the same image, we have the same environmental values, that's set, and we have added this environmental value
>
> **[6:24](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/network-configuration-backup-example-with-ansible?u=76281980&t=384)** of Ansible host key checking. If we don't do this then, because every single time Docker launch, it's going to be a brand new image and you wouldn't have the parameter setting of when you SSH or something for the first time, it will say here's the fingerprints, and if you want a store it or not, you wouldn't have that. And you will result in this error and that's why we need to set this environmental variable. And the [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) of the operations is pretty similar to what we've done before. We create a working directory with pip install the requirements file and we install the requirements file. Then we execute the playbook. So of course, because we now have a new Ansible library that we've installed in our virtual environment. Let's go ahead and do the PIP freeze and replace that requirements file from before now that we have the Ansible version as well as the Ansible core. So let's go ahead and build this Docker image as we've done before we use the Docker built and go ahead and use the dash F switch to indicate which Docker file we're going to build our image from. And we're going to make a target of Ansible example one, don't forget the dot. This means the file is going to be in the current working directory. All right, that was a success. And if we check the Docker image, Ansible example one,
>
> **[7:58](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/network-configuration-backup-example-with-ansible?u=76281980&t=478)** it's there and it's significantly bigger than the one that we had before. Now, let's go ahead and use Docker run to run this Ansible example and voila that playbook is executed correctly within Docker. So now we feel comfortable in putting it into our GitLab runner to be executed in the Docker container. So I understand that in the Docker environment the storage does not persist. So whatever backup configuration we have executed here, locally, in the Docker container, it's just going to be destroyed afterwards. We'll ignore that for now. We'll learn learning on how to create artifacts or have something that persists that could be downloaded per GitLab CI pipeline. So we'll ignore that for now. We're happy that the Ansible playbook works and now we can move on to our next section.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Ansible](../../Skills/Network%20and%20System%20Administration/Ansible.md) (26), [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (6), [DevOps](../../Topics/DevOps.md) (1), [Rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) (1)
> **CLI Commands:** ansible (26), docker (12), python (6), pip (5), make (2)
> **Code Identifiers:** ios (10)
> **Env Vars:** yaml (5), pip (4), ssh (1)
> **Tools:** gitlab (7)
> **Prerequisites:** install (6)
> **Analogies:** such as (2), just like (2)
> **Definitions:** is a  (2), in other words (1)


### 2. GitLab and CI/CD

[↑ Back to Table of Contents](#table-of-contents)

#### [Introduction to Git](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/introduction-to-git?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/introduction-to-git?u=76281980&t=0)** - [Instructor] In this section, let's talk about an important source control system, [Git](../../Skills/Software%20Development/Git.md). If you recall from the last section, three of the most important [DevOps](../../Topics/DevOps.md) principles are collaboration environments with joint responsibilities, continuous development, integration, and deployment cycle and automation in all three steps. If we dig deeper into the three requirements, we could see the need for a joint space to collaborate is required for all three. We need an environment to store all the latest progress. This environment is where we will separate our progress into development, integration, and deployment and they all should be in automated formats. In other words, we need a source control system. A source control system, also known as revision or [Version Control](../../Skills/Web%20Development/Version%20Control.md), is a way to manage code and related files to track changes and allow users to retrieve any of the previous versions of the original files. Git is one of the most if not the most popular version control system in use today. Some other VCSs you may have heard of are VCS, CVS, SVN and Mercurial, but Git is the most popular today and widely used for both public and private projects. In fact, the source code for both the [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md) kernel and [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) are both using Git as their version control system. Git was created by Linus Torvalds for the development of the Linux kernel back in 2005. Git has a following focus right from the beginning.
>
> **[1:34](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/introduction-to-git?u=76281980&t=94)** It supports parallel, independent and simultaneous development of code repositories offline and online. This allows thousands of engineers to work on the same repository at same time without needing to connect to a centralized server. It is also fast and efficient with features such as compression and delta check. Other advantages of Git include maintaining [Accountability](../../Skills/Data%20Science/Accountability.md) by enforcing a changelog every time a change is made. The data object in Git cannot be modified once they're created and placed in the database, making them immutable. The integrity of the repository's insured as the different but related changes are performed together or not at all. Each repository has complete history tracking of all revisions. Also, Git has a very liberal usage license. If we look back at the DevOps principles, Git is a perfect source control system that matches for what we are trying to achieve in DevOps. If we think about version control systems, what we aim to do is to logically organize our files, manage changes, and identify who made the changes. They help us keep track of issues and help us safely consolidate changes and revert them back if needed be. Git trays the files in a tree format where the main branch is the trunk and everything else, such as features and bug fixes, are worked on independently as branches. Once they're done, they're merged back into the main branch.
>
> **[3:08](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/introduction-to-git?u=76281980&t=188)** As an example, we see the main branch was created as having gone through changes A, B and C. At point D, we created a new feature branch and worked on it as D and E. At point E, we decided the feature is done and could be merged or pulled back into the main branch. You may have heard the term GitOps. It is a way to formalize these Git-related strategies such as pull-based or push-based deployments. The basic steps of using Git include making your directory as a Git repository. Git will start to keep track of the files. We could then make changes to the files such as adding, deleting, or updating them. These changes include making a new branch if needed be. We will put the files into a staging area. We need to specify the files that we want to put into the staging area specifically. Finally, we can make the changes permanent by committing the changes. If we have other repositories that we would like to synchronize of the local changes, we can optionally push the changes to them. The best way to learn about Git is to work with it. So let's look at some examples.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Git](../../Skills/Software%20Development/Git.md) (16), [Version Control](../../Skills/Web%20Development/Version%20Control.md) (4), [DevOps](../../Topics/DevOps.md) (3), [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md) (2), [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (1)
> **CLI Commands:** git (16), make (2), python (1)
> **Definitions:** is a  (3), in other words (1), known as (1)
> **Analogies:** such as (4)
> **Env Vars:** vcs (1), cvs (1), svn (1)
> **Exercise Files:** source code (1)
> **Speakers:** - [instructor] (1)

#### [Basic Git operations](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/basic-git-operations?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/basic-git-operations?u=76281980&t=0)** - [Instructor] All right, let's look at some [Git](../../Skills/Software%20Development/Git.md) examples and operations. Before we start it, I want to point out, there's a Basic_Git_Operations.txt file attached along with this course that you could download and practice with. Many of the things we'll do in this section are common practices but we might not have time to go over all the commends, such as git diff or git remote or whatnot, but they're in this file for you to reference and so on. As we mentioned, Git operates in a distributive format. Every Git repository will contain the same history and changes, meaning everybody have the same exact copy. So if we lose one of them, everybody else's, if they're synchronized, then it could be restored and traced back all the way to the original creation of this repository. However, in practice, we typically have a centralized location that we ask everybody to synchronize to. So in this case, let's just say we use the GitLab as our synchronization central location. That will do. So in this case, we'll create a new project on this GitLab, we'll create a blank project and I'll creatively name this my_first_project and the way that GitLab organizes will be the URL, the user, all as the project slug. And we could choose the visibility level. I'm going to go ahead and keep this internal. Initialize a README file, create the project and now voila, we have this repository that has one file, exactly one file, the README file inside of that repository. One thing that GitLab offers that's really nice
>
> **[1:35](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/basic-git-operations?u=76281980&t=95)** is to edit the file within the web UI itself. So what I'm going to do is I'm going to add a new file and creatively call this my_first_file.txt. And I'm going to commit this change and the UI is nice that if it's just something quick and easy that you could just do and you don't have to go through some of the command line operations I will do later on but now that we have a file, let's go back to the project, make sure that we have this first file that is there. Let's just say I am a developer. I want to work on this text file and what I could do is I want to make a copy of this whole repository. So I'm going to go back to my developer machine and what I could do is I could do git clone. So where do I clone it from? I'm going to go back to this project, right? So I'll go ahead and copy the URL for that HTTP address, and prompt my username and password. If you're not using IDE, this would pop up in your command line. So now that it's being cloned down or being copied down into my local directory, I could see that there's this my_first_project and if I got down to that project, I could see there's two file, my_first_project file, my_first_file, as well as the README. So let me go ahead and make some changes or rather, let's go ahead and look at some of the visibility commands in Git. So if I do a git status, it's nothing to be changed, nothing to commit. So if I do a git log, I could see the previous history. Remember, all the histories are kept within this repository, right?
>
> **[3:08](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/basic-git-operations?u=76281980&t=188)** So I could see all the history and then the commit hash on the date and time of where I did that initial commit, as well as adding a new file. So if I clear that, then I could go ahead and make a change into that first file. So I'm just going to use vim. You use whatever you want. But if I just do my_first_file and adding something. Go ahead and save that and if I do a git status, you could see like there's something that's modified but not committed. In other words, it hasn't been put into a staging area, ready to be committed and ready to be pushed back to the origin, which is my remote GitLab repository. So let me go ahead and do that. So if I just do a git commit, I could do a commit message. Modify my_first_file.txt. Oh, I'm sorry. So I need to add this first. So my_first_file and then I'm going to go ahead and commit that and if I do a git status, then I could see now it's green and now it's ready to be pushed. So I could actually do a git remote to see what I have. So right now, I have origin and the origin is my only remote, so in other words, you could have multiple remotes, right? Like you could have something that is GitLab based and then you could have another remote that is [GitHub](../../Skills/Software%20Development/GitHub.md) perhaps. So you could have synchronization on multiple locations. So I am going to do git push. The first is to specify the name of your remote and I know I scrolled it down pretty fast but remember, we talked about branching
>
> **[4:43](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/basic-git-operations?u=76281980&t=283)** and by default, the main branch is created. So if I just do a git push for my origin remote, and now if I come back to my GitLab, if I do a refresh, and then go ahead and take a look at and then you could see that I have added something, right? So now that I could go ahead and form my team members, or if they just do a periodic pull, whatever my change is would be visible and synchronized to the [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) of the team. So let's just say I don't like this file and I wanted to go ahead and delete this file. How can I do that? Let's see. So let me go ahead and do a git remove my_first_file and if I do a git status, you could see this one is deleted, so now I have to add this again. I don't need to add this again because it's already been tracked. So if I just say deleted my_first_file.txt, git status, and if I do a git log, you could see all the changes, right? Like initial commit, adding, modifying and then now deleting but now my delete hasn't been reflected on a remote, so what I need to do is I need to do a git push. The same, origin main. And now it's being pushed. If I do a refresh, now I could see that file is gone. So let's think about the thing that we've just done. On the main branch, everything we do is going back to the main thing, right?
>
> **[6:17](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/basic-git-operations?u=76281980&t=377)** What if we messed up, what if there's more steps that we want to take care of before we want to push it back but we want to keep the history of that? So one thing that we could do is we could do some kind of branching technology or branching strategy. So remember, it's the tree format that we have some smaller branch that grows out and once we're happy with that change, we merge it back into that main branch. So let's go ahead and create a new file. Let's just call this copp.acl. What I have in mind is many times in network engineering, we have som ACL file, which is access control list that we need to keep track with and everybody needs to have the latest version, right? So let's just say we have this file called copp-acl.txt.
>
> **[7:03](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/basic-git-operations?u=76281980&t=423)** And what I'm going to do is I'm just going to copy a COPP file. Oh, sorry. Let me go ahead and do that. This COPP file that I have copy and pasted from this Nexus OS device in my lab. So let's go ahead and practice again. So let's just say git add. Well, let's do a status. So if I do a status, we see this is red, so let's go ahead and do git add copp-acl, git commit.
>
> **[7:38](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/basic-git-operations?u=76281980&t=458)** Git push origin main. All right, let's go ahead and make sure that it's there. These redundant practices, I think it's a good way to reinforce the same idea for more practices. So now we could see the file is there and let's just say I want to make some changes to the file but I want to have a safer way of making that change. I want to do a branch. So now what I could do is we could create a branch. So if I just do a git branch, I see that right now, there's only one main branch. So if I do git branch copp_change, now I could do a git branch. You see, there's two branches. One is copp_change, one is main. But if you notice the star and the coloring stays within main, that means we're still in the main branch, so we'll do git checkout. There's certain shortcuts to everything that I've done here. What I'm doing is just be more verbose and be more illustrative. So feel free to once you're familiar with these processes, go ahead and search for shortcuts if you like so you don't have to create branch, then go into that branch or do a merge elsewhere but anyways, so now you could see that Git tells us that it's switched to the brach copp_change, so if I do the git branch, I could see now I'm in the copp_change branch. So let me go ahead and make a change to that file, copp-acl
>
> **[9:14](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/basic-git-operations?u=76281980&t=554)** and what I'm going to do is just change like the line number. So let me just do 100 and let me just do 90. So if you're familiar with the line number. So now if I push those back, then I could actually add other lines ahead of it with the lower preference. So now I'm saying oh, this is too verbose and I wanted to have lower line number. So now I've changed the line number and if I git add, and...
>
> **[9:50](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/basic-git-operations?u=76281980&t=590)** So remember, we're not at the main branch anymore. Our branches call copp_change so now I need to specify, instead of main is this new branch of copp_change and it's pushed back to the origin. So if I go back to my GitLab, what do you think is going to happen if I go back to here and look at copp-acl? If you say nothing would be different, then you're right because we created a different branch. In order to see that, we have to come back here and look at that branch and then we'll see the changes that we made. What we could do is once we're happy with everything we've done, remember, all the other developers or all the other network engineers could go ahead and make that change to this branch, and it wouldn't impact the main branch. Somebody cloned this would have both branches but they'll see that they're independently developed. So what we could do is we could go ahead and create a merge request, meaning we want to make these changes, this branch changes back into the main branch. And we'll go over assigning reviewer, milestone. This is all part of the DevOp process. Remember, we want more communication, we want to document changes and we want to have [Accountability](../../Skills/Data%20Science/Accountability.md). So what I could do is I could say I pushed the line numbers up and obviously, in the real environment, I will be more verbose
>
> **[11:24](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/basic-git-operations?u=76281980&t=684)** but in this case, I'm just going to assign to myself. I'm not going to assign any milestones or assign, and we'll just go ahead and create that merge request. And now the merge request, I could get some approvers, reviewers, and so on. But I'm just going to approve and we're going to click on merge. And now it's being merged. So if I go back to my repository, if I look at files, and now I am in that main branch and it scrolled down really fast but we had the option of deleting that branch once that is merged, because we don't need that. We could still find that in the history. All the changes that was made in that branch but we don't need that in the brain branch. So if I click here, now you could see the line numbers be reflecting the latest change that we made from the separate branch. Anyhow, I hope you enjoyed the demonstration. I think Git is an amazing source control system that allows collaboration as we see say in [DevOps](../../Topics/DevOps.md), as well as you could see some of the extra features GitLab added to revolve around that DevOps process.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Git](../../Skills/Software%20Development/Git.md) (32), [DevOps](../../Topics/DevOps.md) (2), [GitHub](../../Skills/Software%20Development/GitHub.md) (1), [Rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) (1), [Accountability](../../Skills/Data%20Science/Accountability.md) (1)
> **CLI Commands:** git (32), make (9), find (1)
> **Code Identifiers:** my_first_file (7), copp_change (6), my_first_project (3)
> **Tools:** gitlab (9), command line (2), vim (1), github (1)
> **Env Vars:** readme (3), url (2), copp (2), http (1), ide (1)
> **Cross-References:** go back to (6), we mentioned (1), we talked about (1)
> **File Paths:** my_first_file.txt (3), basic_git_operations.txt (1), copp-acl.txt (1)
> **Definitions:** in other words (2), is an  (1)

#### [GitLab fundamentals](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/gitlab-fundamentals?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/gitlab-fundamentals?u=76281980&t=0)** - [Instructor] In this section and the next few sections, we'll take a look at how GitLab fits into the [DevOps](../../Topics/DevOps.md) picture. We'll use GitLab as a tool to illustrate these practices and features that we can actually implement into our development environment today, and how they can help us enforce the DevOps principles and practices. Let's start with the GitLab fundamentals. GitLab describes itself as the DevOps platform that empowers organization to maximize the overall return on [Software Development](../../Topics/Software%20Development.md) by delivering software faster and more efficient while strengthening security and compliance. To me, the biggest difference between GitLab and other platforms is its integration of a source control system, as well as a CI/CD engine into the same platform. On a daily basis, we're all by now familiar with the DevOps principles and practices. But on a daily basis, as engineers, what we interact with closely is, one, the source control system, and two, is these automation processes, the stages that we could define to automate our task for us. And it's nice to have those two pieces fit in together, as well as some of the other features that we'll see later on in this course that GitLab implement that fits pretty well with the DevOps principles that we've been learning and practicing so far. Personally, I used to use [GitHub](../../Skills/Software%20Development/GitHub.md), and I still do for some of my projects for repositories to store files.
>
> **[1:33](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/gitlab-fundamentals?u=76281980&t=93)** Then, use another platform such as [Jenkins](../../Skills/DevOps/Jenkins.md), when I need to execute and run my automated delivery pipeline. And because they were two separate tools, there were extra steps needed in order for the tools to work together, whereas in GitLab it's integrated into a single platform. It is just a one-step shop integrating both, and the experience is a little bit more smoother, and that is why I choose to use GitLab as the underlying technology for this course. This is a simplified, architectural diagram, it could get pretty complicated if we need to dig deeper into them. Luckily, all-in-one installation took care of most of the complication for us when we're just building a lab and we're learning about GitLab. Especially, we're just using GitLab to illustrate some of the DevOps principles and practices. What we do need to pay special attention of and be aware of is that GitLab server is where we store our code and related files as we've seen in the last two sections. And we will actually have GitLab runners that execute these instructions. And these runners are actually registered to the GitLab server, and constantly communicate back and forth to take instructions from them. Let's take a look at the different parts of the GitLab server through the actual implementation and the browser. GitLab is a very powerful platform, so obviously we'll only cover the big pieces, and we're focused on using the GitLab platform to illustrate the DevOps principles.
>
> **[3:07](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/gitlab-fundamentals?u=76281980&t=187)** We're just going to cover the pieces that we need, and hopefully convey the principles of DevOps, and also illustrate that DevOps is something that's actionable. We could use actual tools to apply them to help you in your everyday operations. Right off the bat, last section and the section before that, we actually created a project, and we created a repository and synchronized the repository, those are pretty self explanatory. What is not as obvious, there's an Admin page that configures the options for instance level configurations. What does that mean? That means if we configure here, it's a very broad configuration that applies to all the projects within the server. For example, if we go to settings, and what I typically do is, especially for a lab device is, actually to expand the signup restrictions and disable signup, because I know this is a publicly accessible URL and I don't want people to just stumble upon it. Sign up and just a safety measure, I will also require admin approvals for new signups. This way, I could restrict signup and don't have to worry about people accidentally sign up to my server, and perhaps expose some private informations. And as we have said before, the two items that we probably interact the most as engineers, one is the CI/CD pipeline, one is the repository. And the CI/CD pipeline is carried out by the GitLab runner. And if you go to the Admin area, and if you go down to Runners,
>
> **[4:38](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/gitlab-fundamentals?u=76281980&t=278)** then this is where you could register additional runners. For me, I've already registered two containers that talks back to this particular GitLab server that execute the GitLab Jobs. And here's where you could get the token to register more runners. And if you're interested in the instruction on how to do that, there's the GitLab installation file that we have attached with this course. Let's go ahead and take a look at how we could define a pipeline. Before we do that, I do want to mention that this Admin area might not be available to you if you use a hosting model, like for example, on the Cloud Hosting option for [gitlab.com](https://gitlab.com). Or if this is built by somebody else, and they're only giving you more restricted rights as the user rather than the admin. Assuming you follow the step we've laid out and installed this on, say a [Ubuntu](../../Skills/Web%20Development/Ubuntu.md) VM, then you would have access to instance level configuration options. If you recall, we've already created my first project. As mentioned before, the source control system is one thing that we interact very, very often as engineers. The other is to execute and build our CI/CD pipeline, like our continuous pipeline. Where can we do that in GitLab is where we put in this CI/CD pipeline, is where we see the progress and so on. But before we do that, we need to create this specific file called .gitlab.ci.yml.
>
> **[6:18](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/gitlab-fundamentals?u=76281980&t=378)** And in this file is where we could define stages and jobs that we want GitLab to perform for us. By default, they have a testing stage where all the job belong to that stage. But in here, I like to be more verbose, just so there's no accidental jobs or whatnot. You don't need to see me type, I'm just going to copy and paste this in. What this file does is it define the stages as one stage, which is deploy. And you will define the job, which I specify as deploy our network. And I will specify image, which is a Docker image, by default you pull from Docker Hub. And I'm pulling the Ubuntu 20.04 image, and I'm associate this job with the deploy stage by this key value pair. And the script session is where we will say the steps we want to take for this particular job. I'm going to commit this change, and I am going to go back to the CI/CD pipeline. And let me go ahead and check why that's not working. Pository files and, oh, that's a misspell, let me go ahead and just edit this file. It should be gitlab. And I'm going to commit this change. And if I go back to the CI/CD pipeline, I should see it's being picked up. This means it's being picked up by the runner, and if I just click on it I could see the progress, is preparing for the Docker executor.
>
> **[7:54](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/gitlab-fundamentals?u=76281980&t=474)** And it's running with GitLab runner 14.6, and that's the same version as the GitLab server. Now it's pulling the Docker image, and you can see all the progress, it's illustrating all the jobs. And voilà! The green means it's good to go.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [DevOps](../../Topics/DevOps.md) (9), [Ubuntu](../../Skills/Web%20Development/Ubuntu.md) (2), [Software Development](../../Topics/Software%20Development.md) (1), [GitHub](../../Skills/Software%20Development/GitHub.md) (1), [Jenkins](../../Skills/DevOps/Jenkins.md) (1)
> **Tools:** gitlab (27), github (1)
> **CLI Commands:** cd (7), docker (4)
> **Definitions:** is a  (5)
> **Analogies:** for example (2), picture (1), such as (1), for instance (1)
> **Cross-References:** go back to (2), in the last (1)
> **UI Navigation:** go to (2), click on (1)
> **Versions:** 20.04 (1), 14.6 (1)

#### [Network application example](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/network-application-example?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/network-application-example?u=76281980&t=0)** - Remember before we had a show version [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) script that we could run locally. Let's see how we could put this file onto GitLab and have it execute for us automatically using the previous knowledge that we've gained from the last few sections. So just to recap, this is the show version Python script. What it requires is obviously the nornir package as well as the nornir netmiko driver. And what we have here is also a host.yaml file that specifies the IP address, the point number, username, password and we can also execute this show version script. And what it would do is it goes out and look at the host file, execute the show version and print out the command. So let's see how we could get this onto the GitLab repository. So let me go ahead and move to our first project. If you'll remember right now we just have the copp-acl.txt file, as well as the readme file. So we could just start adding files to this branch, but remember it's a bad idea to work on the main branch directly. So let's go ahead and create a branch. So if we do a [Git](../../Skills/Software%20Development/Git.md) branch, we see right now is the copp change. So for do a git checkout main, right? So git branch and let's go ahead and say
>
> **[1:32](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/network-application-example?u=76281980&t=92)** git branch add show version and git checkout add show version. Cool, so if I do a git branch, always good to verify, show version. What I need is first the host file. Let me just make sure the naming, the hostfile.yml and I'm just going to copy and paste onto that directory. And there you go. That's .71.72. Those are the two iOS devices, and I am going to look at the show version file, and just copy and paste into this show version onto the repository. So I'm using vim as the editor, feel free to use whatever you feel comfortable with. And I am going to try to execute this show version and it executes correctly. So let me go ahead and do a git status to check out Okay. So the nornir lock is basically the residue of what happened as the nornir executes. It would write all the outputs to the log. So what I really need to add are the host file and the show version file.
>
> **[3:04](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/network-application-example?u=76281980&t=184)** So if I do git add host.YAML, show version, and if I do a git status, then those two new files are in the stage and ready to be committed. And I say, add, show version.py and host.yaml.
>
> **[3:25](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/network-application-example?u=76281980&t=205)** Great. So now, if I do a git status I'm seeing these files are ready to be pushed. So git push, origin and add show version as the branch.
>
> **[3:41](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/network-application-example?u=76281980&t=221)** So I push that branch over to the repository and I've merged it. Now that I have the host file, see right here, and now I have the show version file, how do I push this into the CICD pipeline? Of course, we have to go ahead and edit this file. So why don't I go ahead and just copy and paste the stages which is deployment as well as the show version job I've associated to this stage for deployment. And in this time I'm going to pull the Python 3A image in the script. I'm just going to run Python three show version.py and I'm going to commit this file and we'll see how it goes. Let's go back CICD pipeline and now we could see that it's running and no good. So it says the job show version, job failed and we could see the output and there's a trace back. And we could clearly see it failed at line two from inputting the nornir library. So this is a very important concept. Remember, we're executing this in a brand new environment, every single time in our Docker image. And what we have done before was we installed nornir utils and nor near netmiko into our virtual environment, but that virtual environment does not exist on the Docker image because it's created new every single time. So what we have to do is we need to go back to our repository file and we'll add that step so that every single time
>
> **[5:16](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/network-application-example?u=76281980&t=316)** it runs is going to grab those necessary packages. So if I go back to here and do an edit, and by now you could see it's nice to be able to edit that file directly in the web UI as opposed to having to check that in every single time. And I'm going to commit that change again, and let's go back to CICD pipeline, and it's running again. And let's see if it does work. If we just give it a moment. It's also nice to be able to see the process as it goes through each of the steps. So we'll just be patient and let the Docker image pull. And as you can see this is what happens, distinctly different than what happened last time. It fetches the Python image. It goes ahead and installed the nornir netmiko and it executes the show version. And if we want, we could go ahead and re-execute this again, just go ahead and retry it again. And if I go back to the terminal we should be able to see that the process gets run again. And every single time we run this job it's going to go and pull that package again. It's just going to take a while to pick up this job but eventually it's going to work.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Git](../../Skills/Software%20Development/Git.md) (11), [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (5)
> **CLI Commands:** git (11), python (5), docker (3), make (1)
> **File Paths:** host.yaml (3), version.py (2), copp-acl.txt (1), hostfile.yml (1)
> **Env Vars:** cicd (3), yaml (1)
> **Tools:** gitlab (2), vim (1), terminal (1)
> **Cross-References:** go back to (2)
> **Code Identifiers:** ios (1)
> **Versions:** 71.72 (1)

#### [GitLab DevOps CI/CD pipelines](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/gitlab-devops-ci-cd-pipelines?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/gitlab-devops-ci-cd-pipelines?u=76281980&t=0)** - [Instructor] We have already seen some simple examples of GitLab pipelines. In this section, let's go ahead and take a more detailed look at it. In [DevOps](../../Topics/DevOps.md), CI/CD or [continuous integration](../../Skills/Software%20Development/Continuous%20Integration%20(CI).md) and [continuous delivery](../../Skills/Software%20Development/Continuous%20Delivery%20(CD).md) is an important concept. As we mentioned before, this is usually where we spend most of our time to develop our steps in automation. I'm not going to repeat the definition here because we've already talked about it, but to me the most important bit is they provide a general guideline to divide our automation steps into stages. In particular, if we use GitLab pipeline as an example, they could generally be divide into four stages: build, test, staging and production. If we use a compile language such as [Java](../../Skills/Software%20Development/Java.md) that requires taking the source code and compile it into a binary software before we could use it, this step is generally done in the build stage. But as network engineers, we generally use non-compiled languages such as Java or [Ansible](../../Skills/Network%20and%20System%20Administration/Ansible.md). So generally I would just interpret the build stage as just running the code and taking care of any steps needed before we run this code, such as PIP install certain libraries. Since DevOps is all about streamline processes and automate our steps, testing is crucial in ensuring any new features or bug fixes do not break existing base nor does it introduce new issues.
>
> **[1:36](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/gitlab-devops-ci-cd-pipelines?u=76281980&t=96)** And testing could be divided into certain categories such as unit tests, acceptance tests, [Integration Testing](../../Skills/Software%20Development/Integration%20Testing.md) and so on. And testing used to be the job of QA engineers. If you remember those roles, but it is better if we integrate them into the whole CI/CD process of course. So testing is so important that we will actually dedicate many lessons later on in this course to discuss them in more detail and give more examples. A staging environment is a near replica of the production environment. They should replicate production as closely as possible. And as we get closer to execute our code into the production environment, our scope increases from small to big. So staging is really that one step before we deploy our code into production. As we have discussed before network engineering used to lack a staging environment due to cost and complexity, but with the advance of virtualization of network gears, we could actually pretty accurately replicate our production environment network, in the virtualized staging environment nowadays. The deployment to production is generally the last step in our pipeline. Of course, we'll continue to monitor and test the network, but deployed to is generally the last step that we really need to look out for. And there are many deployment strategies. It is not possible in all cases,
>
> **[3:09](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/gitlab-devops-ci-cd-pipelines?u=76281980&t=189)** but if we could we should make the deployment in stages. For example, we could deploy the new changes into a small percentage of our devices, then gradually migrate to the [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) of the devices. In this way we could discover any possible defects and roll back if needed. And if indeed, unfortunately we introduce some new bugs or break some stuff, the blast radius could actually be smaller that way. Another note for deployed to production in my experience, most organization will automate all the steps up to staging but leave deployment to production as a manual step. Generally this step in between staging and deployment to production would have some more reviews, possible management approvals, or we leave the change into a specific maintenance window, depending on the potential impacts. When it comes to building the pipeline. I probably spend the most time here on the GitLab documentation for CI/CD pipelines. And I will encourage you to also take a look at least browse around on the different stages or branches tagging on different GitLab pipeline features such as scheduling, triggering a pipeline, the variables you could do doing any kind of cashing or artefacts. So I would highly encourage if you do use GitLab or you want to see the practical examples
>
> **[4:42](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/gitlab-devops-ci-cd-pipelines?u=76281980&t=282)** of how GitLab interprets the CI/CD pipeline and apply them, then take a look at this documentation.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [DevOps](../../Topics/DevOps.md) (2), [Java](../../Skills/Software%20Development/Java.md) (2), [Continuous integration](../../Skills/Software%20Development/Continuous%20Integration%20(CI).md) (1), [Continuous delivery](../../Skills/Software%20Development/Continuous%20Delivery%20(CD).md) (1), [Ansible](../../Skills/Network%20and%20System%20Administration/Ansible.md) (1)
> **CLI Commands:** cd (4), ansible (1), pip (1), make (1)
> **Tools:** gitlab (6)
> **Analogies:** such as (5), for example (1)
> **Definitions:** is an  (1), is a  (1)
> **Env Vars:** pip (1)
> **Cross-References:** we mentioned (1)
> **Exercise Files:** source code (1)

#### [GitLab pipeline example: Build](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/gitlab-pipeline-example-build?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/gitlab-pipeline-example-build?u=76281980&t=0)** - [Instructor] Let's go into some more details with the CI/CD pipeline and network automation. Start with the build stage. The build stage is typically used when you're using a compile language that requires a compilation process, such as when you're using [Java](../../Skills/Software%20Development/Java.md) or C when we need to translate the code into a binary and the binary could be then taken for testing, for deployment and so on. In our case, we're using [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md), so the build process is considerably less involved. In our case, we could just think of the build process as getting everything ready for the next step. So what we could do is we could actually modify the gitlab.ci.yml file to move the pip install process into the build stage. So let's see how we could do that. Initially, what we could do is we could go ahead and add a build stage. And we'll follow the same steps and we'll just call this preparation and we'll do the stage of build. And let's go ahead and specify the image as well. If we don't specify the image, it's just going to take whatever default there was when we set up the runner. And what we could do is we could actually take this and put it into the script section so that we now have a preparation job in the build stage. So let's go ahead and commit the change. Once we're committing, we go to the CI/CD pipeline and now the first thing we can notice is now it's a different sequential process.
>
> **[1:34](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/gitlab-pipeline-example-build?u=76281980&t=94)** So now we have the build and later on, we have the deploy. And if we refresh this, we see the job is being picked up and it's being prepped, and it's successful because it's downloading. However, if we go back and we refresh the page and see the status, we could see the job has failed and this show_version job has failed and we're receiving the same error as line two that we cannot import the Nornir package, which is kind of strange because we installed it in the prep stage, in the build stage, but it's failing to see that in the next stage. And what this signals is a very important concept within our CI/CD pipeline, which is that each step is independent of each other and it's good that it's happening this way because just like Docker, we're forced to think of our stage being able to execute parallelly as well as cleanly built from the ground up. So how can we solve this? There're many ways to solve this. One way we could do is just to move this step into another keyword step, which is the before_script. So let's go ahead and do that. We move this into before_script. And we'll just repeat that same step, which is pip install this Nornir package. And we also notice the image is repeated twice,
>
> **[3:13](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/gitlab-pipeline-example-build?u=76281980&t=193)** so we could actually also remove this upfront and have this image and say specify Python 3.8 for both the prep and the show_version job. Now that we have done that, this is kind of redundant, so what we'll do right here is I'm just going to say python --version just so that we could see that sequential step. So I'm going to commit this change and once it's committed, then we go back to the pipeline and see if this is being executed correctly. So we're looking at the preparation and after this is being executed, we'll also see the next step, and one thing I want to point out is you can actually take a look at the time that's been spent from this particular job to gauge how fast this is running. Right now, it's only seconds, so we don't have to worry about it but later on when it gets to minutes, hopefully not hours, but when it gets to minutes, then we will start trying to optimize each of the steps and stages. A good rule of thumb is to keep everything below five minutes. The whole pipeline below five minutes. So if we go back to the pipeline, and now we could see that the show_version job is now being executed successfully. Another issue with our current script as is it is we're executing this show_version script and we could see the output within the console
>
> **[4:47](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/gitlab-pipeline-example-build?u=76281980&t=287)** but it's not something we could keep, like we mentioned before. Every single job is independent of each other, so what if we want to keep some kind of output in a persistent storage? So a little bit of research on the Nornir package, we could see that the Nornir package is the multi-result object and in order to see the results, we could treat this basically as a dictionary on this host, whatever we specify in the host file, the host, as well as getting the first placement of results to display, and that is the result that we would obtain. Let me go ahead and show you what this will look like. So if I look at a modified version of the show_version, what we could do is we could loop through the multi-result object, loop through the keys and we could open it up, write it to a file, using the hostname as the filename and write the result of that first item of results. So if we execute this, the show_version, and we do a list, then we could see we have two additional files. It says iosv-1, iosv-1, iosv-1 and iosv-2. So let's go ahead and copy this additional change over to our script. So now that we have two additional files
>
> **[6:21](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/gitlab-pipeline-example-build?u=76281980&t=381)** that we could download and obtain. So let me go ahead and replace the print results with the new section. All right, so let's go back to the CI/CD pipeline and now it's going through the preparation and soon, it would be able to execute the deployment stage. Now it's being executed and now let's take a look at the console output. If we just be patient in and taking a look. And a lot of times, when we're executing these, it's really just getting familiar with the process, getting familiar with the pipeline and finding the necessary commands and processes that will fit into our workflow. So we could see the package has been installed and it's executed the show_version and job succeeded. But how can we get those output and how can we get those results? So one way that we could do that is to modify the GitLab stages and we could go ahead and declare an artifact, which is essentially telling GitLab to keep whatever we specify in the artifact as a downloadable object. So how can we do that? Let's go ahead and edit this file. And let's go ahead and modify this build pipeline. We already know the files will be hostname and both of them starts with ios, so let's go ahead and do that.
>
> **[7:53](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/gitlab-pipeline-example-build?u=76281980&t=473)** Let's do artifacts and declare the path that we need to keep and we'll just use a wildcard and say ios start. So let's go ahead and commit that change. And the CI/CD pipeline should automatically picked up and ready to run. Since we're not really doing much in the build process, what we could have done is also just bypassed this step but I do want to leave it in just so that you could see this multi-stage process and whatever we need to do in order to prepare for deployment, that is where we should put our steps in. And let's go ahead and look at that show_version. Again, we just need to be patient and let the job run. And now that it's run and what you would notice is now there's a job artifact, and we could go ahead and browse it and now we could see there's two files and if we click on one, we could go ahead and download, save it and if we open up that file, we could see it is a result of show_version for our iOS devices. So the artifact is a pretty important concept to realize, both for compilation and non-compilation languages. If you're wondering about where I find these keywords, before_script, or images or artifacts, it is within the GitLab documentation,
>
> **[9:26](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/gitlab-pipeline-example-build?u=76281980&t=566)** and this is where one of the things that we would constantly do is we know the concepts of [DevOps](../../Topics/DevOps.md), we know the principles and processes, however, in order to execute and realize those concepts and steps, we need an actual application, which will go back to whatever tools we choose, whether it be [Jenkins](../../Skills/DevOps/Jenkins.md), or let it be [GitHub](../../Skills/Software%20Development/GitHub.md) and look through the documentation and see which of these functions, preexisting features that will match our desired principles or processes.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (3), [Java](../../Skills/Software%20Development/Java.md) (1), [DevOps](../../Topics/DevOps.md) (1), [Jenkins](../../Skills/DevOps/Jenkins.md) (1), [GitHub](../../Skills/Software%20Development/GitHub.md) (1)
> **Code Identifiers:** show_version (9), before_script (3), ios (1)
> **CLI Commands:** cd (5), python (3), pip (2), docker (1), find (1)
> **Cross-References:** go back to (4), in the next (1), we mentioned (1)
> **Tools:** gitlab (4), github (1)
> **Definitions:** is a  (3)
> **Prerequisites:** install (2), set up (1)
> **UI Navigation:** go to (1), click on (1)

#### [GitLab pipeline example: Review and test](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/gitlab-pipeline-example-review-and-test?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/gitlab-pipeline-example-review-and-test?u=76281980&t=0)** - [Instructor] We've talked about the importance of collaboration and communication in [DevOps](../../Topics/DevOps.md). Let's see, with an example, how we could do that. So to begin with, let me go ahead and create some labels. It's pretty customary to create labels of, say, features, new features. And let's go ahead and create a label of bugs. Let's give the red because that's no good, it's high importance. And let's go ahead and do some process, processes. And let's go ahead and give that a green. So now we have three labels where we could signal or indicate any of the issues that comes up. Say, you're a project manager and somebody reported a bug, so now you could go ahead and report in this repository as an issue and assign to some of your members. So let's go ahead and create some milestone which we could do is on Issues and Milestone... And I've already created a milestone which is v1. And if we take a look, let's push toward v1. And we have a start date, we have a due date, and we can also associate issues for this particular milestone. So it's a good collaborative tool for, say, project manager, managers, or developers to all collaborate and have a visibility into the progress toward that. So of course, right now my project only has one member.
>
> **[1:33](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/gitlab-pipeline-example-review-and-test?u=76281980&t=93)** So let me go ahead and invite more members. And I could just search for a member. I think Han Solo is a good person. I'm going to give him the role of the maintainer, and I am going to set no expiration. And I'm going to invite Hans to this project. I'm going to go ahead and create an issue. I'm going to list out an issue. I'm going to raise the new issue and say, "Hey, I think we should have some code quality check." I'll label this as issue and say, "I think should have some code quality check."
>
> **[2:09](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/gitlab-pipeline-example-review-and-test?u=76281980&t=129)** And I'm going to assign to Han. And I am going to put a milestone of v1 and label this as Process. Awesome. And I'll make a due date of, say, the last day of April. And I'll go ahead and create that issue. All right, so now I'm logged in as Han Solo right here. And I have some messages, and then I'm seeing that Eric has assigned issue one to me. And I'm going to go ahead and take a look at what he meant. And I could also see I'm a maintainer on the first project. Taking a look at Issue, and I'm going to list that issue, say, "I think we should have some code quality check." And I say, "I think that's a great idea. I will implement that." And I'm going to make a comment. And I'm going to go back to the repository. And what I'll do is I will implement this code checking called the flak8. So what I'll do is I'll go ahead and put in pip install flak8. So I have it available for us. And I am going to add a QA step for then, and I'm going to add a QA job just right before preparation, flak8 code check. And what I'll do is associate this with stage QA, and the script will simply be flak8 show_version.py so I could go ahead and check the code quality.
>
> **[3:48](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/gitlab-pipeline-example-review-and-test?u=76281980&t=228)** I'm going to commit that change and author something just now. And let me go back to that CI/CD pipeline. So you can saw it now, I have three stages is built and then deployment. So let me go ahead and see that pip install flak8. Oh, I'm sorry. There's a typo. So let me go ahead and fix that. So if I go back to Repository, pip install, go ahead and edit that, pip install flak8. Go ahead and commit that change. And as you saw, because these are sequential, so every fail step will stop the progress from moving on to the next stage. So now I go back to the pipeline. Another thing is I could simply rerun this, but I've committed again. So now it's running again. Now it's running as we expect it. And now we see flak8 show_version with import but not used and some trailing whitespaces. So potentially, we could fix all these, but let's just pretend we don't have any errors just for the time sake. And we resolve that issue. And now that we say, "Hey, I come back to this issue," and I'm going to say, "I resolve this," and I'm going to close this issue. Awesome, so that is a quick example of how we could use GitLab for collaboration, setting up milestones, listing out different labels and open issues, collaborate, and close that issue.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [DevOps](../../Topics/DevOps.md) (1)
> **CLI Commands:** pip (4), make (2), cd (1)
> **Cross-References:** go back to (4), as you saw (1)
> **Prerequisites:** install (4)
> **Code Identifiers:** show_version (2)
> **Definitions:** is a  (2)
> **File Paths:** show_version.py (1)
> **Tools:** gitlab (1)

#### [GitLab pipeline example: Deploy](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/gitlab-pipeline-example-deploy?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/gitlab-pipeline-example-deploy?u=76281980&t=0)** - [Instructor] So far our steps has been executed sequentially and automatically. What if we want, as we've mentioned before, a lot of times we want our final deployment to be manually pushed. So we'll run our co-quality check, we'll run some testing, and before we push it down to actual production deployment, we want to have that final step as a manual push. Maybe we want to set a particular maintenance window, maybe we want more reviews, more eyes on the code, so on. So how can we do that? It's actually quite simple. All we have to do is just go to GitLab CI and let's go ahead and edit that file. And let's just say after we do the Flake8 check, we do the preparation and we want the last job to be pushed out manually. So we have this keyword and say when that is manual, an additional keyword that's usually often used in concurrency is allow failure, which I'll set it to false. What this does is typically, even when you have a manual push, if it fails, it's not going to stop the other jobs from executing after it, but what I want is if it fails, then I don't want any job to execute behind it. So I would just say allow failure equals to false. So I'm going to commit this change and right away I could go ahead and look at my pipeline. The pipeline should execute as expected, so of course it starts with the Flake8 code checking.
>
> **[1:36](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/gitlab-pipeline-example-deploy?u=76281980&t=96)** I know last section, we had some white space and I think it was just extra white space as well as extra line at the end or whatnot. Those are typically kind of annoying, but it's necessary for code checking so it conforms to whatever standard that your team or yourself set. So now you can see Flake8 show version of that particular file is passed. And let me go ahead and refresh this so we can see this step is passed, and now we're just doing the preparation and the last step, because we set up to be manual, that we should have as being manual so now as you see, this one is not run and I am going to run this last step manually and now that it is running.

> [!info]- Semantic Content
>
> **Tools:** gitlab (1)
> **UI Navigation:** go to (1)
> **Prerequisites:** set up (1)
> **Speakers:** - [instructor] (1)

#### [GitLab pipeline example: Monitor](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/gitlab-pipeline-example-monitor?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/gitlab-pipeline-example-monitor?u=76281980&t=0)** - [Narrator] For generic monitoring and testing, besides building some steps, like what we've done in the GitLab stages to verify the changes that we've done within the pipeline themselves. One of the features I utilize a lot, is the scheduling function for CI/CD. Essentially, I will build my pipeline for any kind of monitoring show BGP neighbor or if I ping through the interface and grab the results then after building that pipeline, I'll come back to the CI/CD schedules function and go ahead and make a new schedule. And I'll provide a description, say show version daily because this repository gives us show version for all of our inventory files. And I would say run every day at say 10:00 AM, right? So custom 10:00 AM on UTC and the target branch will be main. And I'll go ahead and save that pipeline. And it's going to do the scheduling, but you know you could also execute it right away just to test it out and so on. This is one of the most useful function in my opinion when it comes to monitoring. And keep this function in mind when later on we go into the testing section of this course that you could build a pipeline full of tests, show BGP, show interface status, all of that and create artifacts from the results and use the schedule. To me, that's a very useful combination
>
> **[1:36](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/gitlab-pipeline-example-monitor?u=76281980&t=96)** for narrowing monitoring and testing.

> [!info]- Semantic Content
>
> **CLI Commands:** cd (2), make (1)
> **Env Vars:** bgp (2), utc (1)
> **Ports:** :00 (2)
> **Tools:** gitlab (1)
> **Speakers:** - [narrator] (1)


### 3. Network DevOps Examples

[↑ Back to Table of Contents](#table-of-contents)

#### [Network configuration backup with Ansible](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/network-configuration-backup-with-ansible?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/network-configuration-backup-with-ansible?u=76281980&t=0)** - [Instructor] If you recall a few sections ago, we used a [Ansible](../../Skills/Network%20and%20System%20Administration/Ansible.md) playbook to backup configuration our stage devices. In this section let's, expand on that example to be a more realistic playbook, realistic inventory file, and build from there into a CI/CD pipeline as an example. So to begin with, we had this stage environment, but now I've also enabled our production environment. If you remember, the staging environment was basically just the left side of it, just the LAX. But in production, we actually have a few more devices or essentially, we have the NYC data center as well. It's a carbon copy of LAX. Same thing as in NX-OS for the core and iOS devices for the edge and running EBGB between them OSPF as our IGP. So, now we have two different environments we could go ahead and build our playbook to accommodate both the staging as well as the production environment. As such, we have two host file or two inventory file. One is called the stage_host and one is called the production_host. And in there, we have a little bit more complex inventory file. What do I mean by that? In here, you could see, I have the stage_cor and under the hosts, we have the stage_lax_Qor_r1. We have this host variable, which the IP address
>
> **[1:35](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/network-configuration-backup-with-ansible?u=76281980&t=95)** as well as the ansible_network_os that's all good. But under the stage_edge devices under hosts we're actually have two hosts, each of them with their own host variable but now we have a group variable that's shared between them so, this vara keyword declares the ansible_network_os is iOS for both of them. And in here, we actually have a stage_all hosts name with the children of stage_cor and stage_edge. These two names refers back to the stage_edge and stage_cor up in the same inventory file. And I'm declaring more group variables such as ansible_user, ansible_ssh_pass and connection and so on for all of the devices. So, this is much more scalable. Imagine if we have more devices such as our production_hosts we actually have four more iOS devices we don't have to repeat this line for all of them and risk the possibility of making mistakes. And so same thing here, where we have two cor devices and we declare the variable for both of them. And then having declared all host file with the children device and so on. So this is a little bit more involved inventory file. If you're interested in where I get these keywords and key values, you could take a look at this, build your own inventory and support documentation. All right, so now that we've seen the inventory file, we have, let's take a look at the playbook. The playbook is pretty similar except we'll get
>
> **[3:09](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/network-configuration-backup-with-ansible?u=76281980&t=189)** to these variables just a little bit but in this case previously we only have the iOS config module with the backup, yes. So we added a few more things. One is the backup options of the directory path. So essentially this is in a little bit of a preparation later on when we're involved both environments, the station, the prod I will need to persist or declare an artifact for the production environment but for the staging environment, basically I'm just saying I could throw that away as long as it passes our test, therefore I want to specify different directory path based on whether it's staging or whether it's production. And also, because, previously we only backup iOS and now we have NX-OS. So I've also declared here when the ansible_network_os is iOS. I want to use iOS module and when the ansible_network_os is NX-OS I want to use the NX-OS module, which makes sense, right? Because we don't want to mix and match there might be some commands existing in NX-OS that doesn't exist in iOS and vice versa. So, we want to pin them down. And if you're interested in finding more about these usage, you could take a look at the iOS module or NX-OS module on the documentation where it says, to use it in the playbook, specify the network_os as well as you could take a look at the backup options as well as other variables or other available features in this particular module. All right, so moving on, if you you wondering about
>
> **[4:44](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/network-configuration-backup-with-ansible?u=76281980&t=284)** what these environmental variables are, that is exactly what they are. So if you have this funny looking brackets and the lookup, this is a feature provided by ansible for you to specify some of the values in your environmental variable. So in this case, because I will treat that you user, the password as well as enable password as sensitive information. I don't want to list them in the playbook or the host file inventory host file in this case. So therefore I want to declare them in the environmental variable. So, basically I would say, export ansible_user=cisco. And I could do a printenv ansible_user and, if I could type printenv ansible_user then I could say that value is Cisco. So instead of using that value in the inventory file like a hard code value, that's clear tags and visible to everybody else, I could go ahead and embed that into the environmental variable, and the way to have the playbook look up is using this look up. And that is the same concept within the playbook using the same lookup but it serves a little bit of different purpose. So basically what I want to do is I want to use the same playbook for both of the environments. What does that help us to do, essentially for [DevOps](../../Topics/DevOps.md) if you remember, we want to be able to reduce the variables and reduce ambiguity as much as possible.
>
> **[6:20](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/network-configuration-backup-with-ansible?u=76281980&t=380)** So we could certainly have a playbook that backup staging environment and once that works, we copy and paste that same playbook to backup our production environment, that's doable and could declare them into the same pipeline. However, that introduces more ambiguity. There's always the chance that these two playbooks will have some kind of drift. So what I want is, I want a playbook that's flexible enough to use the same playbook but behave differently when it's staging environment versus production environment. So using the environmental variable allows me to do that. However, I don't think of that as being sensitive information. So what I've done is I put them into a shells script and that's basically, when I execute the shells script for set_prod_ env, it will set the host to particular host name and the backup directory. And when I set the stage to this shells script then the stage would be set to a different host name and backup directory. If that makes sense, let's go ahead and execute this. So essentially what I want is, let's do the set stage first. So, if I do the set_stage_env I'm sending that to the stage environment and I could go ahead and execute that playbook. Remember I am using the stage_hosts and the same playbook, correct? So remember we're matching up the Network OS and therefore when I'm backing up the edge I'm skipping the cor. And when I'm backing up the cor, I'm skipping the edge.
>
> **[7:55](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/network-configuration-backup-with-ansible?u=76281980&t=475)** And if I come back here remember, I'm backing up to a stage backup folder. So this stage backup folder, we have these three devices. So let's go ahead and execute and try it out on the production. So if I just do source set_prod_env and let me go ahead and do ansible_playbook and remember I am using the production host but, I'm using the same playbook and now I'm executing that playbook but it's grabbing the different environmental variable so that I could execute on different devices. It looks like it's working for the edge. Now it's working for the cor. And if I look back here you could see now it's backing up the edge. So that is a more realistic playbook and the associate host files variables and so on. So in the next section, let's see how we could use this and build a more realistic CI/CD pipeline throughout DevOps environment which is [GitHub](../../Skills/Software%20Development/GitHub.md). I'll see you there.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Ansible](../../Skills/Network%20and%20System%20Administration/Ansible.md) (2), [DevOps](../../Topics/DevOps.md) (2), [GitHub](../../Skills/Software%20Development/GitHub.md) (1)
> **Code Identifiers:** ios (9), ansible_network_os (4), ansible_user (4), stage_cor (3), stage_edge (3)
> **Env Vars:** lax (2), nyc (1), ebgb (1), ospf (1), igp (1)
> **Definitions:** is a  (3), is called (2)
> **CLI Commands:** ansible (2), cd (2)
> **Analogies:** such as (2), imagine (1)
> **Cross-References:** in the next (1)
> **Documentation:** the documentation (1)

#### [GitLab integration for configuration backup](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/gitlab-integration-for-configuration-backup?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/gitlab-integration-for-configuration-backup?u=76281980&t=0)** - [Instructor] As we know by now, the CI/CD pipeline building is an iterative process, where we start with something simple and basic and build incrementally. To safe time, we will not let the runner run each of the build iterations in realtime. However, when you're building the pipeline as I have, I would highly recommend to let the runners complete the job per step to make sure each step is successful before moving on to the next, and that's how I usually build my CI/CD pipeline. And we'll follow the same process that we've done before, we'll perform something simple, then gradually build on top of the previous step. I took the first initial step of synchronizing the GitLab repository with the local code that we've done before, so as you can see here, I went ahead and do a [Git](../../Skills/Software%20Development/Git.md) commit, git push, git add, get push to this remote repository and now we have the exact same files as we have on the local repository. So let's go ahead and build this pipeline. Oh, by the way, I did add an extra file which is requirements.txt. This basically allows us to not having to do a pip install for every single package. So basically what we've done is we did a pip freeze, as you could see, this specifies all the packages, all the dependencies that [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) uses, and what we did was pip freeze and then just go ahead and redirect all the outputs to requirements.txt so that we could use the single pip -pip install -r command to install the necessary packages and dependencies
>
> **[1:35](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/gitlab-integration-for-configuration-backup?u=76281980&t=95)** that we need. Let's go ahead and build the pipeline. Let me go ahead and delete whatever we had before and build from the ground up. So to begin with, we could probably just go ahead and have one single stage like we've done before, put one job, specify the image, and hard code all the environmental variables using the variables keyword. This [Ansible](../../Skills/Network%20and%20System%20Administration/Ansible.md) host key checking is a way for Ansible to not error it out, if this is the first time that you're SSH to device, because they do rely on (indistinct) to SSH to a device. So this is pretty straightforward, the script we've done is pip3 install -r reading from the requirements file, and we're going to set the source as stage and then we're going to run that playbook for our stage hosts. We could go ahead and commit the change, and we could go ahead and let it run, however as I mentioned before, to save some time, I'm just going to cancel this pipeline to not waste our time, but let's go back to the editor and keep building. Let's assume it works, we know if we hard code it it would work, but we don't want to hard code the variables, so how do we reference this using environmental variables so it's more flexible? And the way to do that is actually use very similar to shell script, that we could just use this dollar sign as well as the curly bracket to go ahead and say for these variables, we want to reference the environment variable
>
> **[3:10](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/gitlab-integration-for-configuration-backup?u=76281980&t=190)** in this repository. Let me go ahead and paste in the last one, and we can go ahead and save the change, and where do we enter these environmental variables? We go to Settings, CI/CD, and there's the variables where we could enter these variables. As you can see, I've already done entering, and we could also add new ones and optionally, we could protect the variables per branch, but usually I just uncheck it and call it good, especially during testing. So let me go back to the editors, so now we have these variables grabbing from the repository and the environmental variable from this repository to make it more flexible. And as we've done before, we want to keep some of the files produced by our runner, we need to use the artifacts. So let's go ahead and say artifact, and specify the paths, and in this case, remember we have two folders or two paths that we would produce potentially. One is for staging hosts and one is for production hosts. What we want is the production host, so we'll just say backup, and we specify the backup. There's also a useful feature to set the expiration so we don't get a bunch of old configuration backup files, so let me go ahead and say it's one week. Okay, so that's all and good. What we could also do is on the job level, we could actually protect it with the branch. So this job, if I say only specify the main branch,
>
> **[4:49](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/gitlab-integration-for-configuration-backup?u=76281980&t=289)** that means this job will only run in the main branch and ideally it's something being merged in. This would make more sense when we're separating out the stage and the deploying production. So a lot of times if you have a separate branch, you only want this job to run in branches and not the main branch. We only run production playbooks in the main branch, from code check ins. And we could also take advantage of some of the Ansible self tests features, and put them in the test stage, and this is also where GitLab runners will shine, because remember, the GitLab runners can run in parallel, so we could run many, many different tests in parallel if we have enough runners to speed up the build process, and then we could save a lot of time that way. So let's go ahead and add a stage for a test, and we're going to add two tests, which is the test syntax, and we'll test the inventory, and I'll show you in a bit what those mean. So Ansible Playbook provides a check syntax to make sure the syntax of your playbook is valid before you move on. So this is a quick way, and very cheap, quick way to check that nobody's checking the playbook with a malformed syntax. And this inventory test, if you remember the Ansible inventory can have various different locations to put variables.
>
> **[6:21](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/gitlab-integration-for-configuration-backup?u=76281980&t=381)** There's host variable, group variable, and some other places, so what we want to do is we could do a quick check to make sure how Ansible views the inventory variables. So those are the two parallel tests that we could implement to check syntax as well as the inventory. Now that it's done, we could go ahead and add another deploy like the actual deployment for our job, so I'll just say this is deploy stage, and now that is the actual deploy, and this is deploy stage. So this is only to test our stage environment, and what we could do is we're going to add a deploy into our production environment, which is to back up our files. And now that we've separated that stage, remember, we don't need the artifacts here, and we don't need the protection. Some people are checking code on the feature branch or bug fixes, then we run only to the deploy stage, and only when the code's been merged to the main branch that it'll run in the production devices. So now, we could go ahead and commit the change. And we'll let it run and we'll come back and look at the results, and the last step, once that is done, we could optionally run this in the schedule pipeline, which makes a lot of sense for our particular playbook if we're just doing a nightly backup of inventory devices, then we would run it, you know, say 3:00 a.m.
>
> **[7:56](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/gitlab-integration-for-configuration-backup?u=76281980&t=476)** in the morning, 2:00 a.m. in the morning, and we saw how we could do that just by clicking New and using the Chron syntax. So I'm going to pause the video now and come back to it to view the actual results of our final CI/CD pipeline. All right, the job has finished, and we can see it from this pipeline and all the testing stages have been done, deployment stage has been done, and deploy stage has been done. One of the most useful feature we could implement as the job goes longer and longer is to implement some kind of integration for notification. I've included the email integration link as well as some instructions on where to specify email, server, username, password, so on in the installation instruction with this course. So if you have done that, then you will actually get a notification by default on whether your pipeline failed or succeeded. So that's very useful in kind of walking away, grab your coffee and come back when it's needed. Of course, when we see these test inventory, we can actually look at the results on what those are, the two test syntax. So that's good, and also, the deploy stage, and everything has succeeded, as well as making sure when we deploy to production that we could go ahead and download the artifacts. So awesome. As you can see, building a pipeline is an iterative process, and it's a combination
>
> **[9:31](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/gitlab-integration-for-configuration-backup?u=76281980&t=571)** of utilizing GitLab's utility or whatever tools you choose for your [DevOps](../../Topics/DevOps.md) pipeline, their utilities as well as the underlying tools utility, in this case, Ansible for testing, for deployment, and so on. So as we start adding more and more steps, stages, features to our build pipeline, it could get long and it could get pretty complex. So the next logical step would be to optimize those by combining different steps if we could, or using some sort of caching so it doesn't have to download a bunch of different images and Python packages all the time, and those would be ideally part of the DevOps guidelines, is to keep the whole build process from build to deploy within five minutes, and that is a goal that I typically shoot for when I'm building my pipeline.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Ansible](../../Skills/Network%20and%20System%20Administration/Ansible.md) (7), [Git](../../Skills/Software%20Development/Git.md) (3), [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (2), [DevOps](../../Topics/DevOps.md) (2)
> **CLI Commands:** ansible (7), make (5), pip (5), cd (4), git (3)
> **Definitions:** is a  (3), is an  (2)
> **Tools:** gitlab (4)
> **Prerequisites:** install (4)
> **Cross-References:** go back to (2), in the last (1)
> **File Paths:** requirements.txt (2)
> **Ports:** :00 (2)

#### [Network API application example](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/network-api-application-example?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/network-api-application-example?u=76281980&t=0)** - [Instructor] Let's take a look at a [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) API example of application, and in the next lesson, we'll put this API application into our CI/CD pipeline. This isn't actually a skill down example from my book, "Mastering Python Networking", third edition. This is not an advertisement. I'm just saying the code is available online. So if you want to a look at a more in depth version with database authentication, you could feel free to go ahead and do that. But the example we're going to use here is a more simplified version. There are several tools we'll use. We'll use a very popular Python web framework called Flask. It's called a micro framework, means, it only includes the core functionality within the Flask package. And typically the very simple level you could use one single file to build your API front-end. We're also going to use PyYAML package. This package is used for Python to write and read YAML file. If you remember the Nornir package that we've using up to this point requires a host file that's a YAML format. So we're going to use the PyYAML to write the values to that host file, and then launch and execute our Nornir package, and get the result that we want. For client site testing, this is one of my favorite lightweight client testing Python package, which is called HTTPie. It has color coded syntax, very intuitive usage, as well as launching really easily and lightweight. So this is one of my favorite package that we're going to use.
>
> **[1:33](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/network-api-application-example?u=76281980&t=93)** In the code, I've included a Flask example, and let's take a look. The Flask example actually is very simple. If you look at the quick start guide, it almost map step by step as the quick start guide. Basically it has the Flask package that's being installed. It has a simple [Routing](../../Skills/Network%20and%20System%20Administration/Routing.md). It has routes to route and it'll return, Hello World. It has a more specific path. It goes into devices then it returns whatever this function returns. And it also includes a variable which automatically becomes a value that's entered into your function, and you can return back. So that means it becomes pretty flexible at what you could do with that value that you take in, and you process it, return the value for the client. So let's look at this simple Flask example and see how it goes. Once we launch it, it's going to bind into port 5000. So if I go back to my HTTPie client, if I just say 0.0.0 at port 5000. and I am, Hello World, at the route, right? So if I do slash devices, I'm going to see whatever string that's returned here, and voila, we're at devices. And now if we see a variable, say devices, so R1, R2, R2000, this value is going to change accordingly. And that is how we could take a variable in. There's also a PyYAML example
>
> **[3:07](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/network-api-application-example?u=76281980&t=187)** with a simplified usage of PyYAML, basically, we have a Python dictionary value C iOSV-1 which matches out well to the format that we're going to need for Nornir package. Let me go ahead and just delete those. And so you could see the value being pop up. So it's going to open up this file and basically jam these values into that file. If I just do PyYAML example, and you could see now is iOSV-1 and it the same format that we needed. So now, if we combine all of those together, along with the Nornir package, we have our application front-end, right? So in this case, I'm using dictionary in the more elaborate example, I will use a actual database, but here, I just want to keep everything in the single file. So I'm using the device's dictionary. I have a step function where I just at the route of my URL. However, this is what we're after, we're going devices. We have a variable name for whatever host name that we choose to. And we have a version, slash version, and this function will actually dynamically go create the device and give us the show version output. And how, how does it do that? It takes in the value from the user, with the host name, it retrieves the IP address and retrieves the OS from our dictionary, devices dictionary, writes the YAML file, initialize our Nornir function, go out and query the device for show version
>
> **[4:41](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/network-api-application-example?u=76281980&t=281)** and return the output back. So let's take a look at the usage. If I go back to the first terminal, and let me go ahead and terminate this. And if I say Python, main.py, I'm launching this new application. And now if I do HTTP 5,000, I am at route. However, if I do, let show back and say, device, what should I say, LAX edge R1 version. And now it's going to go ahead and query, and give me back my LA edge R1 show version output. And if I say LAX cor R1, and then it's going to go and query that device in real time and come back and tell me the NX-OS output. So in a nutshell, that is the application, pretend you're developer, and you've constructed this really useful API. And now we're trying to take this application, and I'm going to tie into my [DevOps](../../Topics/DevOps.md) pipeline, and say, how can we do a CI/CD pipeline? That's what we're going to do next.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (7), [Routing](../../Skills/Network%20and%20System%20Administration/Routing.md) (1), [DevOps](../../Topics/DevOps.md) (1)
> **Env Vars:** api (4), yaml (3), lax (2), r2000 (1), url (1)
> **CLI Commands:** python (7), cd (2)
> **Cross-References:** go back to (2), in the next (1)
> **Code Identifiers:** iosv (2)
> **Ports:** port 5000 (2)
> **Definitions:** is a  (1), is called (1)
> **File Paths:** main.py (1)

#### [Network API integration with GitLab](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/network-api-integration-with-gitlab?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/network-api-integration-with-gitlab?u=76281980&t=0)** - [Instructor] Let's go ahead and see how we could push the API application we built into production. More often than not, nowadays, we'll deploy our servers as microservers in the Docker container. So for our deployment, we'll go ahead and ask the application developer, in this case, it's us to build the application as a Docker container so we could upload to our Docker Hub. In doing the prep work, we need to first make sure our Flask app is up for production and in this case, I'm not going to be very careful about it. I'm just going to say debug equals to False to prep that into a production package. Then I'm going to build my Dockerfile. This is the sync file that we've seen before and I'll also attach all the resources and these configurations along with the course if you want to take a closer look but basically, we put together the Dockerfile and we could build the Docker image via this Dockerfile by using the command of docker build read this file and make a target of the Flask. I'm naming it flask_example_1 into the Docker image. And once that is built, all of these steps takes a while and therefore, I've pre-executed them and I'm just showing you the output. Once that is built, we could go ahead and do a docker run and run this example. And we go ahead and use the docker run to map the same port, local port 5000 to the Docker port 5000 and using another window to see if we could go ahead
>
> **[1:32](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/network-api-integration-with-gitlab?u=76281980&t=92)** and pull the same information. Nothing breaks just because it's a Docker container. So that is all and good. Now let's see how we could build this application via our GitLab CI/CD pipeline. If you're thinking Hey, if we're using Docker image as our runner, how can you use one Docker to build another Docker image? And the way to do that in GitLab is using this feature called Docker-in-Docker. Basically, what we need to do is we need to register a separate runner that has a specific Docker image, as well as a privileged elevation as the runner in order to enjoy this Docker-in-Docker feature. So what I have done is I went ahead and registered a new runner and because this runner has a elevated privilege, I'm going to show you real quick. This runner has an elevated privilege, I also give it a description of my Docker runner to specify between the two share runners. So therefore, I have went ahead and dedicated this runner for this particular project. And this is not a reversible change. Once you dedicate a runner for a specific project, in order to use this runner, you have to either specify here or you specify in the project to use this runner for those project and this in the admin-only area for you to specify. So once you've done that,
>
> **[3:05](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/network-api-integration-with-gitlab?u=76281980&t=185)** we could go back to our project where we have the files. This is called the python_development project and we could go back to settings, the CI/CD and go into Runners, and I have went ahead and disabled the shared runner, which is used by all the other projects, and the available specific runner is here because we need the elevated privilege as well as the specific Docker image. So now that is ready, we could go ahead and build our pipeline. So if we come back to the pipeline, the editor, we could see this is a simple pipeline and what we've done is we went ahead and used these variables to go ahead and declare the Docker image tag, as well as the username and password, as well as the registry. So what are these $CI_REGISTRY_IMAGE and CI_COMMIT_SHORT_SHA? These are called the predefined variables. And what that means is there's a ton of predefined variables within the runner from GitLab for us to use and by default, they would have some assigned value. So for example, CI_REGISTRY by default goes into the registry for GitLab. So what we also need to do is to override these registries or override these variable values by going into Settings and go into General. I'm sorry, CI/CD,
>
> **[4:37](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/network-api-integration-with-gitlab?u=76281980&t=277)** and you come into these variables and we could override the variables CI_REGISTRY, IMAGE and so on with the value that is fitted for us. So for me, I'm using Docker Hub as my registry and therefore, I would override those variables with the value that I need. However, going back to my registry, the one that I don't need to override is this COMMIT_SHORT_SHA, which is a predefined variable that comes by default when I build my image. So once that is specified, I could just go ahead and specify the same Docker image as my runner. Specify the service for Docker-in-Docker. Go ahead and log in for the specific registry. Goes ahead and build that image and push that image over. And then now we could check the pipeline of what we've done before and let me go ahead and show you how long it was. Especially if you're running for the first time, it's going to take a while, which is why I pre-executed them so we don't have to waste time. It took a duration of 10 minutes, so therefore, it's not time wise spent if we just sit here and watch it. However, we could see it's pulling the image, it's doing everything that we would have done in front of a terminal but it's doing for us via a GitLab runner. And it pushes over, copies the image over and pushes over to Docker Hub. And if I go ahead and check,
>
> **[6:12](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/network-api-integration-with-gitlab?u=76281980&t=372)** I could see that this image was pushed over with devops_python_example and now anybody with the right access could come and pull that image and deploy in their own environment. So in short, I know this is a bit of a advanced features, which is why I want to leave it to the end to explain once we have a familiarity with what [DevOps](../../Topics/DevOps.md) principles are, what the pipeline entails, so these are kind of advanced features if you want to have dedicated runner instead of shared runner and the dedicated runner has elevated privilege, and using some of the other more advanced features for GitLab.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [DevOps](../../Topics/DevOps.md) (1)
> **CLI Commands:** docker (26), cd (3), make (2)
> **Env Vars:** ci_registry (2), api (1), ci_registry_image (1), ci_commit_short_sha (1), image (1)
> **Tools:** gitlab (6), terminal (1)
> **Definitions:** is a  (3), is called (1)
> **Code Identifiers:** python_development (1), devops_python_example (1)
> **Ports:** port 5000 (2)
> **Cross-References:** go back to (2)


### 4. Testing in DevOps

[↑ Back to Table of Contents](#table-of-contents)

#### [Testing fundamentals](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/testing-fundamentals?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/testing-fundamentals?u=76281980&t=0)** - [Instructor] Let's talk about testing fundamentals in [DevOps](../../Topics/DevOps.md). DevOps allows development and operation teams to collaborate smoothly and deliver results continuously. We have seen relevant tools and technologies that could help us do that in this course. One area we have lightly touched on, but haven't spent a lot of time going over is testing. Testing is an integral part to ensure we have high quality software and operation procedures by detecting errors and conflicts as soon as possible. Of course, testing is nothing new. Any form of verification is a form of testing. In traditional testing, we have had dedicated testing teams that perform [Quality Assurance](../../Skills/Software%20Development/Quality%20Assurance.md) at the tail end of the development cycle. Or we would have infrastructure testing and monitoring tools after we deploy our gears. There are manual processes that might take days or weeks to complete. Because of the manual process, it could be error prone and difficult to keep a standard of quality over time. In DevOps, testing is a continuous and automated process. They're integrated into every step of the way. They're also not just a responsibility of one team, testing responsibilities are shared amongst all team members. This shared responsibility should make a lot of sense. I mean, who else can do a better job of testing than each of the subject matter experts. Testing could also be categorized into several types.
>
> **[1:35](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/testing-fundamentals?u=76281980&t=95)** In [Unit Testing](../../Skills/Software%20Development/Unit%20Testing.md), we test a small unit of a function, code block or procedure. This is the smallest testable part of a code or operation. In functional testing, we try to ensure our code or operation is acting in accordance to our specified requirements. For system testing, we want to verify and evaluate if multiple components are working well together while fully integrated. For end to end testing, we test both the functionality as well as the performance of an application under production-like environment. Other test includes performance, load, security, compliance or [API Testing](../../Skills/Software%20Development/API%20Testing.md). As we can see from the list, the test moves from small to large in scale. The end to end testing will require the most time and effort while unit tests will require the least. In practice, we will spend most of the time in unit test because they're the easiest to do and can point us to a more precise cause of an error if any. If you recall from previous lessons, network engineers used to have difficult times with testing environments because network [Hardware](../../Topics/Hardware.md) is expensive and difficult to manipulate. With the advance of virtualization, we could typically use platforms such as CML, GNS3 and Eve-NG for many of the tests we want to do. We also have several testing frameworks available to choose from. We can use pytest to test our [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) code
>
> **[3:08](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/testing-fundamentals?u=76281980&t=188)** as well as frameworks such as pyATS, Batfish and Suzieq to simplify our narrow testing.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [DevOps](../../Topics/DevOps.md) (3), [Quality Assurance](../../Skills/Software%20Development/Quality%20Assurance.md) (1), [Unit Testing](../../Skills/Software%20Development/Unit%20Testing.md) (1), [API Testing](../../Skills/Software%20Development/API%20Testing.md) (1), [Hardware](../../Topics/Hardware.md) (1)
> **Env Vars:** api (1), cml (1), gns3 (1)
> **Definitions:** is a  (2), is an  (1)
> **CLI Commands:** make (1), python (1)
> **Analogies:** such as (2)
> **Code Identifiers:** pyats (1)
> **Speakers:** - [instructor] (1)

#### [Testing with pytest](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/testing-with-pytest?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/testing-with-pytest?u=76281980&t=0)** - [Instructor] Let's see some examples of how we could use Pytest to validate our code as well as our network equipment. Before we can use pyts we need to install it. So we're going to install Pytest, and in the third example we're going to use with request package. So I've installed them at the same line, okay? So now we've installed them. In order for Pytest to pick up the function that we're using for testing, we have to append the function with tests. And in this case, I have two functions that we intend on using Pytest for, just for illustration purpose. We're asserting the first value here, 10, 20, 30 is equal to 10, 20, 30, the second value on the right. And here we're asserting them to not be equal. This two, 10, 20, 30, 30, 20, 10. And in the third function, because it's not prefixed with test, Pytest is not going to pick it up. So let's go ahead and run it. Pytest example one, and we could see that two tests passed. We can have a more of a gross output with a dash V option. And then we could see that the two tests passed. What if we changed this to assert? This is, try to assert passing but the two value appear to not equal. Then we could see the second test would have failed, it would give us a more verbosed, fairly verbosed reason on why that happened. And we could see that's the case. So let's switch that back
>
> **[1:33](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/testing-with-pytest?u=76281980&t=93)** and let's look at the second example. In the second example, I have a switch object that I'm initializing and some of these switch object have default value, some of them do not. For example, device type have a default value. So, nothing to specify Cisco IOS, and these others do not. So, why I'm trying to test this function and to see that if I initialize them the default values are being properly assigned. So, for example, if I initialize this and not passing any values in, the host name should be none, OS should be none, device type should have taken the default one. And the interface would have taken the default one as well. And now I'm testing the non default where when I initialized this object, I've given it a host name, given the OS, and I've changed the device type. So everything else should have changed and the interface should have taken the default one, right? So let's go ahead and run Pytest and I'll do a verbose option. Example two, and now we're seeing that everything pass. So that's a quick way to say, hey, you know I'm trying to test this particular object of various initialization that is actually working. So in the third example, I'm going to test by going out to my LAX Core URL 192.168.2.50. And I am using NXAPI to retrieve the structure output and test to see if the NX-OS version
>
> **[3:08](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/testing-with-pytest?u=76281980&t=188)** is 730D1. So let's go ahead and SSH to that device and see what that version was to begin with. So now I've SSH'd in and if I drop down here then you could see the system version 730D1. So let me go ahead and execute this so that it executes the script to get the OS version and try to see if those pass. So if I do Pytest. Pytest example three, and now they pass. So if I just change this, trying to deliberately assert it wrong and see how that goes. And you could see I'm actually assert false, because it's a zero. But we actually got one as the version coming back. So let me go ahead and do that. So those three are just quick tests or Pytests. If you have used the standard library unit test before within [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) you could kind of tell, Pytest is a lot more simpler to use as well as provides better error message when some error happens. So, Pytest is becoming the preferred testing, at least in the Python world the preferred testing module to use.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (2)
> **Env Vars:** ssh (2), ios (1), lax (1), url (1), nxapi (1)
> **CLI Commands:** ssh (2), python (2)
> **Prerequisites:** install (2), initialization (1)
> **Analogies:** for example (2)
> **Versions:** 192.168.2 (1)
> **Definitions:** is a  (1)
> **Speakers:** - [instructor] (1)

#### [Network testing with pyATS](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/network-testing-with-pyats?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/network-testing-with-pyats?u=76281980&t=0)** - [Instructor] Let's take a look at one of the exciting network testing framework, pyATS. It is a framework released by Cisco from their years and years of work internally for internal network testing, and released to the community to be used. It is a very powerful framework. Have various parsers, generates nice looking reports, triggers, and many other different components. The parser part is open source. So the community could freely contribute to the framework for different parsers, it doesn't have to be Cisco-specific. It does have a bit of a learning curve just because it's such a powerful framework, but I would highly recommend any [DevOps](../../Topics/DevOps.md), engineers with network focus to take a look at pyATS. Look at some of the reusable parsers, triggers, and the usage. And to see if this could be added to your testing framework and library. PyATS could be used in a Docker container, which is right up to our automation and DevOps framework. It also has a large amount of examples in this repository that is public. And in fact, a lot of the examples that I've seen out there as well as the examples I've used, could be taken directly from one of these examples that Cisco freely provided. So let's take a look at some of the usage. To begin with, we need to install pyATS. And the recommended installation is to do a full installation. You recognize this full keyword. What it does is it not only installs the core
>
> **[1:34](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/network-testing-with-pyats?u=76281980&t=94)** but also installs all the frameworks, the libraries, the parsers, along with it. And after you have done the installation, you could do a version-check. What you would realize is pyATS is a bunch of frameworks bundled in together. So I've felt victims of this. Sometimes I would just do a pip upgrade to install pyATS. And say for example, the Genie version will fall out of sync with the core and something will just break. And I would scratch my head all the time, not realizing what broke. The recommended installation is to do a full installation, that will ensure everything's intact. So let me go ahead and show you some examples. The first example is a combination of a testbed as well as a simple show version connection. The testbed is in the YAML format, and it includes all the credentials for the device as well as the device themselves. In this testbed, I only have one device, that's our lax-edg-r1, with the management IP address and some of the topology, interface Ips, and loopback Ips. And in the [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md), our pyATS example was taken directly from this link. You could take a look if you want to see the original one. It loads the testbed, and it specify S1 or switch one is this lax-edg-r1 device that I want to connect to. It connects to it, executes, show version, and disconnect. And the way to execute this is just to pyATS_example_1.
>
> **[3:12](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/network-testing-with-pyats?u=76281980&t=192)** And let's go ahead and take a look. And check that out. It's just automatically executes and also prints out these connection outputs as if you're executing in front of that terminal. Very similar to Pexpect or Paramiko, if you're familiar with those libraries. And generates a bunch of outputs as well as some of the cache commands. So let's take a look at a more involved example that actually does something. So for the second testbed, let me go ahead and change this. This is Example 2. And it has all the credentials for connection, again, as lax-edg-r1, but in this case, I'm also going to include lax-cor-r1 with the corresponding IP and the topology that's between those two devices. And in the second example, I am going to load both devices, load the tests. Well, I'm going to load the testbed in the command line, however, I've specified all the parameters. And what this does is actually connecting to the first device. Does a ping between these two IPs or multiple Ips. In this case, just these two Ips, and then disconnect. So let me go ahead and execute that. So what I'll do is just do a Python example two, but specifying the testbed as testbed number two. And again, this goes behind the scene, execute. Sponsor of the child process, does what is supposed to do, does the statue, the cleanup.
>
> **[4:48](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/network-testing-with-pyats?u=76281980&t=288)** Finish all these steps, and generates a test report. And you could see like the common, topology check, and everything is passed. And so, like I said, it's a very powerful framework. It does all the regression testing and all the feature testing, function testing for Cisco devices when this was an internal pool. And you can imagine those are pretty vast involved. And now that they're open source, I would highly recommend us using it for our DevOps network testing.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [DevOps](../../Topics/DevOps.md) (3), [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (2)
> **Code Identifiers:** pyats (6)
> **CLI Commands:** python (2), docker (1), pip (1)
> **Definitions:** is a  (4)
> **Analogies:** for example (1), similar to (1), imagine (1)
> **Tools:** terminal (1), command line (1)
> **Best Practices:** recommended (2)
> **Prerequisites:** install (2)

#### [Network testing in GitLab](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/network-testing-in-gitlab?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/network-testing-in-gitlab?u=76281980&t=0)** - [Instructor] One of the most common operations we do in networking is to upgrade the network OS. It might not be the most interesting job, but good news, we can actually automate that with our CICD pipeline. Also, we want to combine some testing with what we've learned in this section on pytest, as well as pyATS. As an operator, here's what I would do if I were to perform the network OS upgrade. I would check the current OS on the device. Then I would upgrade the device. I would check the device again for the newly upgraded OS, and then I would typically perform some sanity check for the [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) of the network. So if we think about it, if we want to check the current OS what we could do is we could use what we've learned from pytest, using the request package goes into a device and to assert equal that the OS version is what we would expect, right? And then we would actually have another script to upgrade the device. In this case, I have a upgrade OS which is very boring and it's just a stub script. But of course, in the real operation I would have a real steps to upgrade OS. Then I would validate the OS again with a new version of the OS. And then I would do the sanity check. And for the sanity check, I could use pyATS to do some ping tests, perhaps to validate BGPA neighbor relationship, and so on. And remember when we run those steps we would actually have a report being generated. We could actually punt that into a file
>
> **[1:35](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/network-testing-in-gitlab?u=76281980&t=95)** and have that file act as an artifact. So if I look at my GitLab CI pipeline, one thing I also wanted to mention is typically what we could also do is for our code file is to do something called linting. Linting is basically what we would check for some kind of uniform style guide, and there are tools for that. So for [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) is a center called PEP 8. You know, it tells about not leaving any extra space at the end, how many characters we should have in the line, the indentation common practice, and so on and so forth. So it's a good idea to use this, to check your code beforehand. And typically that's what we do for just linting and co-sanity check. So right here, I have several stages. I have the QA stage where I use Flake8 to check my upgrade OS.py which is the main file that I'm interested in to upgrade OS. And then I will have a test before the operating system for the remote device, where we would just validate the OS. That is what we expect. At any of these steps, if they fail, the whole operation will be terminated and would not proceed further. Right. Then I would actually perform the operate, upgrade the OS. I would also verify it again after and then I would actually do a sanity check for the network, for ping, BGB neighbor, so on and so forth. And what I would do is I would use PyATS for that function. And I would redirect all the output to a network report and keep that as an artifact
>
> **[3:09](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/network-testing-in-gitlab?u=76281980&t=189)** so I could view that later. Then, of course, this pipeline would run if I modify and this pipeline would run or I could trigger it periodically at a set time. And if I take a look at the previous pipeline then I could see each of these steps run successfully. And if I go back to the last one where I do the sanity check for the whole network and I will actually have a artifact report, I would download and verify that everything pings and it's great. So that is how we could in my way of thinking on a network operation for CICD pipeline to automate those steps using GitLab and the GitLab CICD pipeline.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) (1), [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (1)
> **Env Vars:** cicd (3), bgpa (1), pep (1), bgb (1)
> **Tools:** gitlab (3)
> **Code Identifiers:** pyats (2)
> **File Paths:** os.py (1)
> **CLI Commands:** python (1)
> **Cross-References:** go back to (1)
> **Definitions:** is a  (1)


### Conclusion

[↑ Back to Table of Contents](#table-of-contents)

#### [Next steps](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/next-steps?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/next-steps?u=76281980&t=1)** - We have covered a lot of ground in this course. If you're new to the world of [DevOps](../../Topics/DevOps.md), there's certainly a lot of information to digest. Keep in mind, DevOps is a set of principles. They're there to help you make your life easier, not to add extra layers of burden to your already stressed life. Take some time to fully appreciate the concepts introduced in the course, adopt them at the pace feels comfortable to you. There are no need to forklift upgrades. Just take one small step at a time is usually good enough. Along the way, try out some examples to further solidify the experience. I typically find students learn better with relevant hands on examples that are applicable to solving your own problems. If you would like to know more about DevOps' principles, take a look at some of the recommended list of books and classes I included with this course. They range from principles, practices, testing, network automation, [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md), and other topics. Finally, take a deep breath, relax, and enjoy the right.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [DevOps](../../Topics/DevOps.md) (3), [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md) (1)
> **CLI Commands:** make (1), find (1)
> **Definitions:** is a  (1)
> **Best Practices:** recommended (1)
> **Warnings:** keep in mind (1)
> **Speakers:** - we (1)

#### [Conclusion](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/conclusion?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/netops-devops-for-network-engineers-automating-networks/conclusion?u=76281980&t=1)** - Throughout this course, we have looked at several different and important aspects of [DevOps](../../Topics/DevOps.md) in the network engineering context. We spent some time talking about DevOps principles and practices. We also saw examples of applying them in network engineering with tools such as [Git](../../Skills/Software%20Development/Git.md), GitLab, [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md), [Ansible](../../Skills/Network%20and%20System%20Administration/Ansible.md), and various testing frameworks. We spent a good chunk of time discussing DevOps pipelines of building, reviewing, testing, and deployment. We saw network DevOps examples with configuration backup, API applications, and integrated them with different DevOps environments. We examined the importance of testing in DevOps with examples of PI tests and PIATS. I hope you have found this course to be helpful in gaining fundamental knowledge of DevOps in network engineering. Also, I hope you're now comfortable in using [DevOps Tools](../../Skills/Software%20Development/DevOps%20Tools.md) to help manage your network better. I want to thank you for your time in this course. It really means a lot to me to have spent this time with you in the virtual domain. My commitment does not end when this course is over. I will do my best to respond to comments, questions, and suggestions on Twitter, [LinkedIn](../../Skills/Web%20Development/LinkedIn.md), and [GitHub](../../Skills/Software%20Development/GitHub.md). I love learning and sharing knowledge. So please reach out to me with feedbacks, questions, or ideas. I look forward to hearing from you.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [DevOps](../../Topics/DevOps.md) (7), [Git](../../Skills/Software%20Development/Git.md) (1), [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (1), [Ansible](../../Skills/Network%20and%20System%20Administration/Ansible.md) (1), [DevOps Tools](../../Skills/Software%20Development/DevOps%20Tools.md) (1)
> **CLI Commands:** git (1), python (1), ansible (1)
> **Env Vars:** api (1), piats (1)
> **Tools:** gitlab (1), github (1)
> **Analogies:** such as (1)
> **Speakers:** - throughout (1)


## Instructor

- [Eric Chou](../../Instructors/Database%20Management/Eric%20Chou.md)

## Resources

- Exercise files available

## Skills Covered

- DevOps
- NetOps

## Path Context

### In [Network Automation Professional Certificate by Arista Networks](../../Paths/Database%20Management/Professional%20Certificates/Network%20Automation%20Professional%20Certificate%20by%20Arista%20Networks.md)
← [Introduction to Linux (2021)](../Network%20and%20System%20Administration/Introduction%20to%20Linux%20(2021).md) | **4 of 6** | [Hybrid Multicloud Networking- Practical Concepts](Hybrid%20Multicloud%20Networking-%20Practical%20Concepts.md) →

## Part of

- [Network Automation Professional Certificate by Arista Networks](../../Paths/Database%20Management/Professional%20Certificates/Network%20Automation%20Professional%20Certificate%20by%20Arista%20Networks.md)

## Appears In

- [Network Automation Professional Certificate by Arista Networks](../../Paths/Database%20Management/Professional%20Certificates/Network%20Automation%20Professional%20Certificate%20by%20Arista%20Networks.md)

## Related Courses

_Courses sharing skills:_

- [Leveraging Google Cloud Developer and DevOps Tools](../Cloud%20Computing/Leveraging%20Google%20Cloud%20Developer%20and%20DevOps%20Tools.md) — DevOps
- [DevOps Foundations- Your First Project](../DevOps/DevOps%20Foundations-%20Your%20First%20Project.md) — DevOps
- [DevOps Foundations- Monitoring and Observability](../DevOps/DevOps%20Foundations-%20Monitoring%20and%20Observability.md) — DevOps
- [Advanced Data Engineering with Snowflake](../Cloud%20Computing/Advanced%20Data%20Engineering%20with%20Snowflake.md) — DevOps
- [Automated Data Governance in Practice](../Data%20Science/Automated%20Data%20Governance%20in%20Practice.md) — DevOps

---

[↑ Back to top](#)